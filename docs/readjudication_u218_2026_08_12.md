# Re-adjudication — U-218b and U-218e (2026-08-12)

> **Why this exists.** Both units shipped with a broken review pass. On U-218b the refuter panel killed a true P1 3/3 and ~45 verify agents died on a usage limit — because a finding whose verifiers all fail scores zero kills, **15 findings landed in the "refuted" bucket unadjudicated**. U-218e never got its independent-reviewer leg (Codex out of credits). Their finding text was not preserved, so this run RE-HUNT the diffs rather than re-verifying a list.

> **Method.** 7 hunt lenses (4 on U-218b `bcc15ec`, 3 on U-218e `db25eed`) + one adjudicator per unit using **three outcomes** — CONFIRMED / REFUTED / **UNADJUDICATED** — with inability-to-verify mapped to UNADJUDICATED, never REFUTED. Deliberately small fan-out (9 agents, not 45): over-fanning is what killed the original verification. **9/9 agents completed, 0 errored/skipped/empty.**

> ⚠️ **Read the confirmation rate skeptically.** 35 CONFIRMED / 1 REFUTED / 1 UNADJUDICATED is the mirror image of the original panel's over-refuting; the adjudicator prompt's emphasis on the danger of false REFUTED likely over-corrected. The two P1s were re-verified by hand against the source (see notes). Treat P2/P3 entries as leads carrying real evidence, not as a settled backlog.

## U-218b — 20 findings

### [P1 / CONFIRMED] Re-push endpoint mints a fresh RequestId — the exact substitution this unit's own runbook forbids
**Location:** `integrations/intuit/qbo/bill/api/router.py:108` · lens `b-push`

The converted endpoint calls QboOutboxService.enqueue(), which mints str(uuid.uuid4()) as RequestId (integrations/intuit/qbo/outbox/business/service.py:107) whenever no coalescible row is found. ReadPendingQboOutboxByEntity matches only Status IN ('pending','failed') (integrations/intuit/qbo/outbox/sql/qbo.outbox.sql:286), so a bill whose push already dead-lettered ALWAYS gets a brand-new row with a brand-new RequestId. The same commit's own runbook states verbatim (docs/runbooks/qbo-outbox-dead-letter-replay.md:85-87): "The script preserves RequestId. Do not use QboOutboxService.enqueue() as a substitute — enqueue mints a fresh request id and forfeits Intuit dedup." And the same commit's report_db_to_qbo_bills prints "enqueue via bill completion or POST /sync/bill-to-qbo" (scripts/reconcile_project.py:326-329) for precisely the unmapped bills whose earlier push failed — i.e. the script actively routes operators to the unsafe path for the rows that require the safe one. The router docstring asserts the opposite ("with a durable RequestId for Intuit dedup"), which is true only for a first push. Fix at depth: when a dead_letter row exists for (Bill, entity_public_id, sync_bill_to_qbo), either reuse its RequestId (reset it to pending, i.e. the replay-script semantics) or refuse with a pointer to scripts/retry_qbo_outbox_dead_letters.py.

**Failure scenario:** Bill B is completed; the outbox handler POSTs /bill, Intuit commits it, then self.qbo_bill_repo.create() raises on a DB blip. worker._process_inner's bare `except Exception` dead-letters the row on attempt 1, so no qbo.BillBill mapping exists. The operator runs reconcile_project, sees "[DB->QBO] Bill #B — missing BillBill mapping (report only; enqueue via bill completion or POST /sync/bill-to-qbo)", and POSTs that endpoint. read_pending_by_entity finds nothing (the row is dead_letter, not pending/failed), so a new row with a new RequestId is created; sync_to_qbo_bill sees no mapping (service.py:422) and POSTs /bill again with a requestid Intuit has never seen. QBO now holds two A/P Bills for the same vendor invoice. Had the operator used retry_qbo_outbox_dead_letters.py, the original RequestId would have been reused and Intuit would have returned the already-created Bill, healing the mapping instead of duplicating it.

**Adjudicator:** Walked end-to-end. router.py:108 calls QboOutboxService().enqueue(...); enqueue coalesces only onto a row returned by repo.read_pending_by_entity (outbox/business/service.py:75-77), whose sproc ReadPendingQboOutboxByEntity filters `AND [Status] IN ('pending','failed')` (qbo.outbox.sql:286). A dead_letter row therefore never matches, and enqueue falls to `request_id = str(uuid.uuid4())` (service.py:107). The handler then runs sync_to_qbo_bill, which returns early only if a BillBill mapping exists (connector/bill/business/service.py:420-424) — for a bill whose push landed in QBO but whose mapping write failed, no mapping exists, so it POSTs /bill again under a requestid Intuit has never seen. The contradiction is real and same-commit: docs/runbooks/qbo-outbox-dead-letter-replay.md:85-87 says verbatim 'Do not use QboOutboxService.enqueue() as a substitute — enqueue mints a fresh request id …

### [P1 / CONFIRMED] reconcile_project.py has no assert_cli_system_admin: --write strands orphan header-only Bills in prod
**Location:** `/Users/chris/Applications/build.one/build.one.api/scripts/reconcile_project.py:514` · lens `b-replay`

main() (line 1189) never calls assert_cli_system_admin(), so current_user_id=None / current_is_system_admin=False for the whole run (shared/authz/context.py:22-36 defaults). Step [2/4] calls BillBillConnector.sync_from_qbo_bill for every unmapped QBO bill. Inside the connector the CREATE path runs in this order: bill_service.create(...) (entities/bill/business/service.py:205 — NO access guard, succeeds), create_mapping(...) (succeeds), then _sync_line_items -> BillLineItemConnector.sync_from_qbo_bill_line, whose first statement is self.bill_service.read_by_id(bill_id) (integrations/intuit/qbo/bill/connector/bill_line_item/business/service.py:71) -> assert_can_access_bill (entities/bill/business/service.py:580-588). _should_bypass() is False and dbo.UserCanAccessBill(NULL, 0, id) returns 0 (both EXISTS branches compare = NULL; shared/sql/dbo.access_udfs.sql:114-144), so EVERY line raises EntityNotAccessibleError and _sync_line_items raises RuntimeError. The compensating rollback then runs (connector service.py:224-232): delete_mapping() is repo-level and succeeds, but delete_header() = BillService.delete_by_public_id, whose first statement is self.read_by_public_id (service.py:1267) — also guarded — so it raises, compensation.py:52-65 swallows it and logs CRITICAL. Net result: the Bill header survives with zero line items and its qbo.BillBill mapping has been deleted. The operator only sees 'sync failed: N of M bill line(s) failed to project'. This is exactly the outbox-authz boundary rule every scripts/sync_qbo_*.py already follows.

**Failure scenario:** Operator runs `python scripts/reconcile_project.py --project-id 128 --write` for a project with 12 unmapped 2026 QBO bills. Twelve dbo.Bill rows are created with IsDraft=0 and TotalAmount populated but no BillLineItem rows, and their BillBill mappings are deleted, so nothing links them back to QBO. AP totals are inflated by the header amounts, the daily reconcile (which only looks for MISSING mappings) sees them as unmapped, and each subsequent --write run mints 12 more duplicate zombie headers.

**Adjudicator:** Walked the whole chain. grep confirms scripts/reconcile_project.py contains neither assert_cli_system_admin nor set_authz_context, unlike 17 sibling scripts including sync_qbo_bill.py (helper at scripts/sync_helper.py:17-29). So current_is_system_admin is False and current_user_id is None for the entire run. With --write, sync_qbo_to_db_bills calls connector.sync_from_qbo_bill for each unmapped QBO bill (reconcile_project.py:514). Inside, the CREATE path runs bill_service.create (connector service.py:196) — no access guard anywhere in BillService.create; it only reads current_user_id.get() for CreatedByUserId, and VendorService.read_by_public_id/read_by_id are unguarded (entities/vendor/business/service.py:76-86) — then create_mapping (repo-level, succeeds), then _sync_line_items. BillLineItemConnector.sync_from_qbo_bill_line's first statement is self.bill_service.read_by_id(bill_id) (bi…

### [P2 / CONFIRMED] Writes-off starvation is now completely silent in the database — the queue grows unbounded with zero durable evidence
**Location:** `integrations/intuit/qbo/outbox/business/worker.py:144` · lens `b-park`

The pre-claim guard at worker.py:144-151 returns before any row is claimed, so nothing is ever marked. `git show bcc15ec^` confirms the BASELINE had no write-refused branch at all: QboWriteRefusedError (is_retryable=False) fell to `if not error.is_retryable` and `_dead_letter` on attempt 1, writing a self-explanatory LastError into qbo.Outbox. Dead-lettering healthy rows was wrong, but it was maximally durable and queryable. The replacement removed 100% of the DB signal and left nothing in its place: `QboOutboxService.enqueue` is deliberately ungated (outbox/business/service.py:42-112 — it always creates or coalesces a row), the drain endpoint returns `{"qbo": "ok"}` HTTP 200 so the scheduler sees success (shared/api/admin.py:158-167), and there is no backlog detector anywhere — `ReconciliationService.reconcile_bills` runs only qbo_missing_locally and qbo_voided, with `local_missing_qbo` still an unimplemented TODO (reconciliation/business/service.py:106). The sole replacement signal is a payload-free ERROR line (worker.py:145-150). Aggravator: the endpoint calls `drain_once()` once per 30-60s tick, so a backlog accumulated during the window clears at only ~60-120 rows/hour. Fix at depth: emit a durable signal (a qbo.ReconciliationIssue or a per-row LastError stamp) or record queue depth on the skip event.

**Failure scenario:** ALLOW_QBO_WRITES is dropped or mistyped in App Service Application Settings during a deploy (the same class of accident as the pinned-tag and ACR incidents in TODO history). Every completed Bill enqueues an outbox row that is never claimed. qbo.Outbox contains only ever-growing `pending` rows with no error text; [qbo].[ReconciliationIssue] is empty; `retry_qbo_outbox_dead_letters.py` reports "No dead-letter rows matched"; the drain endpoint reports ok. Bills look complete in build.one and are absent from QBO, so weekly AP bill-pay silently omits them until someone reconciles by hand weeks later.

**Adjudicator:** Every supporting fact checks out. `git show bcc15ec^:.../worker.py` confirms the baseline had NO write-refused branch, so QboWriteRefusedError (is_retryable=False) fell straight to `if not error.is_retryable` → _dead_letter with a self-explanatory LastError. The replacement guard (worker.py:144-151) returns before qbo_app_lock/claim, so no row is ever marked. QboOutboxService.enqueue is ungated and always creates or coalesces (service.py:75-112). The drain endpoint returns {'qbo':'ok'} on HTTP 200 regardless of drain_once's False return (shared/api/admin.py:158-167). ReconciliationService.reconcile_bills runs only qbo_missing_locally and qbo_voided with local_missing_qbo an explicit TODO (reconciliation/business/service.py:100-106). So there is no durable CAUSE signal and no backlog detector. Downgraded from P1 for two reasons the finding overstates: the ever-growing `pending` rows in qb…

### [P2 / CONFIRMED] The runbook shipped with this unit documents a symptom that never existed, and points the operator at an empty query
**Location:** `docs/runbooks/qbo-outbox-dead-letter-replay.md:11` · lens `b-park`

This runbook was created in the same commit (bcc15ec) but describes the pre-review v1 of the change. Line 11 lists `qbo.outbox.row.write_refused_park_ceiling` as an App Insights symptom and line 45 lists `Write-refused park ceiling exceeded` as a LastError pattern; line 4 says rows reach dead_letter when "a park ceiling tripped". Grep across the repo finds that identifier only in this runbook and TODO.md — the ceiling was deleted in this same commit and the event is never emitted. Worse, "Recovery C" (lines 104-107) instructs the operator to find rows with `LastError LIKE 'Parked: QBO writes disabled%'` and `Status='failed'` — precisely the population the new pre-claim guard prevents from ever existing in the writes-off case (worker.py:144-151 returns before `_process` runs, so no row is ever re-marked). The Prevention section (129-135) never mentions `qbo.outbox.drain.skipped_writes_disabled`, the only signal that actually fires. `scripts/retry_qbo_outbox_dead_letters.py:11-12` carries the same stale claim ("ALLOW_QBO_WRITES was accidentally off and rows dead-lettered after exhausting retries"), now unreachable.

**Failure scenario:** On-call is paged for "bills not reaching QBO" and works this runbook. Step 1 (dead-letter inventory) returns zero rows. The App Insights query for `qbo.outbox.row.write_refused_park_ceiling` returns zero events because the event does not exist. Recovery C's `LastError LIKE 'Parked: QBO writes disabled%'` returns zero rows. Every documented diagnostic is empty during the exact incident the runbook exists for, so the responder concludes the outbox is healthy and looks elsewhere while completions keep piling up.

**Adjudicator:** All four claims verified against the shipped files. Runbook lines 4, 11 and 45 reference a park ceiling / qbo.outbox.row.write_refused_park_ceiling / 'Write-refused park ceiling exceeded'; repo-wide grep finds those strings only in this runbook — no .py emits them. Recovery C (lines 104-108) tells the operator to find `LastError LIKE 'Parked: QBO writes disabled%'` with Status='failed', but the pre-claim guard (worker.py:144-151) returns before any row is claimed in the writes-off case, so that population is created only by the mid-handler defence-in-depth branch, which needs a flag flip inside a single tick — effectively never. The Prevention section (129-136) names qbo.outbox.row.dead_lettered but never qbo.outbox.drain.skipped_writes_disabled, the only event that actually fires. scripts/retry_qbo_outbox_dead_letters.py:11-12 does carry the now-unreachable 'ALLOW_QBO_WRITES was acciden…

### [P2 / CONFIRMED] The write gate also disables U-215's stranded-row reclaim, which is a DB-only operation and needs no QBO write
**Location:** `integrations/intuit/qbo/outbox/business/worker.py:158` · lens `b-park`

`_reclaim_stranded_rows()` sits inside the block guarded by worker.py:144-151, so it is skipped whenever ALLOW_QBO_WRITES is not true (and, pre-existing from U-211, whenever the budget breaker is tripped). Reclaim touches only SQL Server — `repo.reclaim_stranded` calls `ReclaimStrandedQboOutbox` (integrations/intuit/qbo/outbox/sql/qbo.outbox.sql:520-566), which issues one UPDATE and no QBO API call — so gating it behind a QBO *write* flag is a scope error, not a safety property. The consequence compounds: `ReadPendingQboOutboxByEntity` matches only `Status IN ('pending','failed')` (qbo.outbox.sql:286), so a row stranded `in_progress` is invisible to the Policy C coalesce in `QboOutboxService.enqueue`, and a re-completion of the same Bill mints a SECOND row with a fresh `uuid4()` RequestId (outbox/business/service.py:107). Fix: hoist `_reclaim_stranded_rows()` above both pre-claim guards (it already runs under the drain applock, which is all it needs).

**Failure scenario:** A container restart during a deploy strands a `sync_bill_to_qbo` row in_progress after its POST reached Intuit but before the mapping was stored, and ALLOW_QBO_WRITES is off during the same maintenance window, so reclaim never runs. The operator re-completes the Bill; coalesce cannot see the in_progress row, so a second outbox row is created with a different RequestId. When writes are restored inside DEFAULT_RECLAIM_AFTER_SECONDS=900 (worker.py:65), the stranded row is still not reclaimable and the new row is the only claimable one — it POSTs with a `requestid` Intuit has never seen, so Intuit's idempotency cannot dedup it, producing the duplicate QBO bill this unit was created to eliminate.

**Adjudicator:** Structurally verified. _reclaim_stranded_rows() is called at worker.py:158, inside the `with qbo_app_lock(...)` block that both pre-claim guards return before reaching (budget at 126-136, writes at 144-151). ReclaimStrandedQboOutbox (qbo.outbox.sql:518-566) is a single UPDATE against qbo.Outbox with an OUTPUT capture and no external call — gating it behind a QBO *write* flag is a scope error, exactly as claimed. The compounding is real: ReadPendingQboOutboxByEntity matches only pending/failed (qbo.outbox.sql:286), so an in_progress stranded row is invisible to the Policy C coalesce and a re-completion mints a second row with a fresh uuid4 RequestId (service.py:107). Practical weight is higher than the finding's writes-off framing suggests because the same block sits under U-211's budget guard, and the budget breaker HAS tripped in prod for a full month — during which no stranded row coul…

### [P2 / CONFIRMED] Excel->DB orphan detection treats 'outside this run's narrow snapshot' as 'not in DB' and clears col Z
**Location:** `/Users/chris/Applications/build.one/build.one.api/scripts/reconcile_project.py:779` · lens `b-replay`

check_excel_to_db decides a col-Z public_id is orphaned by membership in all_bill_public_id_map / all_expense_public_id_map (built at lines 1009-1018), and on a miss calls update_excel_range(..., f"Z{excel_row_num}", [[""]]) in write mode. Those maps are not 'the DB' — they are a heavily filtered snapshot: _load_bills_for_project:208-216 selects only bills with b.IsDraft=0 AND YEAR(b.BillDate)=2026 AND at least one bli.IsBilled=0 for this project, then filters per bill to li.project_id == project_id and not li.is_draft (same shape for expenses at 254-263). A line item that exists in the DB but sits outside that window — most commonly a fully-billed bill (every project line IsBilled=1), but also a 2025-dated bill whose Excel row carries a 2026 date, or a line coded to a different project — reads as 'orphaned'. The row-scope filter (line 758) only skips rows whose col H DRAW REQUEST is non-empty, and nothing in the codebase writes col H (grep for draw_request across the repo hits only tests and the Box folder-name helper) — it is a human-maintained column, so 'DB says IsBilled=1 while Excel col H is still blank' is a normal steady state, not a rare window. Col Z is the idempotency key that sync_to_excel_workbook uses to avoid re-appending a line; clearing it is destructive and only recoverable from SharePoint version history. The correct orphan test is a lookup by public_id against the unfiltered table, not membership in the reconciliation snapshot.

**Failure scenario:** Project 128 has 60 workbook rows for bills that were invoiced last month (BillLineItem.IsBilled=1) but whose col H was never stamped by the PM. `reconcile_project.py --project-id 128 --write` reports all 60 as 'orphaned (not in DB)' and blanks col Z on each. The DB<->Excel link is gone; the next completion or a later --write run where any of those lines re-enters scope sees the public_id missing from col Z and appends 60 duplicate cost rows to the DETAILS tab, double-counting project cost in the G702/G703 rollups.

**Adjudicator:** Mechanism verified exactly. check_excel_to_db decides orphanhood by membership in all_bill_public_id_map/all_expense_public_id_map (reconcile_project.py:766-769) and, in write mode, calls update_excel_range(..., f'Z{excel_row_num}', [['']]) (777-781). Those maps are built at 1008-1018 from all_bill_li_by_id/all_exp_li_by_id, i.e. the snapshot from _load_bills_for_project, whose SQL is `WHERE bli.ProjectId = ? AND b.IsDraft = 0 AND YEAR(b.BillDate) = 2026 AND bli.IsBilled = 0` plus a per-bill `li.project_id == project_id and not li.is_draft` filter (208-232; expenses identical at 254-278). A bill all of whose project lines are IsBilled=1 is therefore absent from the map while existing in the DB, as is a 2025-dated bill or a line coded to another project. The row-scope filter is only `if draw_request: continue` on col H (758-759), and nothing in the codebase writes col H — grep for draw_re…

### [P2 / CONFIRMED] Replay-script parameter assertion is a self-comparison — the two datetime binds are pinned to themselves
**Location:** `tests/test_u218b_bill_push_surface.py:326` · lens `b-vacuity`

`assert update_call.args[1:] == (update_call.args[1], update_call.args[2], 10)` compares the captured call's arguments against themselves. `cur.execute(sql, now, now, *ids)` produces `args = (sql, now, now, 10)`, so `args[1:] == (args[1], args[2], 10)` reduces to `len(args) == 4 and args[3] == 10`. The two datetime binds — NextRetryAt and ModifiedDatetime in `scripts/retry_qbo_outbox_dead_letters.py:143-155` — are the load-bearing part of the replay UPDATE (they are what makes a reset row actually get picked up by the drain within ~30s), and nothing in the suite asserts anything about them. This is the same class of defect the commit message claims mutation testing cleared out of this file; it survived because the vacuity is inside a comparison that *looks* like it pins the bindings. The sibling assertions in the same test (`"AND Status = 'dead_letter'" in sql`, `"RequestId" not in sql`) are real; only this one is empty.

**Failure scenario:** Change the first bind to a tz-naive `datetime.now()` (a known hazard in this repo — the rest of the outbox stack is tz-aware UTC), or to `now + timedelta(days=30)`, or reorder the SET clause so NextRetryAt receives what was meant for ModifiedDatetime. `test_retry_qbo_dead_letters_apply_reasserts_dead_letter_status_and_preserves_request_id` still passes, as does the whole suite. An operator runs `--apply` during an incident, sees "Reset N row(s) ... Worker will pick them up within ~30s", and the rows sit unreplayed (wrong-timezone or far-future NextRetryAt) with Status='pending' and no error anywhere.

**Adjudicator:** Read the assertion literally: tests/test_u218b_bill_push_surface.py:326-330 is `assert update_call.args[1:] == (update_call.args[1], update_call.args[2], 10)`. With cur.execute(sql, now, now, *ids) the captured args are (sql, now, now, 10), so args[1:] == (args[1], args[2], 10) reduces to len(args) == 4 and args[3] == 10 — the two datetime binds are compared against themselves and nothing constrains their value, tz-awareness or ordering. Grep confirms this is the only test touching the replay UPDATE's parameters; the sibling assertions on the same call ('AND Status = \'dead_letter\'' in sql, 'RequestId'/'LastError'/'DeadLetteredAt' not in sql) are genuine. The named mutations — a tz-naive datetime.now(), a far-future NextRetryAt, or a swapped SET ordering — would all leave this test and the suite green, and the operator-visible message still promises 'Worker will pick them up within ~30s…

### [P2 / CONFIRMED] Router draft-guard and 404 are untested — deleting either leaves the suite green, and the outbox handler does not re-check
**Location:** `tests/test_u218b_bill_push_surface.py:46` · lens `b-vacuity`

`test_sync_bill_to_qbo_router_returns_202_and_enqueues` is the only test of `sync_bill_to_qbo_router`. It sets `bill.is_draft = False` on a MagicMock and `read_by_public_id` to a truthy mock, so both `if not bill:` (integrations/intuit/qbo/bill/api/router.py:99) and `if bill.is_draft:` (:102) are exercised only on their pass-through branch. Deleting either guard leaves the test — and the whole suite — green; only *inverting* the draft guard is caught. That is precisely the mutation asymmetry the commit message documents for this unit's known-vacuous budget mock, reproduced here. It matters more after U-218b than before: `_handle_sync_bill` (integrations/intuit/qbo/outbox/business/worker.py:596-608) re-reads the bill by public_id and calls `BillService.push_to_qbo` with no `is_draft` re-check, so the router guard is now the single gate between an unfinalized bill and a real QBO write, and the write is asynchronous — the caller gets 202 and never sees a synchronous error. The commit message asserts "RBAC, draft validation and the 404 survive unchanged"; no test backs that claim.

**Failure scenario:** A future edit drops the `if bill.is_draft:` block (e.g. while moving validation into the handler and only half-finishing, the exact half-fix pattern this unit's own P1 was). Suite stays green. Someone POSTs /sync/bill-to-qbo/{id} for a draft bill with incomplete line items; the row enqueues, drains ~30s later, and `sync_to_qbo_bill` creates a real Bill in QuickBooks with partial/zero lines. Deleting the 404 guard instead enqueues a row for a nonexistent public_id that dead-letters in the handler's `raise ValueError`, also silently.

**Adjudicator:** Verified. grep across tests/ shows exactly one reference to sync_bill_to_qbo_router (tests/test_u218b_bill_push_surface.py:46-78), and it sets bill.is_draft = False on a MagicMock with read_by_public_id returning that truthy mock, so `if not bill:` (router.py:99) and `if bill.is_draft:` (router.py:102) are exercised only on the pass-through branch — deleting either leaves the assertion set (enqueue called once, 202, body) satisfied. The escalation is real: worker._handle_sync_bill (worker.py:596-608) re-reads by public_id and calls BillService.push_to_qbo, which goes straight to sync_to_qbo_bill (entities/bill/business/service.py:1845-1846) with no is_draft check anywhere; sync_to_qbo_bill requires bill_number, bill_date and at least one line but never checks draft status, and reads ALL line items via read_by_bill_id. So the router guard is the single gate between an unfinalized bill and…

### [P2 / CONFIRMED] Write-refused park test pins both operator-facing constants against themselves; the prefix is a documented runbook SQL predicate
**Location:** `tests/test_u218b_bill_push_surface.py:219` · lens `b-vacuity`

`assert kwargs["last_error"].startswith(WRITE_REFUSED_PARK_PREFIX)` and the window assertion on line 221 both use constants imported from the module under test (worker.py:46-47), so any change to either constant is invisible to the test. The park prefix is not internal: `docs/runbooks/qbo-outbox-dead-letter-replay.md:106` instructs operators to find these rows with `LastError LIKE 'Parked: QBO writes disabled%'`, making the literal string a docs contract. The sibling budget-park test 20 lines below (line 241) gets this right — it asserts the literal `"Parked: monthly QBO API budget exhausted"`, which is what keeps `scripts/unpark_qbo_outbox_budget.py:45` in sync — and line 242 independently recomputes the expected retry time via `reset_at_for_month("2026-08")` rather than reading it back from the code under test. The write-refused test copies the shape but drops both properties.

**Failure scenario:** Rename `WRITE_REFUSED_PARK_PREFIX` to "Parked: writes refused" (or widen `WRITE_REFUSED_PARK_INTERVAL` from `timedelta(minutes=15)` to `timedelta(hours=15)`). Full suite green. During an ALLOW_QBO_WRITES outage an operator runs the runbook's Recovery-C query, gets zero rows back, and concludes nothing is parked while bill pushes sit in `failed` with a 15-hour NextRetryAt.

**Adjudicator:** Verified line by line. tests/test_u218b_bill_push_surface.py:20-24 imports WRITE_REFUSED_PARK_PREFIX and WRITE_REFUSED_PARK_INTERVAL from the module under test; line 219 asserts last_error.startswith(WRITE_REFUSED_PARK_PREFIX) and line 221 brackets next_retry_at with before + WRITE_REFUSED_PARK_INTERVAL on both sides — renaming the prefix or widening the interval to hours leaves both green. The prefix is an operator contract, not an internal: docs/runbooks/qbo-outbox-dead-letter-replay.md:106 instructs `LastError LIKE 'Parked: QBO writes disabled%'`. The contrast with the sibling is exactly as described — line 241 asserts the literal 'Parked: monthly QBO API budget exhausted' (the string scripts/unpark_qbo_outbox_budget.py:45 depends on) and line 242 independently recomputes reset_at_for_month('2026-08') rather than reading the value back from the code under test. P2 as filed.

### [P3 / CONFIRMED] reconcile_project prints a false "all bills have QBO mappings" all-clear when every bill is unmapped
**Location:** `scripts/reconcile_project.py:1059` · lens `b-push`

check_db_to_qbo now `continue`s on any bill not in mapped_bill_ids (scripts/reconcile_project.py:304-305) instead of appending the old "missing BillBill QBO mapping" issue, so unmapped bills contribute nothing to qbo_issues. The caller still gates its success banner on `if not qbo_issues:` and prints "OK — all {len(bills_by_id)} bills and {total_lines} line items have QBO mappings" — where total_lines sums line items across ALL bills, including the unmapped ones it just skipped. The banner is therefore emitted, and is literally false, in the exact state the section exists to detect. Two supporting losses in the same hunk: stats['db_to_qbo_issues'] drops to 0 for those bills, and main()'s dry-run any_action list no longer contains the unmapped-bill counter (line 1329-1332), so a whole-fleet dry run also stops printing the "To apply all repairs, run with --write" prompt. The bill-level detection did not disappear (report_db_to_qbo_bills still prints each row), but it now arrives after a contradicting all-clear and is invisible to every aggregate the script exposes.

**Failure scenario:** Run `python scripts/reconcile_project.py --project-id 128` against a project whose bills were never pushed (e.g. the July-2026 QBO monthly cap blocked the drain, or a batch dead-lettered). mapped_bill_ids is empty, check_db_to_qbo returns [], and section [1/4] prints "OK — all 42 bills and 130 line items have QBO mappings" immediately before printing 42 "[DB->QBO] ... missing BillBill mapping" lines. An operator scanning for the OK banner — or anything consuming stats['db_to_qbo_issues'] == 0 — concludes the project is reconciled while its entire A/P is absent from QBO.

**Adjudicator:** Core defect verified. The commit diff replaced check_db_to_qbo's bill-level issue append with `continue` (reconcile_project.py:304-305), and the caller still gates the banner on `if not qbo_issues:` (1057-1059), printing 'OK — all {len(bills_by_id)} bills and {total_lines} line items have QBO mappings' where total_lines sums line_items_by_bill_id across ALL bills including the skipped unmapped ones. With every bill unmapped, qbo_issues == [] and the false all-clear prints immediately before report_db_to_qbo_bills' per-bill 'missing BillBill mapping' lines. Two sub-claims are overstated and cost it a severity notch: (a) 'invisible to every aggregate the script exposes' is false — stats['db_unmapped_bills'] is printed in BOTH the per-project summary (line 1302) and the TOTAL SUMMARY (line 1316); (b) dropping 'db_pushes' from the dry-run any_action list (1327-1332) is correct, not a loss, s…

### [P3 / CONFIRMED] Mid-handler write-refused park still increments Attempts, and its ceiling was deleted
**Location:** `integrations/intuit/qbo/outbox/business/worker.py:360` · lens `b-push`

The QboWriteRefusedError branch parks via self.repo.mark_failed (worker.py:374), which increments the same Attempts counter that MAX_ATTEMPTS gates, and returns before the `next_attempt > MAX_ATTEMPTS` check at line 396. The bounded park ceiling was removed on the argument that the new pre-claim writes_allowed() guard in drain_once (worker.py:144) makes this branch unreachable — but the branch was deliberately retained as "defence-in-depth", so on the one path where it still fires it now does so unbounded, reproducing at handler level the exact defect the unit fixed at drain level: a healthy row's shared retry budget consumed by parks that are not real attempts. Reachability is genuinely low — writes_allowed() is a pure os.getenv read (base/client.py:53-63), both checks run in the same process and tick, and client.py:324 is the sole raise site — which is why this is P3 and not higher. The right-depth fix is to either delete the now-dead branch outright or park without incrementing Attempts (a mark_parked that leaves the counter alone), not to leave an unbounded budget burn behind a 'can't happen' comment.

**Failure scenario:** Anything that makes writes_allowed() flip between drain_once's pre-claim check and the handler's first POST — an operator or in-process job mutating os.environ['ALLOW_QBO_WRITES'], or a future caller invoking _process outside drain_once — parks the row and burns one attempt, with no ceiling to stop it. After five such parks a healthy row carries Attempts=5; the next genuine retryable error (a QBO 503) evaluates next_attempt=6 > MAX_ATTEMPTS=5 and dead-letters it with "Retries exhausted after 5" despite zero completed QBO attempts — the same misleading operator signal the commit message identifies as the bug being fixed.

**Adjudicator:** Verified in shipped code. worker.py:360-380 handles QboWriteRefusedError by calling self.repo.mark_failed and returning, before the `if next_attempt > MAX_ATTEMPTS` check at worker.py:397. FailQboOutbox unconditionally does `[Attempts] = [Attempts] + 1` (qbo.outbox.sql:465), so every park burns one of the 5 shared attempts with no ceiling anywhere in the branch. Reachability is as narrow as the finding itself states: writes_allowed() is a pure os.getenv read (base/client.py:53-63), the sole raise site is _enforce_write_gate (base/client.py:324), and drain_once's pre-claim guard (worker.py:144-151) runs in the same tick — so it takes an in-process os.environ mutation or a _process call outside drain_once. The failure scenario's arithmetic is off by one (after five parks the next retryable error evaluates next_attempt=7, not 6) but the outcome — dead-letter with a misleading 'Retries exhau…

### [P3 / CONFIRMED] New replay runbook documents a diagnostic signal the same commit deleted, and its Recovery B contradicts Recovery A
**Location:** `docs/runbooks/qbo-outbox-dead-letter-replay.md:11` · lens `b-push`

Two defects in the runbook shipped by this unit. (1) Line 11 tells operators to look for `qbo.outbox.row.write_refused_park_ceiling` events and line 45 for a `Write-refused park ceiling exceeded` LastError; line 4 repeats "or a park ceiling tripped". The park ceiling was deleted in this very commit — grep across the repo finds those strings in no .py file, only in this runbook and TODO.md. An operator following Step 1/Step 2 will query App Insights for a signal that can never be emitted and mis-diagnose the row. (2) Recovery B's manual SQL (lines 96-97) sets LastError = NULL and DeadLetteredAt = NULL, directly contradicting Recovery A's script, whose docstring (scripts/retry_qbo_outbox_dead_letters.py:19-21) and success message (:167) deliberately preserve both "as audit history of why the row dead-lettered". The two documented recoveries leave the row in materially different audit states, and the destructive one is the fallback an operator reaches for under pressure.

**Failure scenario:** A bill push dead-letters during an incident. The operator opens the runbook, runs the Step-1 inventory, reads a LastError of "Retries exhausted after 5: QboServerError", then searches App Insights for qbo.outbox.row.write_refused_park_ceiling per Step 2 to rule out a write-gate cause. Zero hits — not because the cause is excluded but because the event no longer exists — so the write-gate hypothesis is wrongly discarded. The script being unavailable on the operator's box, they fall to Recovery B, which nulls LastError and DeadLetteredAt; the row replays, and the forensic record of why it dead-lettered is gone before anyone triages the systemic cause.

**Adjudicator:** Both defects verified. (1) `grep -rn write_refused_park_ceiling .` across the whole repo returns exactly one hit: docs/runbooks/qbo-outbox-dead-letter-replay.md:11. 'Write-refused park ceiling exceeded' likewise appears only at runbook line 45, and line 4 repeats 'or a park ceiling tripped'. No .py emits either string — the shipped park branch writes only WRITE_REFUSED_PARK_PREFIX ('Parked: QBO writes disabled', worker.py:46/378). The finding's aside that the string also lives in TODO.md is wrong (it does not), which is immaterial to the claim. (2) Recovery B's SQL sets `[LastError] = NULL, [DeadLetteredAt] = NULL` (runbook 96-97), while Recovery A's script deliberately preserves both — scripts/retry_qbo_outbox_dead_letters.py docstring lines 19-21 and the success message at line 167 ('RequestId, LastError, and DeadLetteredAt preserved'); its UPDATE (141-154) touches only Status/Attempts…

### [P3 / CONFIRMED] Duplicate-bill runbook never updated for the new fail-loud duplicate path it now produces
**Location:** `docs/runbooks/qbo-duplicate-bill.md:86` · lens `b-push`

This commit changed BillBillConnector's duplicate-DocNumber behavior from (intended) auto-adopt to record-a-critical-issue-and-re-raise: _recover_duplicate_qbo_bill writes a qbo.ReconciliationIssue with drift_type='duplicate_qbo_bill_docnumber', severity='critical', details ending "Manual link required", then raises the typed QboDuplicateError, which is is_retryable=False (base/errors.py:222 under QboClientError) and so dead-letters the outbox row on attempt 1. qbo-duplicate-bill.md was not touched. It has no mention of the new drift_type, no query against qbo.ReconciliationIssue in Diagnosis, and — most consequentially — no procedure for the "Manual link required" the issue text instructs. Its Common-causes list still names "Legacy pre-outbox code path still active" (line 86) with `grep -rn 'sync_to_qbo_bill' entities/` as the check, which this unit made obsolete. The project's per-unit pipeline makes the Docs step mandatory on any behavior change; this is the runbook that owns the behavior that changed.

**Failure scenario:** A bill push hits QBO fault 6140. A critical duplicate_qbo_bill_docnumber row lands in qbo.ReconciliationIssue and the outbox row dead-letters. The on-call opens qbo-duplicate-bill.md, whose Step 3 ("Check outbox history") maps this to "No rows → created by a non-outbox path" or a generic dead-letter, and whose Recovery section only covers merging duplicates in QBO and re-pulling. There is no documented way to create the qbo.BillBill mapping the issue demands, and the reconcile that could backfill it is gated behind QBO_RECONCILE_BILL_AUTOFIX (default false, per integrations/intuit/qbo/reconciliation/business/service.py). The bill stays unmapped and invisible to the DB→QBO reconcile — which, per finding 2, now reports the project as OK.

**Adjudicator:** Verified. The commit changed BillBillConnector to catch the typed QboDuplicateError, call _recover_duplicate_qbo_bill (service.py:488-499, 565-600) which records drift_type='duplicate_qbo_bill_docnumber', severity='critical', details ending 'Manual link required', and re-raise; QboDuplicateError inherits QboClientError with is_retryable=False (base/errors.py:154-157, 222), so worker._handle_qbo_error's `if not error.is_retryable` dead-letters on attempt 1. docs/runbooks/qbo-duplicate-bill.md is not in the commit's file list and its last touch predates it. Reading it confirms every gap claimed: no mention of duplicate_qbo_bill_docnumber, no query against qbo.ReconciliationIssue anywhere in Diagnosis (Steps 1-4 are QBO UI, qbo.Bill, qbo.Outbox, App Insights), and Recovery A/B/C cover merging in QBO, re-pulling, and root-cause only — no procedure for the 'Manual link required' the issue tex…

### [P3 / CONFIRMED] The write-refused park has no terminal state and no recovery tooling — its prefix constant has zero consumers
**Location:** `integrations/intuit/qbo/outbox/business/worker.py:360` · lens `b-park`

The park branch at worker.py:360-380 has no MAX_ATTEMPTS ceiling and no path to any terminal status; it only calls `mark_failed`, and `FailQboOutbox` unconditionally does `Attempts = Attempts + 1` (qbo.outbox.sql:465) with nothing in this branch reading the counter. `ClaimNextPendingQboOutbox` does not filter on Attempts either (qbo.outbox.sql:362-368), so a repeatedly-parking row is re-claimed forever at 15-minute intervals with Attempts climbing without bound. It is unreachable today only because `writes_allowed()` (base/client.py:53-63) reads one env var that cannot change inside a single drain tick — but the branch exists precisely to survive a refusal the pre-claim guard did not predict, and a per-entity refusal is a live possibility: `_recode_writes_allowed` (base/client.py:66-90) is a second QBO write gate that is checked only at `confirm()` today and is the obvious candidate to move into `_enforce_write_gate` alongside the global one. Unlike the budget twin there is also no recovery tool: `scripts/unpark_qbo_outbox_budget.py:45` matches only the budget prefix, and `WRITE_REFUSED_PARK_PREFIX` (worker.py:46) is written into LastError and read by nothing in the repo.

**Failure scenario:** A future per-entity or per-realm write gate raises QboWriteRefusedError while the global ALLOW_QBO_WRITES is true. The row is claimed every tick, parks, and re-parks every 15 minutes forever — Attempts grows without bound, the row never dead-letters, never appears in any dead-letter inventory, and no unpark script matches its LastError prefix, so it is an immortal row that only a hand-written UPDATE can clear.

**Adjudicator:** Verified. The park branch (worker.py:360-380) calls only mark_failed and returns; FailQboOutbox unconditionally increments Attempts (qbo.outbox.sql:465) and ClaimNextPendingQboOutbox filters on Status/NextRetryAt/ReadyAfter with no Attempts predicate (qbo.outbox.sql:362-368), so a repeatedly-parking row is re-claimed forever at 15-minute intervals with no terminal state. Recovery tooling is absent: scripts/unpark_qbo_outbox_budget.py:45 hardcodes PARKED_LAST_ERROR_PREFIX = 'Parked: monthly QBO API budget exhausted%' only, and a repo-wide grep for WRITE_REFUSED_PARK_PREFIX / 'Parked: QBO writes disabled' returns worker.py:46/378 (writer), tests/test_u218b_bill_push_surface.py:23/219, and the runbook literal at line 106 — no consumer that can act on it. The second-gate premise also checks out: _recode_writes_allowed exists as a parallel default-deny gate (base/client.py:66-90) outside _enf…

### [P3 / CONFIRMED] MAX_LIMIT=2098 still exceeds the effective SQL Server parameter ceiling, so the advertised max --limit fails
**Location:** `/Users/chris/Applications/build.one/build.one.api/scripts/retry_qbo_outbox_dead_letters.py:48` · lens `b-replay`

The comment reasons 'SQL Server caps bound parameters at 2100; UPDATE uses 2 datetime params + N ids' and sets MAX_LIMIT = 2098 — i.e. 2 + 2098 = 2100 user parameters, which is already AT the cap before the driver's own overhead. pyodbc executes the parameterized UPDATE at lines 141-154 via sp_prepexec/sp_executesql, which itself consumes 2-3 of the 2100 RPC parameter slots (@handle/@params/@stmt), so the usable ceiling for user parameters is 2097-2098, not 2100. The validator at lines 75-77 prints 'Error: --limit must be between 1 and 2098', actively advertising the value that fails. Note the SELECT is unaffected (TOP is interpolated, only --kind values are bound), so the failure lands after the row list has been printed and the operator has already committed to --apply. Correct cap is ~2095, or chunk the UPDATE.

**Failure scenario:** A multi-day QBO outage leaves ~3000 dead-lettered rows. The operator runs `--kind sync_bill_to_qbo --apply --limit 2098` (the value the validator told them was the max). The SELECT prints 2098 rows, then the UPDATE binds 2100 parameters and SQL Server raises error 8003 ('The incoming request has too many parameters...'); get_connection rolls back and the script exits on a traceback with zero rows reset, mid-incident, with no hint that the fix is simply a smaller --limit.

**Adjudicator:** Arithmetic verified in the file. MAX_LIMIT = 2098 (retry_qbo_outbox_dead_letters.py:47-48) with the comment '2100; UPDATE uses 2 datetime params + N ids'; the UPDATE at 141-154 binds now, now, *ids, i.e. 2 + 2098 = 2100 USER parameters at --limit 2098 — at the documented server ceiling before any driver overhead, not below it, contrary to the commit message's 'capped below SQL Server's 2100-parameter ceiling'. SQL Server counts the RPC's own parameters toward the 2100 limit, and pyodbc executes parameterized statements as sp_prepexec (@handle/@params/@stmt) or sp_executesql (@stmt/@params), adding 2-3 slots — so 2100 user parameters exceeds the cap on every driver path and raises error 8003. The validator advertises the failing value ('--limit must be between 1 and 2098', line 76), and the SELECT is unaffected (TOP is interpolated at line 101), so the failure lands after the row list has…

### [P3 / CONFIRMED] DB->QBO check prints 'OK — all bills have QBO mappings' in the same run that reports unmapped bills
**Location:** `/Users/chris/Applications/build.one/build.one.api/scripts/reconcile_project.py:1059` · lens `b-replay`

The U-218b rework moved bill-level drift out of check_db_to_qbo (an unmapped bill now hits `continue` at line 305 instead of appending an issue) and into the report-only report_db_to_qbo_bills. The caller's success message at 1057-1059 was not updated: it still keys off `if not qbo_issues`, which is now empty precisely when every bill is unmapped. The unmapped count also lands in stats['db_unmapped_bills'], which is excluded from the dry-run any_action list (1327-1332), so the exit guidance stays silent about it. For a tool whose entire purpose is drift detection, the headline line of the section now asserts the opposite of the finding printed two lines later.

**Failure scenario:** A project whose 40 completed bills all lack a BillBill mapping (e.g. the outbox was never drained) is reconciled: check_db_to_qbo skips all 40 unmapped bills, returns [], and the script prints 'OK — all 40 bills and 0 line items have QBO mappings', followed by 40 '[DB->QBO] ... missing BillBill mapping' lines and a summary reading 'DB->QBO issues: 0'. An operator scanning summaries across 136 projects records the project as clean.

**Adjudicator:** Same defect as finding 2, stated more precisely. Verified: check_db_to_qbo now `continue`s on unmapped bills (reconcile_project.py:304-305), the banner still keys off `if not qbo_issues:` (1057-1059), and the unmapped count lands in stats['db_unmapped_bills'], which is absent from main()'s dry-run any_action list (1327-1332) — though that absence is correct, since the push it once gated was retired and there is no repair to apply. One factual slip in the example: total_lines sums line_items_by_bill_id across all loaded bills, so the banner would read 'all 40 bills and N line items' with N > 0, not '0 line items'. The headline contradiction — an OK banner immediately above 40 '[DB->QBO] ... missing BillBill mapping' lines and a 'DB->QBO issues: 0' summary — is exactly as described. P3 as filed.

### [P3 / CONFIRMED] report_db_to_qbo_bills test has no positive control, and its headline mock assertion cannot fail
**Location:** `tests/test_u218b_bill_push_surface.py:87` · lens `b-vacuity`

`report_db_to_qbo_bills(bills_by_id, mapped_bill_ids)` (scripts/reconcile_project.py:315-332) takes no connector and no realm_id, so `sync_mock.assert_not_called()` on the patched `BillBillConnector.sync_to_qbo_bill` is structurally unfalsifiable — the function has no reference through which it could ever call it. That leaves `assert count == 1` as the test's only content, and it is exercised solely with `mapped_bill_ids=set()`, i.e. only the report branch. The skip branch (`if bill_id in mapped_bill_ids: continue`, reconcile_project.py:323-324) is never executed by any test. This is now the sole reporter of unmapped bills: U-218b changed `check_db_to_qbo` to `continue` past unmapped bills (reconcile_project.py:304-305) and delegate them here, so a regression in the skip has no second detector.

**Failure scenario:** Delete the `if bill_id in mapped_bill_ids: continue` line, or pass a container the membership test cannot match (the caller feeds it `bill_bill_repo.read_all_bill_ids()`, whose element type no test ever exercises). Suite green. Every completed bill on every project prints as "missing BillBill mapping" in the reconcile output and `db_unmapped_bills` totals the whole book — the DB→QBO section becomes all-noise, and the real unmapped bills it exists to surface are indistinguishable.

**Adjudicator:** Confirmed with one correction to the stated mechanism. report_db_to_qbo_bills(bills_by_id, mapped_bill_ids) (reconcile_project.py:315-332) indeed takes no connector and no realm_id, and the test (tests/...:87-96) exercises it only with mapped_bill_ids=set(), so the skip branch `if bill_id in mapped_bill_ids: continue` (323-324) is never executed by any test — and since U-218b made this the SOLE reporter of unmapped bills (check_db_to_qbo now `continue`s at 304-305), a regression there has no second detector. The claim that sync_mock.assert_not_called() is 'structurally unfalsifiable' is overstated, though: the patch targets BillBillConnector.sync_to_qbo_bill at class level, so a future re-added push that constructs a connector INSIDE the function would trip it. It is a weak guard with no positive control, not a dead one. Residual gap is real; P3 as filed.

### [P3 / CONFIRMED] Negative controls for the argparse guards and the pre-claim write gate depend on a live DB call failing, not on an assertion
**Location:** `tests/test_u218b_bill_push_surface.py:364` · lens `b-vacuity`

`test_retry_qbo_dead_letters_rejects_unknown_kind` (:364) and `test_retry_qbo_dead_letters_rejects_limit_above_sql_cap` (:370) call `retry.main()` without patching `retry.get_connection` — unlike every other test in the file, which routes through `_run_retry_main`'s fake connection. They pass today only because the guards return 2 before `with get_connection()` at scripts/retry_qbo_outbox_dead_letters.py:105. The same shape appears in this unit's headline guard test, `test_drain_once_skips_claim_when_writes_disabled` (tests/test_qbo_api_budget.py:413), which does not patch `qbo_app_lock` — so the only thing standing between that test and `sp_getapplock` on the live DB is the guard it is testing. Per CLAUDE.md the harness is deliberately pure-logic / no-live-DB.

**Failure scenario:** Regress the `--kind`/`--limit` validation or the `writes_allowed()` pre-claim guard. Instead of a readable assertion diff, the test reaches `get_connection()`: on a developer machine with .env creds and an allowlisted IP it opens a real connection to prod SQL (and for the drain test, takes the named application lock `qbo_outbox_drain` against the live DB); on CI it hangs for the pyodbc login timeout and then errors. The regression is still surfaced, but as an infrastructure failure that reads like a flaky/offline test rather than a broken guard — and it touches production while doing so.

**Adjudicator:** Verified in all three tests. test_retry_qbo_dead_letters_rejects_unknown_kind (:364-367) and test_retry_qbo_dead_letters_rejects_limit_above_sql_cap (:370-373) patch only sys.argv and call retry.main() directly, unlike every sibling which routes through _run_retry_main's fake connection (:265-286); they pass solely because the guards return 2 before `with get_connection()` (retry_qbo_outbox_dead_letters.py:96 — the finding says 105, immaterial). Same shape in tests/test_qbo_api_budget.py:413-429: test_drain_once_skips_claim_when_writes_disabled patches writes_allowed but NOT qbo_app_lock, while its polarity twin test_drain_once_claims_when_writes_allowed (:433-449) does patch it — so the only thing between that test and sp_getapplock against the live DB is the guard under test. On a dev box with .env creds and an allowlisted IP that means a real prod connection and a named application lo…

### [P3 / CONFIRMED] The 'one sanctioned push path' invariant is guarded by one deleted symbol name inside one function body
**Location:** `tests/test_u218b_bill_push_surface.py:386` · lens `b-vacuity`

`test_sync_qbo_bill_has_no_push_flag_or_push_path` asserts `"sync_local_to_qbo" not in inspect.getsource(bill_script.sync_qbo_bill)`. `sync_local_to_qbo` was only the wrapper name deleted from this script (three sibling scripts — sync_qbo_item.py:190, sync_qbo_vendor.py:108, sync_qbo_customer.py:145 — still define functions with that name, so the string is not even reserved). The actual push API is `BillBillConnector.sync_to_qbo_bill`, which the assertion does not mention. `inspect.getsource` is also scoped to the `sync_qbo_bill` function body only, not the module. The unit's stated invariant is that exactly one sanctioned push path exists; this test enforces something much narrower than that.

**Failure scenario:** A future contributor re-adds a batch push to scripts/sync_qbo_bill.py — either calling `BillBillConnector().sync_to_qbo_bill(...)` directly inside `sync_qbo_bill`, or as a new module-level function invoked from `main()`. Both assertions pass and the suite stays green, restoring the second unsanctioned push path (with a fresh idempotency UUID per run) that U-218b was built to eliminate — the same shape that produced duplicate QBO bills from reconcile_project.py.

**Adjudicator:** Verified. tests/test_u218b_bill_push_surface.py:386-387 asserts `'sync_local_to_qbo' not in inspect.getsource(bill_script.sync_qbo_bill)`. That string is not even reserved — `grep -rn 'def sync_local_to_qbo' scripts/` returns four live definitions (sync_qbo_customer.py:145, sync_qbo_term.py:189, sync_qbo_item.py:190, sync_qbo_vendor.py:108). The real push API is BillBillConnector.sync_to_qbo_bill, which the assertion never mentions, and inspect.getsource is scoped to the sync_qbo_bill function body (scripts/sync_qbo_bill.py:479) rather than the module, so a re-added push — inline via a connector call, or as a new module-level function invoked from main() — passes both this assertion and the '--push' check on parse_args. The test enforces something strictly narrower than the unit's stated invariant. P3 as filed.

### [P3 / REFUTED] The skipped-writes ERROR fires unconditionally and carries no payload, so the unit's only alarm is guaranteed noise
**Location:** `integrations/intuit/qbo/outbox/business/worker.py:145` · lens `b-park`

worker.py:145-150 logs at ERROR on every drain tick whether or not a single row is queued, and its `extra` carries only `event_name` — no queue depth, no oldest-row age, no realm. The commit justifies removing per-row dead-lettering on the grounds that this event "is a better alarm", but base/client.py:53-63 documents ALLOW_QBO_WRITES as default-deny specifically so local dev cannot push, which means every non-prod environment emits this ERROR permanently: the in-process drain runs on a 5s interval (shared/scheduler.py:114-122), roughly 17k ERROR events/day, and the Function App at 30-60s emits 1.4-2.9k/day. An unconditional ERROR in a namespace that also carries genuine per-row failures (`qbo.outbox.row.dead_lettered`) trains operators and alert rules to mute the namespace — and per finding 1 this is the only signal that exists. Gate the log on there actually being claimable work and include the count, or emit at WARNING with a periodic ERROR escalation once depth or age crosses a threshold.

**Failure scenario:** An App Insights alert rule on qbo.outbox ERROR events is created, immediately fires continuously from a dev/staging App Service where the gate is intentionally unset, and is disabled or filtered out as noise. When ALLOW_QBO_WRITES is later lost in production, the identical event fires with no queue-depth context and is suppressed by the same filter, so the silent starvation in finding 1 goes undetected.

**Adjudicator:** The two literal code claims are true — worker.py:145-150 logs at ERROR before any claim attempt (so it is not gated on there being work) and its `extra` carries only event_name — but the finding's thesis and failure scenario are positively excluded by the deployment topology. The 17k events/day figure and the 'every non-prod environment emits this ERROR permanently' premise both depend on shared/scheduler.py's 5s in-process drain running where ALLOW_QBO_WRITES is unset. That job only registers when ENABLE_SCHEDULER == 'true' (shared/scheduler.py:36-38, 41-58), which is default-deny AND explicitly disabled in prod (CLAUDE.md:66 'In-process APScheduler fallback ... currently disabled in prod'; SESSION_NOTES 1928, 2121 'prod-only ... local dev is default-deny'). The only live drain caller is the scheduler Function App POSTing /api/v1/admin/outbox/drain/qbo behind the drain secret, against t…

## U-218e — 17 findings

### [P2 / CONFIRMED] for_uploads_single()'s compensating control does not exist — every transient upload failure is swallowed and never retried at any level
**Location:** `entities/bill/business/service.py:1958` · lens `e-multipart`

`RetryPolicy.for_uploads_single()` (base/retry.py:54-63) drops in-process retry to max_attempts=1 on the explicit premise that "Transient resilience lives one level up: the outbox row retries with a mapping guard, so a lost upload does not silently orphan QBO documents." That level does not retry. `_sync_attachments_to_qbo` re-raises exactly two error classes — `QboBudgetExceededError` (:1946) and `QboWriteRefusedError` (:1954) — and its `except Exception as e` at :1958 swallows every retryable type the seam now produces (`QboServiceUnavailableError`, `QboServerError`, `QboTimeoutError`, `QboTransportError`, `QboMalformedResponseError`), appending them to a local `errors` list. `push_to_qbo` then logs a warning (:1872-1876) and returns `qbo_bill` normally, so the worker calls `mark_done`. Nothing is written to `[qbo].[ReconciliationIssue]`, no dead-letter is created, and there is no attachment-drift reconcile anywhere in `integrations/intuit/qbo/reconciliation/`. The unit itself argues this exact swallow is unacceptable (it is why `QboWriteRefusedError` was added to these clauses — "the bill would report pushed while attachments silently vanished"), yet left it in place for the whole transient family while simultaneously removing the only retry that covered them. Fix is either to re-raise retryable QboErrors here so the row retries under the mapping guard, or to record a ReconciliationIssue per swallowed attachment so the loss is durable rather than a log line.

**Failure scenario:** A completed Bill drains; `sync_to_qbo_bill` succeeds and QBO mints the Bill. QBO then returns 503 on `POST /v3/company/{realm}/upload` for the bill's PDF. `post_multipart` uses for_uploads_single() so there is exactly one attempt; `QboServiceUnavailableError` (is_retryable=True) propagates to `_sync_attachments_to_qbo`, misses both re-raise clauses, and is caught by `except Exception` at :1958. The loop continues, `push_to_qbo` returns, the worker marks the row done. The QBO Bill permanently carries no source document; there is no ReconciliationIssue row, no dead letter, and no retry at any level — only a `logger.error` line. On a path the commit measures at 1,446 uploads / 60 days.

**Adjudicator:** Walked end-to-end. retry.py:59-60 states the compensating control verbatim ("the outbox row retries with a mapping guard"). entities/bill/business/service.py:1946 re-raises only QboBudgetExceededError, :1954 only QboWriteRefusedError; :1958 `except Exception as e` appends to `errors` and the loop continues. push_to_qbo :1872-1876 logs a warning and returns qbo_bill; worker.py:310 then calls repo.mark_done. QboTimeoutError/QboServerError/QboServiceUnavailableError/QboMalformedResponseError are all is_retryable=True (errors.py:105-146) and none of them are re-raised, so no outbox retry ever fires for them. `grep -rn attachable|Attachment integrations/intuit/qbo/reconciliation/` returns ZERO hits, so the docstring's "attachments can be reconciled later" (:1832, :1892) is also false — no ReconciliationIssue, no dead letter, only a log line. Two corrections that lower severity from P1 to P2: …

### [P2 / CONFIRMED] Retry budget (30s) is not larger than the per-request timeout it must survive, so for_writes/for_reads deliver exactly one attempt on any timeout
**Location:** `integrations/intuit/qbo/base/retry.py:42` · lens `e-multipart`

`max_total_budget_seconds` defaults to 30.0 and `for_writes()`/`for_reads()` inherit it, while tier A (the default for `get`/`post`/`put`) has read=write=30.0s (base/client.py:44). Because the budget is measured across all attempts including the first, a single timed-out request consumes the entire budget, so `execute_with_retry`'s guard at :191 (`if sleep_seconds >= remaining_budget: raise`) always fires on attempt 1 — `remaining_budget` is <= 0 and full-jitter `sleep_seconds` is >= 0. `for_writes(max_attempts=3)` and `for_reads(max_attempts=5)` therefore never retry the single most common transient. This directly falsifies the unit's headline behavior change for the newly-routed attachable reads: `get_attachable` / `query_all_attachables` now map timeouts to retryable `QboTimeoutError`, but they still get exactly one attempt, so "transient failures now retry instead of hard-failing" does not hold for timeouts. U-218e demonstrably understood this interaction — it added `TIER_C_REQUEST_CEILING_SECONDS` (:20) with the comment "A single upload attempt can burn a full tier-C timeout before QBO answers" — but applied the corrected budget only to `for_uploads_single()`, where max_attempts=1 makes it unreachable. The unit also newly exposed `timeout_tier` on `post()`/`put()` (base/client.py:217/276), so any future caller selecting tier B or C pairs a 60-120s request with a 30s budget, widening the mismatch to 4x. Fix: derive each policy's budget from its tier ceiling (max_attempts * tier_read + backoff), the way for_uploads_single() already tries to.

**Failure scenario:** A Bill push POSTs to QBO during an Intuit brownout and the connection hangs. httpx raises ReadTimeout at 30.0s; `_send_once` maps it to `QboTimeoutError(is_retryable=True)`. In `execute_with_retry`, attempt=1 < max_attempts=3, so it computes `sleep_seconds = uniform(0, 1.0)`, then `elapsed ≈ 30.0+` and `remaining_budget = 30.0 - 30.0 <= 0`. `sleep_seconds >= remaining_budget` is true, it logs `qbo.retry.budget_exceeded` and raises. The configured 3 attempts silently collapse to 1 for every timeout, on writes and reads alike.

**Adjudicator:** Verified in code and corroborated by the repo's own sibling integration. retry.py:42 max_total_budget_seconds=30.0; for_writes/for_reads (:46-51) inherit it. client.py:44 tier A is read=write=30.0. execute_with_retry measures `elapsed = time.monotonic() - start_time` (:189) across ALL attempts including the first, so a tier-A ReadTimeout makes elapsed >= 30.0, remaining_budget <= 0, and the guard at :191 (`sleep_seconds >= remaining_budget`, with full-jitter sleep always >= 0) fires unconditionally on attempt 1. Decisive corroboration: the unit's own test tests/test_u218e_attachable_shared_client.py:311-315 (`test_for_writes_budget_exceeds_on_same_scenario`) PINS exactly this — one call, retries collapsed — and integrations/box/base/retry.py:57-66 fixed precisely this bug for Box after a 2026-07-08 incident by deriving the budget from max_attempts * TIER_C_REQUEST_CEILING_SECONDS, which …

### [P2 / CONFIRMED] The mapping guard the at-most-once upload design rests on is not durable — a failure between the QBO upload and the mapping write lets an outbox re-drain mint a duplicate Attachable
**Location:** `integrations/intuit/qbo/attachable/connector/attachment/business/service.py:449` · lens `e-multipart`

Dropping `requestid` (`post_multipart(include_requestid=False)`, base/client.py:241/261) removes server-side dedup entirely, so the only thing preventing a duplicate document is the local mapping check at :385 (`read_by_attachment_id`). That guard is written last and is not failure-isolated from the QBO side effect: `upload_attachable` (:414) mints the Attachable in QBO, then `qbo_attachable_repo.create` (:427) and `_create_mapping` (:447) persist locally. `_create_mapping`'s ValueError is swallowed with a bare `logger.warning` at :449-450 and the function still returns success; any non-ValueError repo failure at :427 or :447 propagates into `_sync_attachments_to_qbo`'s `except Exception` (bill service :1958) and is likewise swallowed. Either way the outbox row completes or parks with a QBO Attachable that has no local mapping. Because `push_to_qbo` re-raises `QboBudgetExceededError` / `QboWriteRefusedError` from a *later* attachment in the same loop, whole-row re-drains are a designed-for path, not a hypothetical — and on re-drain an unguarded attachment is re-POSTed byte-identically with no idempotency token. This is precisely the duplicate-document outcome the unit's P1 fix targeted, just relocated one level up. Fix: write the mapping in the same transaction as the QboAttachable row, or make a mapping-write failure fatal to the attachment leg so it cannot be reported as synced.

**Failure scenario:** A bill drains with two attachments. Attachment A uploads successfully (QBO mints Attachable 5001), then `mapping_repo.create` raises a pyodbc error; `_sync_attachments_to_qbo` catches it at bill service :1958 and continues. Attachment B then raises `QboBudgetExceededError`, which re-raises at :1946, so the worker parks the row. On re-drain, `sync_attachment_to_qbo` for A finds `read_by_attachment_id(A) -> None`, re-downloads the same blob and re-POSTs it to /upload with no `requestid`. QBO mints Attachable 5002. The Bill now carries the same PDF twice, and the next pull hash-matches the orphan against the same local Attachment.

**Adjudicator:** Every link walked in connector/attachment/business/service.py: guard read at :385 (read_by_attachment_id), irreversible QBO create at :414 (upload_attachable), local staging row at :427, mapping write at :447 — with its ValueError caught at :449-450 and downgraded to logger.warning while the function returns local_qbo_attachable as success. A non-ValueError failure at :427 or :447 propagates to BillService :1958 and is swallowed there (verified above). Either way QBO holds an Attachable with no AttachableAttachment row. The re-drain path is real, not hypothetical: BillService :1946/:1954 re-raise budget/write-refused from a LATER attachment in the same loop, and worker.py:338-358 / :360-380 mark_failed with a next_retry_at (park, not dead-letter), so the whole push_bill row re-drains and re-enters _sync_attachments_to_qbo. On that re-drain the unmapped attachment fails the :385 check and…

### [P2 / CONFIRMED] At-most-once upload has no compensating retry anywhere — the justification names a control that does not exist
**Location:** `entities/bill/business/service.py:1958` · lens `e-regress`

`RetryPolicy.for_uploads_single` (base/retry.py:59-60) and `post_multipart`'s docstring (base/client.py:251-252) both justify max_attempts=1 with "Transient resilience lives one level up: the outbox row retries with a mapping guard." It does not. `_sync_attachments_to_qbo` re-raises only `QboBudgetExceededError` (:1946) and `QboWriteRefusedError` (:1954); every other error hits `except Exception as e` at :1958, is appended to a list, and `push_to_qbo` merely logs a warning (:1872-1876). The outbox row is then marked DONE. So the exact three classes the at-most-once change stops retrying in-process — QboTimeoutError, QboServiceUnavailableError/QboServerError, and QboMalformedResponseError — are retried by nobody. The at-most-once decision itself is right; the missing half is that nothing replaced the retry it removed, and the code comment asserts otherwise, so a future maintainer will believe uploads are covered. Note the outcome is unchanged from pre-U-218e (the old client's 5xx→QboValidationError was swallowed identically), so this is a gap the re-route inherited and then papered over, not a new one — but it is now load-bearing on a documented decision.

**Failure scenario:** Bill drain pushes a bill with a PDF attachment. Intuit returns 503 (or a 2xx with an empty body) on POST /upload. `for_uploads_single` correctly does not retry; `upload_attachable` raises QboServiceUnavailableError/QboMalformedResponseError; `_sync_attachments_to_qbo:1958` swallows it into `errors`; `push_to_qbo:1873` logs a warning; the outbox row completes successfully and is never re-drained. The attachment is permanently absent from the QBO bill with only a log line — and per docs/audit_qbo_integration_2026_08_07.md:226 there is no reconciliation for missing attachables. In the malformed-2xx variant Intuit DID create the Attachable, so the next realm pull hash-matches it, `_create_mapping` raises "already mapped", and the orphan is unrecoverable forever.

**Adjudicator:** Same defect as finding 1, and this is the best-calibrated statement of it. Verified: retry.py:59-60 and client.py:251-252 both assert outbox-level mapping-guarded retry; BillService :1946/:1954 re-raise only budget and write-refused; :1958 swallows QboTimeoutError / QboServiceUnavailableError / QboServerError / QboMalformedResponseError into a list that only reaches logger.warning at :1873; worker.py:310 mark_done. No reconciliation exists (grep over integrations/intuit/qbo/reconciliation/ finds nothing attachable-related), matching docs/audit_qbo_integration_2026_08_07.md:226 ("no evidenced reconciliation of missing attachables"). This finding's framing is the accurate one and I verified its no-regression claim against the diff: the pre-U-218e client mapped all 5xx to non-retryable QboValidationError, so the outcome is unchanged — the unit inherited the gap and then papered over it with…

### [P2 / CONFIRMED] Upload at-most-once relies on an outbox retry that never happens — one transient QBO error permanently drops the attachment
**Location:** `entities/bill/business/service.py:1958` · lens `e-timeout`

post_multipart deliberately uses RetryPolicy.for_uploads_single() (max_attempts=1, client.py:262) on the stated premise that "Transient resilience lives one level up: the outbox row retries with a mapping guard" (retry.py:59-61, client.py:251-252). That compensating control does not exist. sync_attachment_to_qbo has exactly one production caller — BillService._sync_attachments_to_qbo — and the `except Exception as e:` at line 1958 catches every retryable QboError the shared client now raises (QboTimeoutError, QboServerError, QboServiceUnavailableError, QboMalformedResponseError, QboTransportError, QboRateLimitError), appends a string to a list that is only logged at line 1872-1876, and lets the loop continue. push_to_qbo then returns normally, the worker marks the outbox row done (worker.py:310), and the row never retries. There is also no repair path: grepping integrations/intuit/qbo/reconciliation/business/service.py for 'attachable'/'Attachment' returns nothing, so the docstring's "attachments can be reconciled later" (line 1832, 1892) is also false. Note the policy is blanket: 429 and connect-phase transport errors are NOT post-commit-ambiguous (QBO definitively did not create the Attachable), so retry was provably duplicate-free for those classes and was dropped anyway. Fix is either a mapping-guarded retry inside sync_attachment_to_qbo for the unambiguous classes, or re-raising retryable QboErrors from line 1958 so the outbox row (which already backs off and re-drains) actually gets its turn.

**Failure scenario:** Outbox row kind=push_bill drains for a bill with one PDF attachment. POST /bill succeeds. QBO returns 503 (or the 120s tier-C read times out) on POST /upload. for_uploads_single raises QboServiceUnavailableError on attempt 1. sync_attachment_to_qbo propagates it; line 1958 swallows it into `errors`; line 1872 logs a warning; push_to_qbo returns qbo_bill; worker.mark_done fires. The bill is in QBO with no attachment, no outbox retry, no reconciler, and no dead-letter — a silent permanent document loss on a path carrying ~1,446 rows/60 days.

**Adjudicator:** Third statement of the same defect; every claim in it checks out, including the ones the other two got wrong. Verified: `grep -rn sync_attachment_to_qbo` returns exactly one production caller (entities/bill/business/service.py:1938); :1958 catches every retryable class the shared client now raises; push_to_qbo :1872-1876 logs only; worker.py:310 mark_done. The repair-path claim is verified — grep for attachable/Attachment across integrations/intuit/qbo/reconciliation/ returns nothing, so the docstrings at :1832 and :1892 are false. The sharpest observation here, and it is correct: the blanket max_attempts=1 also covers QboRateLimitError (429) and connect-phase QboTransportError, which are NOT post-commit-ambiguous — QBO definitively did not create the Attachable — so retry was provably duplicate-free for those classes and was dropped anyway. Severity corrected P1 -> P2 for the same reaso…

### [P2 / CONFIRMED] The headline at-most-once fix is not protected by any test — restoring upload retry leaves the suite green
**Location:** `tests/test_u218e_attachable_shared_client.py:292` · lens `e-timeout`

Every test that could detect an upload retry injects the local `deterministic_retry` fixture, whose fake `time.monotonic` returns 1000.0 on its first call and 1120.0 on every call after. execute_with_retry takes start_time from call #1 and computes `elapsed = 1120 - 1000 = 120` in its except block, so `remaining_budget = 30 - 120 = -90` and the `sleep_seconds >= remaining_budget` guard (retry.py:191) aborts before attempt 2 for ANY policy whose max_total_budget_seconds is the 30s default. That means test_upload_retryable_error_sends_exactly_one_post (line 349) and test_upload_does_not_retry_on_503 (line 368) assert calls == 1 for reasons that have nothing to do with max_attempts. test_for_uploads_single_is_at_most_once (line 305) only inspects the classmethod's return value, not what post_multipart passes. So the mutation that actually matters — client.py:262 `policy=RetryPolicy.for_uploads_single()` -> `RetryPolicy.for_writes()`, i.e. exactly the pre-review state that duplicated documents — is invisible to all 1601 tests. The commit's mutation matrix entry "upload retry re-enabled" was scored against a mutation of for_uploads_single() itself, not of the call site. Given the unit is undeployed and cannot be acceptance-tested (no sandbox realm), this is the only guard the P1 has. A test that stubs the transport to succeed on attempt 2 without the fixture (or that asserts the policy identity threaded into _execute) closes it.

**Failure scenario:** Apply a one-token edit at integrations/intuit/qbo/base/client.py:262 changing `RetryPolicy.for_uploads_single()` to `RetryPolicy.for_writes()`. Run the full suite: 1601 still green. The reproduced duplication bug (readtimeout-then-200 sending 2 POST /upload) is back in production with zero test signal.

**Adjudicator:** Traced the fixture arithmetic through execute_with_retry. deterministic_retry (tests/test_u218e_attachable_shared_client.py:291-302) patches retry_module.time.monotonic to return 1000.0 on call #1 and 1120.0 thereafter; execute_with_retry takes start_time from call #1, so in the except block elapsed = 120.0 for every policy. Under the proposed mutation of client.py:262 to RetryPolicy.for_writes(): attempt 1 < max_attempts 3, sleep_seconds = 0.01 (uniform patched), remaining_budget = 30 - 120 = -90, and the :191 guard fires -> raise after exactly one HTTP call. So test_upload_retryable_error_sends_exactly_one_post (:349, all three parametrizations) and test_upload_does_not_retry_on_503 (:368) still assert calls == 1 and still pass. test_for_uploads_single_is_at_most_once (:305) only inspects the classmethod's return value, and I checked the other upload tests (wire shape :92, content-type…

### [P2 / CONFIRMED] The mapping guard that makes a re-drain safe is neither crash-safe nor fail-loud — re-drain duplicates the QBO Attachable
**Location:** `integrations/intuit/qbo/attachable/connector/attachment/business/service.py:449` · lens `e-timeout`

The at-most-once argument for the upload rests on the mapping check at line 385 (`read_by_attachment_id` -> skip if mapped). But the mapping is written two steps AFTER the irreversible QBO create: upload_attachable at 414, then qbo_attachable_repo.create at 427, then _create_mapping at 447 — whose ValueError is caught at 449 and downgraded to `logger.warning`, after which the function returns normally as if it had succeeded. Any failure in that window (DB blip on the repo create, a raced 1:1 constraint hit in _create_mapping, worker process recycle) leaves an Attachable live in QBO with no local mapping, and the caller is told nothing. U-218e is what makes this reachable: it put the upload behind the write gate and made BillService re-raise QboWriteRefusedError (service.py:1954) so the worker parks and RE-DRAINS the whole bill (worker.py:360-380) — a re-drain path that did not exist for the attachment leg before this unit, since the upload was previously ungated. On re-drain the unmapped attachment fails the line-385 check and is uploaded a second time. That is the same duplicate-document outcome the unit's P1 fix exists to prevent, relocated one layer out. _create_mapping's ValueError should at minimum not be swallowed, and the repo create + mapping write should be one transaction (or the mapping written first against the returned QBO id).

**Failure scenario:** Bill with three attachments drains. Attachment #1 uploads; qbo_attachable_repo.create at line 427 raises a transient pyodbc error; BillService line 1958 swallows it and the loop continues. Attachment #2 uploads and maps fine. Attachment #3 trips the monthly budget breaker -> QboBudgetExceededError propagates (service.py:1946) -> worker parks the row until the 1st. On re-drain, attachment #1 has no mapping row, so it is uploaded again: the bill in QBO now carries two identical PDF attachments, and the next pull hash-matches the orphan and raises "already mapped" permanently.

**Adjudicator:** Duplicate of finding 3; independently re-walked and confirmed, with one attribution correction. Ordering at connector/attachment/business/service.py :385 guard -> :414 QBO create -> :427 repo create -> :447 mapping, ValueError swallowed at :449-450 with a normal return. Re-drain machinery confirmed at worker.py:338-358 (budget park to reset_at) and :360-380 (write-refused park), both mark_failed rather than dead-letter, and BillService :1946/:1954 are what deliver those errors from a later attachment in the same loop. Re-upload on re-drain carries no requestid (client.py:241/261) so QBO cannot dedup. Correction to the finding's causal claim: it says U-218e is 'what makes this reachable' because the write gate created a new re-drain path — but the QboBudgetExceededError park predates this unit (U-211, and BillService :1946 is not new in this diff), so the re-drain trigger already existed;…

### [P3 / CONFIRMED] for_uploads_single()'s max_total_budget_seconds is unreachable dead configuration, and its pinning test cannot fail
**Location:** `integrations/intuit/qbo/base/retry.py:62` · lens `e-multipart`

`for_uploads_single()` computes `max_total_budget_seconds = TIER_C_REQUEST_CEILING_SECONDS + 10.0` (140.0), but with `max_attempts=1` `execute_with_retry` always raises at :167 (`if attempt >= policy.max_attempts`) before reaching the budget computation at :189-191. The value is never read on any code path. `tests/test_u218e_attachable_shared_client.py:308` asserts `policy.max_total_budget_seconds >= TIER_C_REQUEST_CEILING_SECONDS`, which pins a field the runtime cannot consult — the assertion passes regardless of whether the budget logic is correct, so it reads as protection that is not there. The same tier-ceiling reasoning is genuinely needed by `for_writes()`/`for_reads()` (finding 2) where it was not applied. Either drop the field from this policy and the test, or move the derivation to a shared helper the multi-attempt policies actually use.

**Failure scenario:** Someone later raises for_uploads_single's max_attempts (e.g. to re-enable retry once requestid is probed on /upload) and trusts the 140s budget plus the green test to bound the operation. The budget was never exercised, so the first real multi-attempt upload behaves in a way no test has ever covered — and the assertion at test line 308 stays green through the change.

**Adjudicator:** Traced execute_with_retry line by line. With max_attempts=1 the loop body runs once; on a retryable QboError control reaches :167 `if attempt >= policy.max_attempts` (1 >= 1) and raises there. The budget computation at :183-191 is below that raise and is never reached on any path. On a non-retryable error :152 raises even earlier. So retry.py:62's `max_total_budget_seconds = TIER_C_REQUEST_CEILING_SECONDS + 10.0` (140.0) is never read at runtime. tests/test_u218e_attachable_shared_client.py:308 asserts `policy.max_total_budget_seconds >= TIER_C_REQUEST_CEILING_SECONDS` — a pure dataclass-field assertion that cannot observe runtime behavior, so it reads as protection that is not there. Contrast integrations/box/base/retry.py:57-66 + tests/test_box_retry_upload_budget.py:54-60, where the same constant IS honored by a max_attempts=3 policy and the test actually exercises attempt 2. P3 corre…

### [P3 / CONFIRMED] budget.py still documents QboAttachableClient as an independent metering choke point, which U-218e eliminated
**Location:** `integrations/intuit/qbo/base/budget.py:200` · lens `e-multipart`

The `QboApiBudget` docstring reads "`record_call` is invoked at the single HTTP choke points (QboHttpClient._send_http and QboAttachableClient) once per real HTTP round-trip". U-218e deleted the four hand-placed `_meter_call` sites and the `api_budget` ctor param from the attachable client; metering now happens only in `QboHttpClient._send_http` (base/client.py:586). The comment names a second choke point that no longer exists and contradicts the unit's own headline ("the last CorePlus caller outside the gated/metered/retried choke point"). Note the round-trip semantics did stay correct through the re-route — `download_attachable` remains deliberately unmetered (it is outside the seam) and the 401-refresh resend correctly meters twice — so this is documentation drift only.

**Failure scenario:** An engineer auditing CorePlus cap consumption after the next overage reads this docstring, searches for the attachable client's metering site to confirm the upload is counted, finds none, and either concludes uploads are unmetered (they are) or re-adds a second `_meter_call`, double-counting every upload against the monthly cap.

**Adjudicator:** budget.py:199-200 reads verbatim: "`record_call` is invoked at the single HTTP choke points (QboHttpClient._send_http and QboAttachableClient) once per real HTTP round-trip". The diff (`git show db25eed -- .../attachable/external/client.py`) deletes _meter_call, the four call sites, and the api_budget ctor param; the current client (attachable/external/client.py:36-48) takes only realm_id/http_client/minor_version and holds no budget. Metering now happens solely at client.py:586 inside _send_http. The docstring names a second choke point that does not exist, inverting the 'there is exactly one' invariant it states. Also verified the finding's own caveat is correct: download_attachable (:118-146) stays unmetered by design and the 401-refresh resend (client.py:474) legitimately meters twice, so round-trip semantics are intact — documentation drift only. P3 correct.

### [P3 / CONFIRMED] Attachable reads went from 1 metered CorePlus call to up to 5, on the documented dominant budget consumer
**Location:** `integrations/intuit/qbo/base/client.py:204` · lens `e-regress`

The deleted `_request`/`query_attachables` had no retry layer at all — one HTTP round-trip, one `_meter_call`, then raise. Routing through the shared client applies `RetryPolicy.for_reads()` (max_attempts=5, base/client.py:204) and `_send_http` meters EVERY attempt (:586), including the 401-refresh resend. QboRateLimitError (errors.py:122), QboServerError (:131), QboServiceUnavailableError (:134) and QboMalformedResponseError (:141) are all is_retryable=True. QBO 429 throttles typically carry no Retry-After, so `compute_backoff_seconds` falls to full jitter — expected total sleep ~7.5s across four retries, comfortably inside the 30s `max_total_budget_seconds` — meaning all 5 attempts run and all 5 are metered. The attachable full-realm list is the system's largest QBO consumer (~19K rows ≈ 20 page GETs per sync run, × 3 pull entities × 4 runs/hour — docs/audit_qbo_integration_2026_08_07.md:398), plus one `get_attachable` per downloaded attachable (connector/attachment/business/service.py:274). The module docstring (attachable/external/client.py:22-24) acknowledges "changes ... metered call count" but nothing bounds it, and the same doc notes July 2026 hit 94% of the 500K/mo CorePlus cap and hard-blocked the org.

**Failure scenario:** Intuit enters a rate-limit or 5xx window during a scheduled pull. Each of the ~20 attachable query pages, and each per-attachable `get_attachable`, now burns 5 metered calls instead of 1 before surfacing the same error the old client surfaced on attempt 1. A 30-minute Intuit degradation across the 12 attachable-touching pull runs/hour multiplies the month's dominant budget term ~5×, driving `QboApiBudget` past its block threshold; `record_call_or_raise` then starts raising QboBudgetExceededError, which the bill/VC/purchase attachable legs swallow as log-only while `_handle_qbo_error` parks every genuine outbox write until the 1st of next month.

**Adjudicator:** Mechanism confirmed; the stated consequence is arithmetically overstated, hence the severity correction. Confirmed: the deleted _request performed exactly one _meter_call then one httpx request with no retry layer (visible in the diff), while get() now uses RetryPolicy.for_reads() (client.py:204, max_attempts=5) and _send_http meters EVERY attempt including the 401 resend (client.py:586). QboRateLimitError/QboServerError/QboServiceUnavailableError/QboMalformedResponseError are all is_retryable=True (errors.py:115-146), and with no Retry-After the full-jitter sleeps sum to at most 1+2+4+8=15s, inside the 30s budget, so all 5 attempts run and all 5 are metered. Call-site volume is real (docs/audit_qbo_integration_2026_08_07.md P2 finding: ~19K attachables ≈ 20 page GETs per run, 3 pull entities, 4 runs/hour). Overstatement: the 5x applies only to FAILING calls. A 30-minute Intuit degradati…

### [P3 / CONFIRMED] U-211's "upload refused when the monthly budget is blocked" guarantee lost its only test
**Location:** `tests/test_qbo_api_budget.py:326` · lens `e-regress`

`test_attachable_upload_blocked_raises_before_upload` used to set `budget.record_call_or_raise.side_effect = _budget_exceeded_error()` and assert `QboBudgetExceededError` plus `upload_client_cls.assert_not_called()` — i.e. it pinned that a blown CorePlus cap refuses the upload before any byte leaves. The rewrite kept the test NAME but changed its subject entirely: it now deletes ALLOW_QBO_WRITES, asserts `QboWriteRefusedError`, and at :326 asserts `budget.record_call_or_raise.assert_not_called()`. No test anywhere now exercises the budget breaker on the upload path. The surviving `test_send_http_blocked_raises_without_sending` (:236) calls `_send_http` directly with a hand-built GET, so it proves the seam meters but not that `upload_attachable`/`post_multipart` actually reaches that seam. A capability that had a dedicated regression guard before the unit has none after it.

**Failure scenario:** A later refactor gives `post_multipart` its own send path (e.g. to stream large files, or to hit a separate upload host the way Box does), bypassing `_send_http` and therefore `record_call_or_raise`. The full suite stays green: the write-gate test only proves refusal happens before transport, and the GET-based `_send_http` test is unaffected. The attachable upload silently becomes an unmetered CorePlus caller again — exactly the U-211 hole this unit was closing — and is discovered only when the monthly cap is blown past the block threshold without the breaker firing.

**Adjudicator:** Verified against the test diff. The pre-unit test_attachable_upload_blocked_raises_before_upload set `budget.record_call_or_raise.side_effect = _budget_exceeded_error()`, expected QboBudgetExceededError, and asserted `upload_client_cls.assert_not_called()`. The rewritten test (tests/test_qbo_api_budget.py, current :311-334) keeps the name but deletes ALLOW_QBO_WRITES, expects QboWriteRefusedError, and at :326 asserts `budget.record_call_or_raise.assert_not_called()` — the opposite subject. I read the whole attachable section of that file: test_attachable_get_blocked_raises_without_sending and test_attachable_query_blocked_raises_without_sending still pin the breaker on the GET/query legs through the real seam, but nothing exercises the breaker on post_multipart, and test_send_http_blocked_raises_without_sending (:236) calls _send_http directly with a hand-built GET so it cannot see an up…

### [P3 / CONFIRMED] Download path now resolves QBO auth twice per attachable and builds a fresh QboAuthService + httpx.Client per call
**Location:** `integrations/intuit/qbo/attachable/connector/attachment/business/service.py:270` · lens `e-regress`

`_download_from_qbo` already resolves auth on the connector's shared service at :261 (`self.auth_service.ensure_valid_token(realm_id)`), then constructs a `QboAttachableClient(realm_id=realm_id)` per attachable at :270. Because `QboAttachableClient.__init__` (attachable/external/client.py:45-48) forwards neither an `auth_service` nor an `http_client` — and exposes no parameter to do so — each construction lazily builds a brand-new `QboAuthService` (base/client.py:125-128) plus a new `httpx.Client` (:131-133), and `_send_once` then calls `ensure_valid_token_classified` (:406), which does its own `read_by_realm_id` DB round-trip with token decryption on every single call (auth/business/service.py:216). The deleted client received an already-resolved `access_token` and did zero auth work, so this is net-new per-attachable DB and object churn on the hottest attachable path.

**Failure scenario:** A full-realm `QboAttachableService.sync_from_qbo` (~19K attachables per docs/audit_qbo_integration_2026_08_07.md:398) now issues ~19K extra `ReadQboAuthByRealmId` round-trips plus ~19K extra QboAuthService/QboAuthRepository/httpx.Client constructions on top of the ~19K it already made — doubling DB load on the SQL Basic tier for the duration of the run. If the access token expires mid-run, every one of those independent service instances independently enters the `qbo_app_lock('qbo_auth_refresh:{realm}')` path (auth/business/service.py:256) with a 15s timeout, serializing the download loop behind lock acquisition instead of reusing one refreshed token.

**Adjudicator:** Double resolution confirmed. connector/attachment/business/service.py:261 calls self.auth_service.ensure_valid_token(realm_id) (used only as a guard), then :270 constructs QboAttachableClient(realm_id=realm_id) per attachable, which (attachable/external/client.py:45-48) forwards neither auth_service nor http_client and exposes no parameter to do so — so QboHttpClient.__init__ lazily builds a fresh QboAuthService (client.py:125-128) and a fresh httpx.Client (:131-133). client.py:406 then calls _resolve_auth -> ensure_valid_token_classified, which at auth/business/service.py:214-216 does its own read_by_realm_id DB round-trip with token decryption on every call and has no cache. The diff confirms the deleted client took a pre-resolved access_token and did zero auth work. Two corrections that do not change the P3 verdict: (1) the httpx.Client-per-call churn is NOT net-new — the old construc…

### [P3 / CONFIRMED] Metering docstring still names QboAttachableClient as a choke point it no longer is
**Location:** `integrations/intuit/qbo/base/budget.py:200` · lens `e-regress`

`QboApiBudget`'s class docstring still reads "`record_call` is invoked at the single HTTP choke points (QboHttpClient._send_http and QboAttachableClient) once per real HTTP round-trip." After this unit the attachable client holds no `api_budget` at all — the ctor param was deleted and every metered call routes through `_send_http` (base/client.py:586). The doc now describes a second choke point that does not exist, which inverts the invariant it is meant to state ("there is exactly one"). The same staleness exists in the reviewed docs: docs/audit_qbo_integration_2026_08_07.md:422 still lists `query_attachables_for_entity` as retained-but-unused, and :427 still records the attachable upload as bypassing ALLOW_QBO_WRITES — both fixed by this unit. Per CLAUDE.md the Docs refresh is a required step of the per-unit pipeline, and no docs file was touched by this commit.

**Failure scenario:** An engineer adding a tenth QBO entity client reads budget.py:200, concludes that per-client hand-placed metering is a sanctioned pattern (it names one), and wires `record_call_or_raise` directly into the new client while ALSO routing through QboHttpClient — double-counting every call against the 500K/mo CorePlus budget and tripping the block threshold at ~50% of real usage. Conversely, an auditor re-checking the July cap incident reads the stale audit lines at :422/:427 and re-books already-fixed findings.

**Adjudicator:** Duplicate of finding 5 on its primary claim, and the primary claim is verified: budget.py:199-200 still names QboAttachableClient as a choke point, while the attachable client holds no api_budget after this unit and all metering is at client.py:586. I additionally verified both secondary doc citations: docs/audit_qbo_integration_2026_08_07.md still carries "query_attachables_for_entity retains the known-broken unsupported AttachableRef WHERE query ... effectively unused now but invites misuse" (the method was DELETED by this commit) and "[P2 / CONFIRMED] Attachment upload to QBO bypasses the ALLOW_QBO_WRITES gate entirely" (fixed by this commit), and `git show db25eed --stat` confirms no docs file was touched. Partial mitigation on that secondary point: a dated audit report is a point-in-time snapshot rather than living documentation, so its staleness is weaker evidence than the budget.p…

### [P3 / CONFIRMED] post_multipart's include_requestid escape hatch would reuse one outbox key across every attachment of a bill
**Location:** `integrations/intuit/qbo/base/client.py:241` · lens `e-timeout`

post_multipart exposes `include_requestid: bool = False` as the documented flip-behind-a-live-probe follow-up, but hard-codes `idempotency_key=None` (line 261) and provides no parameter to supply one. _execute's fallback chain (lines 356-360) therefore resolves to `get_idempotency_key()` — the ContextVar the outbox worker installs once per ROW via `idempotency_key_context(row.request_id)` (worker.py:269), which wraps the entire push_bill handler including the whole attachment loop. Flipping include_requestid=True would consequently stamp the identical `?requestid=<row.request_id>` on the bill's POST /bill and on every attachment upload in that row. If Intuit honours requestid on /upload — the exact thing the live probe would be testing — attachments 2..N are deduplicated as replays of attachment 1 and silently never created, which is a worse failure than the duplication the flag was meant to prevent because it produces no error at all. post_multipart should accept a per-call idempotency_key (e.g. uuid5 over attachment.public_id) rather than inheriting the row-scoped one.

**Failure scenario:** A future unit flips include_requestid=True after a live probe on a single-attachment bill. A three-attachment bill then drains: all three POST /upload calls carry the same requestid; QBO returns the attempt-1 Attachable for calls 2 and 3; upload_attachable parses them as success and _create_mapping raises "QboAttachable X already mapped" — which line 449 swallows as a warning. Two of three documents are missing from the bill in QBO with a clean outbox row.

**Adjudicator:** Verified as a latent trap in the documented follow-up. post_multipart (client.py:234-266) exposes include_requestid but hard-codes idempotency_key=None at :261 with no parameter to supply one; _execute's fallback at :356-360 then resolves `idempotency_key or get_idempotency_key()` through resolve_idempotency_key. worker.py:269 wraps the entire handler dispatch in `with idempotency_key_context(row.request_id)`, and correlation.py:63-79 documents that every POST/PUT in that block reuses the key. So flipping include_requestid=True would stamp the identical requestid on POST /bill and on every attachment upload in the row. If Intuit honours requestid on /upload — exactly what the booked live probe would test — attachments 2..N return attempt 1's Attachable, upload_attachable parses it as success (attachable/external/client.py:207-216), and _create_mapping raises 'already mapped' which :449 s…

### [P3 / CONFIRMED] for_uploads_single's retry budget is unreachable and its ceiling constant is unbound to the tier it mirrors
**Location:** `integrations/intuit/qbo/base/retry.py:62` · lens `e-timeout`

for_uploads_single sets max_total_budget_seconds = TIER_C_REQUEST_CEILING_SECONDS + 10.0 = 140.0, but execute_with_retry only consults max_total_budget_seconds inside the retry branch, after the `attempt >= policy.max_attempts` check at line 167 — with max_attempts=1 that check always fires first, so the budget is never read on any code path. It reads as a live guard sizing the upload's time envelope and is inert. Worse, TIER_C_REQUEST_CEILING_SECONDS (line 20) is a hand-copied mirror of _TIMEOUT_TIERS['C'] in client.py:46 with no test binding the two: the only assertion, test_u218e_attachable_shared_client.py:307, compares the policy against the constant rather than against the tier, so raising tier C to 300s leaves the 130.0 constant and its comment silently wrong and the suite green. Either derive the constant from _TIMEOUT_TIERS['C'] or drop the dead budget override.

**Failure scenario:** A later unit raises _TIMEOUT_TIERS['C'] to 300s for large-PDF headroom. retry.py:20 still declares 130.0 and its comment still claims to mirror tier C; test_for_uploads_single_is_at_most_once still passes because it compares the policy to the stale constant, not to the tier. Any future policy that does honour the budget (e.g. a for_uploads with max_attempts>1) is then sized to less than half of one attempt's timeout and retries never fire.

**Adjudicator:** Both halves verified. Unreachability: execute_with_retry raises at :167 (`attempt >= policy.max_attempts`) before the budget block at :183-191, so with max_attempts=1 the 140.0 budget is never read. Unbound constant: retry.py:17-20 declares TIER_C_REQUEST_CEILING_SECONDS = 130.0 with a comment explicitly claiming to mirror client.py _TIMEOUT_TIERS['C'] (:46, read/write 120 + connect 5), and nothing binds them — the only assertion, tests/test_u218e_attachable_shared_client.py:308, compares the policy against the constant rather than against the tier, so raising tier C would leave the constant and its comment silently wrong with the suite green. Minor citation drift: the finding cites test line 307 for that assertion; 307 is `assert policy.max_attempts == 1` and the compared assertion is 308. The stated remedy is already implemented next door — integrations/box/base/retry.py:57-66 derives …

### [P3 / CONFIRMED] Shared client's constructor timeout is now dead code and hand-duplicates tier A instead of referencing it
**Location:** `integrations/intuit/qbo/base/client.py:132` · lens `e-timeout`

QboHttpClient.__init__ still builds httpx.Client(timeout=httpx.Timeout(connect=5.0, read=30.0, write=30.0, pool=5.0)) — a literal re-statement of _TIMEOUT_TIERS['A'] (line 44). Since U-218e, _send_http is the sole user of self._http_client and it always passes an explicit per-request `timeout=` (line 604), so the constructor value can no longer take effect on any path. It is both dead and a drift hazard, and it diverges from the two integrations this seam was ported from: MsGraphClient does httpx.Client(timeout=_TIMEOUT_TIERS['A']) at integrations/ms/base/client.py:120, and BoxHttpClient follows the same pattern. Reference the tier table (or drop the argument) so tier A has exactly one definition.

**Failure scenario:** A future unit tunes tier A to read=45.0 in _TIMEOUT_TIERS to relieve slow QBO query endpoints. The constructor literal stays at 30.0, so the client's declared default and its actual per-call behaviour disagree; anyone debugging a timeout by reading __init__ (the obvious place) sees 30s while every request on the wire uses 45s, and any future code path that omits the per-call timeout silently reverts to the stale 30s.

**Adjudicator:** Confirmed, and confirmed as newly-dead in THIS unit. client.py:131-133 still builds httpx.Client(timeout=httpx.Timeout(connect=5.0, read=30.0, write=30.0, pool=5.0)) — a literal restatement of _TIMEOUT_TIERS['A'] (:44). `grep -n self._http_client` shows exactly two uses: close() (:144) and the request at :597, which always receives an explicit `timeout=timeout`; that timeout is resolved unconditionally in _execute (:351-353, which raises on an unknown tier) and threaded through both _send_http call sites (:430, :474), so no path can fall back to the constructor value and httpx's per-request timeout overrides the client default. The diff confirms _TIMEOUT_TIERS and the per-call timeout parameter are new in U-218e (the old _send_http signature had no timeout), so the constructor literal became dead here rather than being pre-existing dead code. Divergence from the sibling is real: integrat…

### [P2 / UNADJUDICATED] minorversion=65 reaches GET /query for the first time; a shape change there yields a totally silent "0 attachments" across every pull
**Location:** `integrations/intuit/qbo/attachable/external/client.py:87` · lens `e-regress`

The commit's "latent fix riding along" is a live wire change on the highest-volume pull path. The deleted code built `?minorversion=65` into the URL string AND passed `params={"query": ...}`; httpx replaces the query string when params= is given, so every `SELECT * FROM Attachable` in prod history ran at Intuit's BASE minor version. `_execute` now merges it in (base/client.py:361-362), so after deploy those queries run at MV 65 for the first time. The hazard is not the version — it is that the parse is total: `query_attachables` does `data.get("QueryResponse", {})` then `.get("Attachable", [])` (:93-95), so ANY unexpected envelope yields `[]`; `query_all_attachables` breaks immediately on `if not attachables` (:108); `_query_attachables_with_fallback` returns `[]` and only logs when `filtered` is non-empty (attachable/business/service.py:109-111); and all three pull loops guard on `if bill_attachables:` with no else. There is no error, no warning, no ReconciliationIssue, and the watermark advances. The unit is UNDEPLOYED with no sandbox realm, so this specific change cannot be acceptance-tested, and the existing tests all feed the handler a hand-built `{"QueryResponse": ...}` so they cannot detect it.

**Failure scenario:** Deploy to prod. Intuit's MV-65 response for `SELECT * FROM Attachable` differs from the base-version envelope the parser assumes (different wrapper key, or an error surfaced as a 200 with a Fault body). `data.get("QueryResponse", {})` returns {} → `query_attachables` returns [] → `query_all_attachables` breaks on the first page and returns [] → every `sync_attachables_for_{bill,vendor_credit,purchase}` reports zero attachables → sync_qbo_bill.py:352, sync_qbo_purchase.py:210 and sync_qbo_vendorcredit.py:294 each take the falsy branch with no log line and the watermark advances. Bills, expenses and credits silently project doc-less for as long as it takes someone to notice missing PDFs in an invoice packet — which is precisely the "false no-attachments" failure the service docstring (attachable/business/service.py:85-89) says the full-list filter was built to eliminate.

**Adjudicator:** I confirmed both code-side halves but cannot establish the trigger, so per the rules this is not CONFIRMED and is certainly not REFUTED. CONFIRMED in code: (a) the deleted client built ?minorversion=65 into query_url AND passed params={'query': ...} (visible in the diff), and httpx replaces the URL query when params= is supplied, so every historical SELECT * FROM Attachable ran at Intuit's base minor version; the shared client now merges minorversion into effective_params at client.py:361-362, so MV65 reaches /query for the first time on deploy. (b) the failure is totally silent: query_attachables :93-95 returns [] for ANY unexpected envelope, query_all_attachables :108 breaks on the first empty page, _query_attachables_with_fallback (attachable/business/service.py:109-111) logs only when `filtered` is non-empty, and the pull loops guard with a bare `if bill_attachables:` (sync_qbo_bill.…
