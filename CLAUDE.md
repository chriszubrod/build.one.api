At the start of each session, read SESSION_NOTES.md for historical context.

## Working Style

- **Plan before coding.** Propose a step-by-step plan and wait for approval before writing any code. Do not start implementing until the plan is confirmed.

## Architecture Decisions (April 2026)

- **Multi-repo structure.** The project is split into three repos under `/Users/chris/Applications/build.one/`: `build.one.api` (this repo), `build.one.web` (React frontend), `build.one.ios` (iOS app). Each is an independent GitHub repo.
- **API response envelope.** All API endpoints return standardized envelopes: `{"data": [...], "count": N}` for lists, `{"data": {...}}` for single entities, `{"status": "accepted", ...}` for 202. Use helpers from `shared/api/responses.py`.
- **Lookup endpoint.** `GET /api/v1/lookups?include=vendors,projects,...` returns slim dropdown data. Used by React frontend. See `shared/api/lookups.py`.
- **Jinja is gone.** As of 2026-04-24 (Wave E5) all Jinja web controllers, the `templates/` tree, `static/` directory, `get_current_user_web`, `require_module_web`, `WebAuthenticationRequired`, `RefreshRequired`, and the `Jinja2` dependency have been removed. React (`build.one.web`) is the only UI surface. Do not reintroduce Jinja templates or `require_module_web` — use `require_module_api` on every router.
- **React frontend.** `build.one.web` uses React + Vite + TypeScript. Dev server talks directly to the prod API via `VITE_API_BASE_URL`. All UI ships through React — the Jinja migration completed 2026-04-24.
- **Old AI layer was removed; new `intelligence/` layer replaces it.** The legacy AI surface is gone (`shared/ai/`, `integrations/azure/ai/`, `entities/qa`, `entities/search`, `entities/anomaly`, `entities/categorization`, `ClaudeExtractionService`, the project-resolution agent, and the attachment `ExtractionService`). Azure OpenAI, Document Intelligence, AI Search, and embeddings are all gone. A new `intelligence/` package has been built from scratch (see section below). **Do not reintroduce LangChain/LangGraph** — the new layer uses HTTPX directly against provider APIs.
- **Email-agent pipeline (2026-04-26; reshaped 2026-05-06 — three-signal classification + non-blocking draft create).** Polled invoice inbox → email_specialist agent (3 signals: email metadata + sender history + DI) → bill_specialist delegation → draft Bill in ~30–60 s end-to-end. The Function App's `poll_email_inbox` (5m cadence) calls `POST /api/v1/admin/email/poll` which lists messages tagged `Blue category` in the shared `invoice_inbox_email`, persists EmailMessage + EmailAttachment rows, and uploads attachments to blob. Separately `process_email_inbox` (1m cadence, inner-drain) calls `POST /api/v1/admin/email/process_one` which claims the oldest pending email and kicks off an email_specialist agent run.
  - **DI: prebuilt-layout with keyValuePairs (NOT prebuilt-invoice).** Generic-shape extractor returns `{content, key_value_pairs, tables, pages_count, raw}` for any document type — invoice / credit memo / statement / receipt / non-financial. `integrations/azure/document_intelligence/business/service.py::_hoist_and_validate` is a thin pass-through; doc-type classification + field extraction are the agent's job. Idempotent — re-running on an already-extracted attachment short-circuits to the cached result (saves ~6 s + the DI charge).
  - **email_specialist tools** (6 total): `read_email_message`, `search_email_sender_history(from_email, exclude_public_id?)` ← prior-context lookup, `extract_email_attachment` ← DI on demand, `record_extracted_fields(attachment_id, vendor_name?, invoice_number?, …)` ← persist agent's typed-field interpretation onto `EmailAttachment.Di*` columns, `bridge_email_attachment` ← EmailAttachment → Attachment via hash-dedup'd `EmailAttachmentBridgeService` (no blob copy; bridge layer normalizes `application/octet-stream` + `.pdf` filename → `application/pdf` so downstream PDF-only validators don't reject), `mark_email_outcome(public_id, outcome, classification, decided_action, classification_reason, confidence)` ← stamps the agent's *semantic* decision + workflow status + Outlook category. Plus the delegation tool `delegate_to_bill_specialist`.
  - **Controlled vocabularies** persisted on `EmailMessage` so future emails from the same sender are informed by past decisions:
    - `AgentClassification` ∈ {`vendor_invoice` | `vendor_credit_memo` | `vendor_statement` | `vendor_expense_receipt` | `customer_payment` | `customer_question` | `customer_dispute` | `internal_reply` | `internal_forward` | `vendor_newsletter` | `non_actionable` | `unknown`}
    - `AgentDecidedAction` ∈ {`delegated_to_bill_specialist` | `delegated_to_bill_credit_specialist` | `delegated_to_expense_specialist` | `flagged_needs_review` | `marked_irrelevant` | `marked_processed`}
    - `AgentClassificationConfidence` DECIMAL(5,4) — agent's overall confidence; ≥0.95 routes per classification, else `needs_review` regardless of value
    - `AgentClassificationReason` NVARCHAR(1024) — one-sentence why
  - `EmailMessage.AgentSessionId` is now properly linked back at `mark_email_outcome` time (the tool forwards `ctx.session_id` to the API — was always NULL before 2026-05-06).
  - **`ReadEmailSenderHistory` sproc** powers the sender-keyed prior-context tool. Returns aggregate counts by ProcessingStatus / AgentClassification / AgentDecidedAction + counts of committed Bills/Expenses/BillCredits sourced from prior emails by this sender + distinct Vendor rows transitively associated via committed Bills. Eliminates the cold-start problem for first-of-its-kind senders.
  - **Outcome categories** applied back to Outlook via `mail_client.set_categories` (append, don't replace — preserves human's input `Blue category`). Multi-attachment outcome rollup precedence: `awaiting_approval > needs_review > processed > irrelevant`.
  - The legacy `entities/inbox`, `entities/email_thread`, `entities/classification_override`, `entities/review_entry`, the old `email_intake` workflow, and the old `templates/inbox/` + `templates/agent/` Jinja trees are still gone — the new pipeline is `entities/email_message/` (schema + repos + tools) and `intelligence/agents/email_specialist/` (agent + prompt). DI lives in `integrations/azure/document_intelligence/`. `integrations/ms/mail` is actively used.
- **bill_specialist non-blocking-draft pattern (2026-05-06).** `create_bill` and `add_bill_line_items` are NO LONGER approval-gated. Drafts have no external side effects (no QBO, no SharePoint, no Excel — only `complete_bill` triggers those, and `complete_bill` IS still approval-gated). This unblocks the email-driven flow which previously hung 5+ min on every approval timeout.
  - **`Vendor.IntakeNotes` (NVARCHAR(MAX))** holds free-text per-vendor agent guidance (e.g. "trim trailing /N suffix from invoice numbers"). Surfaced in `FindVendorForInvoice` results; bill_specialist applies the rules verbatim. UI to edit this field is a future task — populate via SQL today.
  - **`FindVendorForInvoice` sproc** + tool — multi-strategy ranked Vendor lookup (`domain_contact`@1.00 → `exact_name`@0.95 → `exact_abbreviation`@0.90 → `prefix_name`@0.85 → `substring_two_words`@0.75 → `substring_first_word`@0.65). Returns up to 5 ranked candidates with `intake_notes` per row. **Use this — not `search_vendors` — for invoice-driven vendor binding.**
  - **`FindProjectForInvoice` sproc** + tool (on project_specialist) — ranked Project lookup matching Ship To addresses against `Project.Name` (which encodes the address — e.g. `"TB3 - 917 Tyne Blvd"`). bill_specialist reaches it via `delegate_to_project_specialist`.
  - **Inline summary-line fields on `create_bill` API** — `line_description` / `line_quantity` / `line_rate` / `line_amount` / `line_markup` / `line_price` / `line_is_billable` / `line_sub_cost_code_id` / `line_project_public_id`. Server populates the auto-created `BillLineItem` (the one carrying the attachment via `BillLineItemAttachment`) with these values directly. For invoice flows: `qty=1`, `rate=total`, `amount=qty*rate`, `markup=null`, `price=amount`, `is_billable=true`, `description` is a 6-word category summary (e.g. `"Lumber, subfloor, hangers, anchors, delivery"`) folding all DI line items. The full PDF is the per-line detail; reviewers read it for breakdowns.
  - **`Bill.Memo` template (invoice flows)**: minimal — only fields not captured in typed columns. Format: `"DOC#:{raw_invoice_number} | Ref:{po_or_job_or_reference}"`. Skip either field when not applicable. Vendor / BillNumber / Total / Project / IntakeSource live in their typed columns. Match-strategy / confidence audit data lives in the `AgentSession` transcript, not the memo.
  - **Duplicate-Bill source-email backfill** (`LinkBillSourceEmailMessage` sproc + `BillRepository.link_source_email_message`). When `BillService.create()` hits the (vendor, bill_number) uniqueness conflict and the caller passed a `source_email_message_public_id`, the existing Bill's `SourceEmailMessageId` gets stamped (only when currently NULL — won't overwrite a different link). Preserves the email-trail audit when the same Bill came in via two paths (e.g., bill_folder pipeline + email pipeline).
- **`Bill.attachment_public_id` is now a universal create requirement.** Every `POST /api/v1/create/bill` (manual, agent, script) must pass an `attachment_public_id` referencing an existing Attachment row carrying the source PDF. Server creates a BillLineItem and links the attachment to it via `BillLineItemAttachment`. When `line_*` summary fields are also passed (per the inline-summary pattern above), the auto-created line is the summary line; otherwise it's an empty placeholder for the human to fill in via the Edit page. Manual UI uploads via `/upload/attachment` first to obtain the public_id; email_specialist obtains it via `bridge_email_attachment`. **Other entities (Expense, BillCredit) have NOT adopted this rule yet** — only Bill.
- **`SourceEmailMessageId BIGINT NULL FK` on Bill / Expense / BillCredit.** Many-to-one back to EmailMessage; only `Bill.CreateBill` sproc currently writes it. Thread navigation via `EmailMessage.ConversationId` JOIN — no many-to-many table.
- **`core/notifications/` is gone, but `entities/device_token/` is back (2026-04-28, v0.1.0 iOS beta).** SLA scheduler stays removed. Push *registration* is reintroduced as a thin entity package: two endpoints under `/api/v1/mobile/device-token/{register,deactivate}` (auth-only, no module RBAC) backed by `dbo.DeviceToken`. **No sender yet** — tokens accumulate but nothing actually pushes through APNs. The sender lands when v0.1.x picks a trigger surface (review submitted, etc.). Do not reference `core.notifications` or `push_service`.
- **Bill folder processing — queue + tick + scheduled enumerator (2026-04-26).** The legacy `bill_agent` folder scanner is gone, but the React Bills "Process Folder" button is alive. POST inserts one `BillFolderRunItem` per PDF (`status='queued'`); the scheduler's `process_bill_folder` timer drains one item per tick (~30s cadence, inner-loop until empty or 4 min); the scheduler's `enumerate_bill_folder` timer (every 5 min) calls `/api/v1/admin/bill-folder/enumerate` so files dropped into `/source` get auto-picked-up without anyone clicking the button. Hour-window dedup on item_ids prevents stuck files from churning a fresh queue row every tick. Expense folder processing is still un-rebuilt.
- **`ProcessEngine` is CRUD-only.** Only the instant-workflow path (`execute_synchronous` / `_handle_instant_workflow`) is wired up. The `executor` property still raises `RuntimeError`; the email-intake / expense-intake / approval / timeout handlers are gone. `route()` will return `"Unknown workflow type"` for anything that isn't an instant workflow.

## Intelligence Layer (April 2026)

- **`intelligence/` package** houses all AI disciplines — agents today, room for RAG, extraction, classification, etc. Built from scratch to replace the removed AI layer. No LangChain/LangGraph; HTTPX directly against provider APIs.
- **Agent fleet (2026-04-26).** Eleven agents live in prod:
  - **scout** — pure orchestrator. Nine delegation tools: `delegate_to_{sub_cost_code, cost_code, customer, project, vendor, bill, bill_credit, expense, invoice}`. No direct entity tools, no module permissions (role `Agent Orchestrator`, zero grants — makes no direct HTTP calls).
  - **sub_cost_code_specialist** — SubCostCode CRUD + CostCode read.
  - **cost_code_specialist** — CostCode CRUD + SubCostCode read.
  - **customer_specialist** — Customer CRUD + Project read (for "what projects does X have?" child queries).
  - **project_specialist** — Project CRUD + Customer read (for parent resolution).
  - **vendor_specialist** — Vendor CRUD; **search-only reads** (`list_vendors` deliberately not exposed; ~1,100 rows would dominate context). Soft-deletes via `SoftDeleteVendorByPublicId` sproc.
  - **bill_specialist** — Bill (vendor invoice) parent CRUD + line-item CRUD + `complete_bill` workflow (locks IsDraft=false then enqueues SharePoint + Excel + QBO push via the outbox). Search-first (~18K rows). **`create_bill` and `add_bill_line_items` are NOT approval-gated** (drafts only; no external side effects). `complete_bill`, `update_bill`, `delete_bill`, `update_bill_line_item`, `remove_bill_line_item` ARE gated. For invoice-driven creation: use `find_vendor_for_invoice` (not `search_vendors`) + `delegate_to_project_specialist` + `create_bill` with inline `line_*` summary fields → single-call draft + populated summary line + attachment linked.
  - **bill_credit_specialist** — BillCredit (vendor credit memo) parent CRUD + `complete_bill_credit` workflow. Search-first (~400 rows). Same parent (Vendor), different fields (`credit_date` / `credit_number`, no due_date / no payment_term).
  - **expense_specialist** — Expense (vendor expense or refund) parent CRUD + `complete_expense` workflow. **`Expense.IsCredit=true` doubles as the "ExpenseRefund" concept** — no separate table; same specialist handles both via a single tool set. Search-first (~10K rows).
  - **invoice_specialist** — Invoice (OUR customer invoice, billed against a Project — NOT a vendor invoice) parent CRUD + `complete_invoice` workflow (PDF packet + SharePoint upload + QBO push + source-line billed-status sync). Search-first (~900 rows). Project read tools for parent resolution.
  - **email_specialist** — system-triggered orchestrator for the polled invoice inbox. **Not invoked by Scout**; the scheduler-driven `/admin/email/process_one` kicks off one run per pending EmailMessage. Reshaped 2026-05-06 to a three-signal model (email + sender history + DI). Tools (6 + 1 delegation): `read_email_message`, `search_email_sender_history`, `extract_email_attachment`, `record_extracted_fields`, `bridge_email_attachment`, `mark_email_outcome`, plus `delegate_to_bill_specialist`. Carries grants ONLY on Email Messages module (read+update). v1 routes to bill_specialist only — credit memos / refunds / receipts / statements get controlled-vocabulary classifications + `flagged_needs_review`.
  - Each specialist has its own narrow `Role` row with grants only on the entities it owns. Scout's role has zero grants — it makes no direct HTTP calls.
  - Scout's prompt routes by **literal word choice first** ("cost code" → CostCode, "sub-cost-code" → SubCostCode, "customer/client" → Customer, "project" → Project, "vendor/supplier" → Vendor, "bill"/"vendor invoice" → Bill, "bill credit"/"credit memo" → BillCredit, "expense"/"refund"/"receipt" → Expense, plain "invoice"/"customer invoice" → Invoice). Then by format/context when ambiguous.
  - **Search-vs-list discipline:** when a list response would exceed ~10K tokens / 500-1000 rows, drop list and force search. Today: Vendor + all four transactional specialists are search-only.
- **Adding another specialist** is mechanical: new `intelligence/agents/{name}_specialist/` package (definition + prompt + `__init__`), new credentials_key + DB user/role/grants (template: `seed.intelligence_agents.sql`), register `delegate_to_{name}` in scout's `__init__.py` + append to `scout.tools` tuple. Per-entity tools still land in `entities/{name}/intelligence/tools.py`.
- **Delegation primitive.** `intelligence/composition/delegation.py::make_delegation_tool(name, target_agent, description)` returns a Tool whose handler spawns a sub-session via `background.start_run()` with `ParentSessionId` set, subscribes to the sub's channel, forwards live events onto the parent's channel for the UI, and returns the sub's final text as the tool result. Sub-agent approvals carry their own `session_public_id` in the event so the UI POSTs decisions to the sub-session's `/approve` URL.
- **Agent-as-user auth.** Each agent is a first-class `User` + `Auth` row. At run start the orchestrator logs the agent in via `POST /api/v1/mobile/auth/login` and obtains a JWT. Every HTTP tool call routes through `shared.rbac.require_module_api()` exactly like a human request — free RBAC audit trail. Tools never call services directly; `ToolContext.call_api()` is the one and only path.
- **Least-privilege role per agent.** Scout makes no HTTP calls, so its role has zero module grants. Each specialist's role has grants only on the entities it owns. Do not reuse Admin.
- **Tools are colocated with entities.** `entities/{name}/intelligence/tools.py` alongside the entity's `api/` / `business/` / `persistence/` / `sql/`. Self-register on import.
- **Approval UX is generic.** Any tool with `requires_approval=True` pauses the loop, emits an `ApprovalRequest` event (carrying `session_public_id`), awaits `POST /api/v1/agents/runs/{id}/approve` with `{decision: approve|reject|edit, edited_input?}`, then resumes. Each approval-gated tool supplies an `approval_summary` callable to label the card; the UI renders a form built from the tool's JSON Schema.
- **Parallel tool dispatch.** When the LLM emits multiple `tool_use` blocks in one turn, the runner runs them concurrently via `asyncio.create_task`. Events (ApprovalRequest, ApprovalDecision, ToolCallEnd) interleave live on a shared queue; `result_blocks` land in original `pending_calls` order so the LLM's tool_result history is preserved. Forwarded sub-agent events from concurrent delegations will visually interleave on scout's stream — UI grouping into per-source-session lanes is a separate piece of work.
- **Cross-worker safety.** Prod runs `-w 2` gunicorn; both the approval coordinator and the SSE channel registry are process-local dicts.
  - Approval coordinator: in-memory future races a DB poller on `AgentApprovalRequest.Status`; whichever signals first wins (same worker ~ms, cross-worker ~1.5s).
  - SSE `/events` falls back to `intelligence.api.replay::tail_session()` (DB-polled synthesis) when the local channel registry misses. Emits terminal `Done` only when session status is terminal; 10-min safety cap on stuck sessions.
  - `background.start_run` pre-registers the channel before returning `public_id` (via `session_channel.get_or_register`) so delegation handlers don't race the lazy channel-creation path.
  - SSE stream emits `: keepalive` comments every 15s to survive Azure proxy idle timeout during long approval waits.
- **Transport retry.** `intelligence/transport/anthropic.py` retries 429/503/529 with exponential backoff + jitter (1s/2s/4s, ±25%, Retry-After honored, 3 attempts total). 529 "Overloaded" is Anthropic's capacity signal; Sonnet hits these intermittently during high-load windows.
- **Agent credentials pattern.** Each `Agent` declares a `credentials_key` (one of `scout_agent`, `sub_cost_code_agent`, `cost_code_agent`, `customer_agent`, `project_agent`, `vendor_agent` today); the auth helper reads `{key}_username` / `{key}_password` off `config.Settings`. Prod Azure App Service Application Settings must carry all six pairs plus `ANTHROPIC_API_KEY` and `INTERNAL_API_BASE_URL`.
- **Conversation threading.** `AgentSession.PreviousSessionId` links continuation sessions. `POST /api/v1/agents/runs/{public_id}/continue` with a follow-up `user_message` creates a new session pointing at the prior head; `intelligence/persistence/history.py::load_chain_history()` walks the chain and synthesizes canonical Messages as prior context.
- **HTTP surface** (`intelligence/api/router.py`): `POST /api/v1/agents/{name}/runs`, `GET /api/v1/agents/runs/{public_id}/events` (SSE; live from in-memory channel or DB tail), `POST /api/v1/agents/runs/{public_id}/cancel`, `POST /api/v1/agents/runs/{public_id}/continue`, `POST /api/v1/agents/runs/{public_id}/approve`. All gated on `Modules.DASHBOARD`. Cancel / continue / approve require caller to match the session's `RequestingUserId`.
- **React integration.** `build.one.web/src/agents/` holds the hook + SSE client; `ScoutTray` renders as a right-side push drawer toggled by a header button. Conversation threading (user bubbles + agent blocks + "New" reset) + approval cards + record-block rendering are wired. `ApprovalCard.tsx` auto-builds a form from the tool's JSON Schema.
- **Schema files.** `intelligence/persistence/sql/dbo.agent_session.sql`, `dbo.agent_turn.sql`, `dbo.agent_tool_call.sql`, `dbo.agent_approval_request.sql`. All idempotent (`IF NOT EXISTS` guards + `ALTER TABLE ADD` for additive changes). View + MERGE pattern. Seed scripts: `seed.intelligence_agents.sql` (user/auth/role/grants for all three agents), `seed.scout_role.sql` (least-privilege cutover).
- **Deploy path is Docker, not ZIP.** Prod image: `buildone-esgaducjg4d3eucf.azurecr.io/buildone:latest`. Kudu deployment history shows stale Feb 2026 entries — ignore it. Build + deploy: `az acr build --registry buildone --image buildone:latest --image buildone:<shortsha> --file Dockerfile .` then `az webapp restart --name buildone --resource-group buildone_group`. ACR handles the amd64 build server-side (no Docker daemon needed on ARM Mac). ~90s end-to-end.
- **Test scripts.** `scripts/approval_smoke.py` (exercises the approval primitive), `scripts/scout_dry_run.py` (full scout end-to-end), `scripts/scout_sse_dry_run.py` (SSE surface via real HTTP), `scripts/inspect_agent_session.py` (read a persisted session by public_id).

## Project Conventions

- **Entity pattern**: `entities/{name}/` with `api/`, `business/`, `persistence/`, `sql/` sub-packages. `web/` subpackages are gone as of Wave E5 — React owns the UI.
- **SQL**: All DB access via stored procedures (pyodbc). Migrations run with `python scripts/run_sql.py path/to/file.sql`
- **Concurrency**: SQL Server ROWVERSION columns with base64 encoding for transport
- **UI**: all UI is React (`build.one.web`). No server-rendered templates, no `templates/` directory. API routes return JSON only.
- **Workflow engine**: ProcessEngine for main entity CRUD; lightweight child entities use direct CRUD
- **Lazy imports**: Some services (e.g., BillService) use lazy imports in `__init__` to avoid circular deps — always use `self.*` instance attributes, not bare class names
- **Stored procedure NULL handling**: UPDATE sprocs unconditionally SET all columns. Use CASE WHEN guards for fields that should preserve existing values when NULL is passed
- **Auto-save**: Bill and Expense edit pages debounce saves at 300ms. Any action that depends on persisted state (Complete, Delete) must flush (`await autoSave()`) or guard (`isSaving = true`) against pending auto-saves before sending the request
- **RBAC chain**: User → UserRole → Role → RoleModule → Module. Role entity is the core — UserRole and RoleModule are join tables. Role assignment is managed inline on the User Profile page. Authorization middleware: `shared/rbac.require_module_api(Modules.X, "can_read"|"can_create"|…)` FastAPI dependency on every entity router; permission map is cached per `(user_sub, company_id)` for 5 minutes.
- **Access Control Rebuild — all 5 phases shipped 2026-04-30 → 2026-05-06.** The rebuild replaced single-tenant role-name "admin" gating with a multi-tenant model. JWT carries `uid` (User.PublicId), `cid` (active Company.PublicId), `isa` (IsSystemAdmin) claims. Auth dependency populates `current_user_id` / `current_company_id` / `current_is_system_admin` ContextVars per request. `User.IsSystemAdmin = 1` is the bypass primitive (replaces role-name "admin"); Christopher + Apple Reviewer have it. Permissions are the OR-union across every UserRole row for the (user, active Company) pair, with UserModule additive read-only on top. Cache key is `(user_sub, company_id)` — switching Companies via `POST /api/v1/auth/switch-company` invalidates and re-resolves. `JWT_CID_GRACE_DAYS=0` (default since Phase 2) — tokens missing `uid`/`cid` claims fail closed, no DB fallback. Phase 3 enforces TimeEntry row scoping by UserId. Phase 4 added `Workflow.CreatedByUserId` FK + `IsAgent` filter on user lists (default-hidden, opt in via `?include_agents=true`). Phase 5-thin added `CompanyId NOT NULL` + `DEFAULT (1)` to 24 tenant-data tables (zero code change — DEFAULT fires on any INSERT that omits the column). Phase 5b (multi-tenant filtering enforcement) deferred until a 2nd Company arrives. See `project_access_control_rebuild.md` for the full breakdown.
- **UserModule additive grant**: `UserModule` rows are merged on top of `RoleModule` as **read-only** (`can_read=true`, others false). Implemented in `shared/rbac.py::_resolve_permissions_from_db` (uses `types.SimpleNamespace` to mimic `RoleModule`'s attribute shape, scoped by `read_all_by_user_id_and_company_id`) and mirrored in `entities/auth/api/router.py::_resolve_me_payload`. Never downgrades a role-granted module. `entities/user_module/api/router.py` calls `invalidate_all_caches()` + `publish_profile_changed(user_id)` on every mutation so grants take effect within ~1s, not after the 5-min TTL.
- **UserProject informational only (2026-04-29)**: `/auth/me` exposes `accessible_project_ids: number[]` derived from `UserProject` rows. **No** per-endpoint filtering by project anywhere. Field is published to the React client and ready for UI use; per-endpoint enforcement is an unscoped future project. `entities/user_project/api/router.py` calls `publish_profile_changed(user_id)` on every mutation.
- **User Profile page (2026-04-29)**: `/user/:id` is a single React page (`build.one.web/src/pages/users/UserProfile.tsx`) collapsing the old view+edit split. Sections: Profile basics, Credentials (admin), Contacts, Organizations (`UserOrganization`), Companies (`UserCompany` — dropdown filtered to companies linked to the user's Orgs via `OrganizationCompany`), Roles, Modules, Projects. Admin-only edit affordances throughout (`is_admin` from `useCurrentUser()`).
- **User-relationship join entities (2026-04-29)**: `entities/user_organization/`, `entities/user_company/`, `entities/organization_company/` — all gated `Modules.USERS` (or `Modules.ORGANIZATIONS` for the latter), all routed through `ProcessEngine.execute_synchronous` for audit, registered in `app.py` + `SYNCHRONOUS_TASKS` + `SERVICE_REGISTRY`.
- **Admin credentials endpoints (2026-04-29)**: `GET /api/v1/admin/auth/by-user/{user_public_id}` (Modules.USERS, can_read) returns `{username, has_auth}` (never the hash); `POST /api/v1/admin/auth/set-credentials/{user_public_id}` (Modules.USERS, can_update) creates-or-updates the Auth row and links to the User in one call. Backed by `dbo.ReadAuthByUserId` sproc + `AuthService.set_credentials_for_user`. **Does NOT revoke existing refresh tokens** on password change — security follow-up tracked in TODO.md.
- **Join table UI pattern**: Join tables (UserRole, RoleModule, UserOrganization, UserCompany, OrganizationCompany) resolve FK UUIDs to names via lookup maps (`role_map`, `module_map`, `organization_map`, `company_map`) loaded by the consuming React page. Dropdown values use `public_id` (UNIQUEIDENTIFIER) for the entity, but POST bodies pass internal `id` (BIGINT) for FK columns.
- **Contact entity**: Polymorphic child entity with nullable FKs to User, Company, Customer, Project, Vendor. Fields: Email, OfficePhone, MobilePhone, Fax, Notes. Uses instant workflow (ProcessEngine.execute_synchronous). **UI pattern**: React `<InlineContacts parentEntity={...} parentId={item.id} />` component in `build.one.web/src/components/InlineContacts.tsx` — wired on all 5 parent edit pages (Vendor, Company, Customer, Project, User) and all 5 parent view pages (pass `readOnly` on view). API calls `GET /api/v1/get/contacts/{parent}/{id}` for list, `POST/PUT/DELETE /api/v1/...contact` for mutation.
- **FK cascade on delete**: When deleting entities referenced by FK, nullify or delete child references first. Examples: `BillLineItemService.delete_by_public_id()` nullifies `InvoiceLineItem.BillLineItemId` before deleting; `ExpenseLineItemService.delete_by_public_id()` deletes blob → Attachment → ExpenseLineItemAttachment → ExpenseLineItem. SQL Server FK constraints have no CASCADE DELETE — application code must handle cleanup
- **Expense entity cascade delete**: blob (Azure) → Attachment record → ExpenseLineItemAttachment link → ExpenseLineItem. Each step in its own try-except so cleanup failures don't block the delete
- **Decimal precision**: All financial fields (rate, amount, markup, price, total_amount) must use `Decimal(str(value))` — never `float()`. Float round-trips corrupt values
- **QBO sync mappings**: `sync_to_qbo_bill()` must create `BillLineItemBillLine` mappings after storing QboBillLines, using `line_num` to correlate local BillLineItems with QBO API response lines. Without these mappings, subsequent `sync_from_qbo` creates duplicate BillLineItems
- **`qbo.*` IDs are NOT `dbo.*` IDs**: `qbo.Bill.Id`, `qbo.Purchase.Id`, `qbo.Invoice.Id` are staging-table internal PKs in a separate keyspace. The `qbo.BillLineItemBillLine` / `qbo.PurchaseLineExpenseLineItem` mapping tables only carry `dbo.BillLineItem.Id` / `dbo.ExpenseLineItem.Id`; to reach the dbo parent (`dbo.Bill.Id` / `dbo.Expense.Id`), hop through `dbo.BillLineItem.BillId`. **Never alias `qb.Id AS BillId`** in a `qbo.*` join — use `QboBillId`, etc. (See `feedback_qbo_dbo_id_keyspaces.md` — OHR2-GUEST-09 incident, 2026-04-26.)
- **MS outbox has no human-cancel window**: the `build.one.scheduler` Function App POSTs `/api/v1/admin/outbox/drain` every 30s, so any row enqueued via `BillService.sync_to_excel_workbook()` / `ExpenseService.sync_to_excel_workbook()` / etc. is likely to be drained before you can review it. Audit IDs *before* calling enqueue — the only after-the-fact recovery is `clear_excel_range` row-by-row.
- **Invoice line item enrichment**: `enrich_line_items()` in `entities/invoice/business/enrichment.py` batch-fetches parent data per source type in one DB connection. Returns `vendor_name`, `parent_number`, `source_date`, `sub_cost_code_number/name`, `cost_code_number/name` (via `SubCostCode → CostCode` join), and `attachment_public_id`. Called by the packet generator in `entities/invoice/api/router.py`.
- **Invoice PDF packet**: `POST /api/v1/generate/invoice/{id}/packet` prepends two reportlab-generated TOC pages (basic: type→vendor; expanded: cost_code→type→vendor with subtotals) before merging attachment PDFs. Uses `pypdf` + `reportlab` (both in requirements). TOC "Type" column is derived from `source_type` — no schema field needed.
- **Invoice QBO sync is pull-only** (re-enabled 2026-04-26). `scripts/sync_qbo_invoice.py` populates `qbo.Invoice` + `qbo.InvoiceLine` staging and creates local `dbo.Invoice` + `dbo.InvoiceLineItem` rows via `InvoiceInvoiceConnector.sync_from_qbo_invoice`. **The connector creates ILI rows with `SourceType='Manual'` and no source FK** — it does not back-resolve QBO LinkedTxn references to local Bills/Expenses. The packet generator and Excel reconciliation depend on the source FK, so any QBO-pulled invoice needs a fingerprint-matching step (`qbo.InvoiceLine` Description+Amount+ServiceDate → `qbo.BillLine` AND `qbo.PurchaseLine`) before generating its packet. Procedure documented in the InvoiceAgent playbook (auto-memory).
- **Invoice `complete_invoice` does NOT push to QBO** (deliberately disabled at `entities/invoice/business/service.py:406-409`). Local invoices created via `complete_invoice` mark source `IsBilled=True` locally but do not flip QBO `BillableStatus`. This means QBO continues to suggest already-locally-billed lines in its "Suggested Transactions" tray. Re-enable only when bidirectional QBO invoice sync is fully wired.
- **Contract Labor status workflow**: `pending_review` → `ready` → `billed`. An entry is `ready` only when it has a `bill_vendor_id` AND at least one complete line item. `pending_review` means line items are missing or incomplete.
- **Contract Labor IsBillable flag**: `ContractLaborLineItem.IsBillable = false` means the item is shown on the PDF with `$0.00` and excluded from `total_amount`. Use `is_billable is not False` (not `if is_billable`) to handle `None` (default billable) correctly.
- **Contract Labor BillLineItemId FK**: `ContractLaborLineItem.BillLineItemId` links back to the generated `BillLineItem`. The UPDATE sproc uses a `CASE WHEN` guard to preserve it when `NULL` is passed. Always read and re-pass the existing value when updating a line item to avoid wiping the FK.
- **Contract Labor bill_service variable shadowing**: In `_generate_combined_pdf()` and `generate_bills()`, inner loop accumulator vars must not be named `total_amount` — use `scc_amount`/`scc_price` to avoid shadowing the outer bill total.
- **Contract Labor bills summary**: The per-vendor aggregation for the React Bills page lives at `entities/contract_labor/business/bill_summary.py::build_bills_summary(billing_period)`. Exposed via `GET /api/v1/contract-labor/bills-summary?billing_period=...`. Returns vendor-grouped dicts with `line_items[]`, day-aggregated `line_items_summary[]` (billed_hours / cost_before_markup / price_after_markup), totals, and date range. Extracted from the deleted Jinja `bills_page` web controller in Wave E3.
- **Scroll restoration**: Pages that scroll via `<main id="content" class="main-content overflow-y: auto">` must use `document.getElementById('content').scrollTop`, NOT `window.scrollY` / `window.scrollTo()`. Save to `sessionStorage` on navigation; restore on `DOMContentLoaded` with double `requestAnimationFrame`.
- **Contract Labor import tuple unpack**: `_parse_row()` returns `(dict, skip_reason)` — always unpack with `parsed, skip_reason = self._parse_row(...)`. Assigning to a single variable and calling `.get()` on the tuple crashes at runtime.
- **VENDOR_CONFIG**: Single source of truth for vendor rate/markup lives in `entities/contract_labor/business/bill_service.py`. If the React contract-labor pages ever need it client-side, expose via an API endpoint that returns the dict — do not duplicate the config in TypeScript.
- **Review-submit notifications (May 2026, forward-of-original)**: First-time draft Bill creation auto-fires an email notification via `entities/review/business/notification_service.py`. The notification is a **draft FORWARD of the original vendor email** (`Bill.SourceEmailMessageId → EmailMessage.GraphMessageId → me/messages/{id}/createForward`) so reviewer replies stay in the same MS Graph conversation thread as the source. Recipients resolved by `dbo.ResolveReviewRecipientsByBillId` walking Bill → BillLineItem → Project → UserProject (filtered to `Role.Name='Project Manager'` / `'Owner'`) → User → Contact. **TO** = all PMs, **CC** = all Owners, **BCC** = `Settings.invoice_inbox_email` (always). Submitter excluded. Subject is auto-prefixed by Graph (`FW: <original>`). HTML preamble is prepended to the forwarded body via 2-step POST createForward + PATCH (PATCH is content-type-aware: `\n` → `<br>` for plain-text source emails; `<body>`-tag injection for HTML source emails). Preamble opens with a slash-joined firstname greeting (`Cassidy/Zach,`) when PMs are resolved. PDF attachment inherited from the source. Channel: MS outbox `Kind='send_mail'` → `create_forward_draft` (mode=draft, default) or `forward_message` (mode=send) by `REVIEW_NOTIFICATION_MODE` env var. Failure-isolated — never rolls back Bill or Review row. **Bills without `SourceEmailMessageId`** (manual web-UI creation, bill_folder intake) are silently skipped today; synth-email fallback design captured in TODO.md. v1 limits: see `project_review_notifications.md` memory + TODO.md "Review-submit notifications follow-ups". Runbook: `docs/runbooks/review-notification-failed.md`. **Reviewer assignment requires populating `UserProject.RoleId`**; no React UI yet, do via SQL.

## External Integrations (QBO + MS) — Hardening Patterns (April 2026)

Both `integrations/intuit/qbo/` and `integrations/ms/` follow the same hardened shape after the April 2026 refactors. Future external integrations must follow this pattern.

- **`base/` package per integration**: `client.py` (shared HTTP wrapper), `errors.py` (typed exception hierarchy with `is_retryable`), `locking.py` (`sp_getapplock` context manager), `logger.py` (`LoggerAdapter` that auto-injects `correlation_id` from a ContextVar), `retry.py` (RetryPolicy + `execute_with_retry` with backoff + jitter + Retry-After), `correlation.py` (ContextVar-backed correlation + idempotency key), `idempotency.py` (UUID generation / validation).
- **Durable outbox pattern**: every external write enqueues a row in `[qbo].[Outbox]` or `[ms].[Outbox]` instead of calling the external API inline. Drain is triggered by the `build.one.scheduler` Azure Function App (see "Scheduler Architecture" below), which POSTs `/api/v1/admin/outbox/drain` every 30s. The handler claims rows via `ClaimNextPending*Outbox` (UPDLOCK+READPAST) and dispatches through per-Kind handlers. 5 failed attempts → dead-letter. Policy C debounce coalesces rapid edits via `ReadyAfter` column. (In-process APScheduler fallback exists in `shared/scheduler.py` gated on `ENABLE_SCHEDULER=true`; currently disabled in prod.)
- **Write gates**: `ALLOW_QBO_WRITES=true` and `ALLOW_MS_WRITES=true` must be explicitly set (case-insensitive `"true"`) in prod App Service Application Settings. Default-deny — a fresh checkout cannot accidentally push to prod. The gate applies at both the shared HTTP client level AND at `*OutboxService.enqueue()`.
- **Scheduler jobs must be async-wrapped.** Only relevant if `ENABLE_SCHEDULER=true` (fallback mode). `shared/scheduler.py` registers every job as `async def wrapper()` that awaits `asyncio.to_thread(sync_fn)`. Blocking I/O (httpx, pyodbc) runs on the default thread pool; the FastAPI event loop stays free. Synchronous jobs registered on `AsyncIOScheduler` would block ALL user requests during the tick — do not regress this.
- **Token storage encrypted at rest**: `QboAuthRepository` and `MsAuthRepository` both wrap access/refresh tokens with `shared.encryption.encrypt_sensitive_data()` on write and `decrypt_if_encrypted()` on read. Requires `ENCRYPTION_KEY` env var in prod; self-heals plaintext rows on next write (no migration required).
- **Dead-letter recovery**: `scripts/retry_ms_outbox_dead_letters.py` (and future QBO equivalent) resets rows back to `pending`. Dry-run by default; `--apply` to mutate; `--kind` to filter.
- **Reconciliation**: `[qbo].[ReconciliationIssue]` / `[ms].[ReconciliationIssue]` capture drift + dead-letter escalations. Excel-kind MS dead-letters escalate to **critical** severity so they aren't silent. Daily reconciliation jobs detect "locally complete but not in external system" drift.
- **Completion pipelines enqueue, never call external APIs inline.** `BillService.complete_bill` / `ExpenseService.complete_expense` finalize the entity locally, then enqueue outbox rows for SharePoint upload, Excel sync, QBO push. The API returns 202 immediately; side effects drain asynchronously within ~5-30s.
- **Runbook discipline**: every external failure mode documented in `docs/runbooks/*.md` with Symptom / Severity / Diagnosis / Recovery / Verification / Prevention sections. Follow the existing runbook format when adding new ones.

## Scheduler Architecture (April 2026)

Recurring jobs (outbox drain, QBO pulls, daily reconciles) live in the sibling `build.one.scheduler` repo — an Azure Function App (**Flex Consumption**, Python 3.11) that POSTs to admin endpoints on the API.

- **Why separate**: in-process APScheduler on `-w 1/2` stole threadpool slots from user requests. Moving it out eliminates contention and lets the API run at `-w 2` comfortably.
- **Flex Consumption gotcha (2026-05-06).** Setting `AzureWebJobs.<funcName>.Disabled=true` on a Flex Consumption Function App de-registers that function from host discovery, and setting it back to `false` (or deleting the app-setting) does NOT cause re-registration even after stop+start. Other timers keep firing; the disabled-then-re-enabled one stays missing from `az functionapp function list`. **Don't use that flag on Flex.** Recovery is `func azure functionapp publish build-one-scheduler --python` to force re-discovery. See `docs/runbooks/deploy-restart-timing.md` for the full recovery procedure. (Symptom: `process_email_inbox` was paused mid-session via the Disabled flag; un-pausing required a redeploy.)
- **Admin endpoints** (see `shared/api/admin.py`): all require `X-Drain-Secret` header.
  - `POST /api/v1/admin/outbox/drain` — drains QBO + MS outboxes (30s cadence)
  - `POST /api/v1/admin/sync/qbo/{entity}` — one of bill/invoice/purchase/vendorcredit/vendor/customer/item/account/term/company_info
  - `POST /api/v1/admin/reconcile/qbo` — daily
  - `POST /api/v1/admin/reconcile/ms` — daily
  - `POST /api/v1/admin/bill-folder/tick` — claim-and-process one queued PDF (30s cadence; scheduler inner-loops up to 4 min)
  - `POST /api/v1/admin/bill-folder/enumerate` — scan `/source` and enqueue new PDFs with hour-window dedup (5m cadence)
  - `POST /api/v1/admin/email/poll` — poll the shared invoice mailbox for tagged messages (5m cadence)
  - `POST /api/v1/admin/email/process_one` — claim oldest pending EmailMessage and kick off email_specialist agent run (1m cadence, inner-drain up to 4 min)
  - `POST /api/v1/admin/email/extract/{public_id}` — DRAIN_SECRET-gated DI extraction trigger for verification (the email agent uses the RBAC-gated `/email-attachments/{public_id}/extract` instead)
- **Secret**: `DRAIN_SECRET` env var on both App Service + Function App. Never commit. Rotate if leaked.
- **Rollback to in-process scheduler**: set `ENABLE_SCHEDULER=true` on App Service + restart. Safe — `sp_getapplock` prevents Function + in-process from double-draining.
- **Fail-closed auth**: admin endpoints return 503 when `DRAIN_SECRET` is unset on the server, 401 on mismatch.

## App Service Config — Required Production Settings

- **Always On = true**: critical. Without it, the container gets unloaded after ~20 min of inactivity and cold starts take 10–15 seconds. Enable via `az webapp config set --name <app> --resource-group <rg> --always-on true`.
- **`-w 2`**: configured in `startup.sh`. Two gunicorn/uvicorn workers on B2. Fits in memory now that APScheduler is out of the web process. `-w 4` is feasible but unnecessary at current load.
- **Env vars** that must exist on App Service: `ENABLE_SCHEDULER=false` (in-process scheduler disabled), `DRAIN_SECRET` (matches Function App), `ALLOW_QBO_WRITES=true`, `ALLOW_MS_WRITES=true`, `ENCRYPTION_KEY`, plus DB creds. **Email-agent additions:** `email_agent_username` + `email_agent_password` for the new agent's login; `azure_document_intelligence_endpoint` + `azure_document_intelligence_key` (+ optional `azure_document_intelligence_api_version`, defaults to `2024-11-30`); `invoice_inbox_email` for the shared mailbox the poll service targets. `azure_encryption_key` is an optional dev-only override that takes precedence over `encryption_key` so a developer can decrypt prod-encrypted DB rows locally without touching the dev-only key (see `shared/encryption.py::_get_encryption_key`).
