# TODO

Carry-over items from sessions. Check off as done; prune anything stale.

## 🟢 Stand up the `build.one.api` test harness (owner: SDET / `/sdet`) — v1 SHIPPED 2026-07-11; risk-first backfill continues

- [x] **v1 — harness stood up (2026-07-11).** `pyproject.toml` `[tool.pytest.ini_options]` + `tests/` + `conftest.py` (repo-root `sys.path` anchor + `cl_line_item` factory) + **52 pure-logic unit tests, all green** (`python3 -m pytest -q` → **exit 0**, no longer exit-5). The exit-5 "silent pass" is now a real pass/fail gate. First risk-first batch (PURE logic, NO DB): money-is-`Decimal` (`VENDOR_CONFIG` rate/markup are `Decimal` not `float`, float-corruption canary tied to real config), API envelope + error-mapping helpers (`shared/api/responses.py`), invoice line-PDF filename contract (`build_line_pdf_filename`), contract-labor pure helpers (`get_due_date`, `generate_invoice_number`) + `_parse_row` tuple-unpack + `_parse_time_to_hours` `Decimal`, and **structural regression-guards** pinning the two documented CL bill-math traps in `generate_bills_for_vendor` (grouping uses `is_billable is not False`; no `total_amount` shadowing of inner `scc_cost`/`scc_price`). Both gates held; Codex Pass-1 caught 3 vacuous/weak assertions → tightened + mutation-verified to bite. Context: other repos already have suites (ios `BuildOneTests/` ~38 files · web co-located `src/**/*.test.ts` vitest · mcp `tests/` pytest).
- [ ] **Backfill — DB-integration coverage (deferred out of v1; needs a test-DB strategy).** The risk-first paths that require a live DB / sproc execution: **sprocs** (NULL-overwrite guard, `SET NOCOUNT ON`, no in-sproc `ROLLBACK`, FK cleanup order) → **RBAC scoping** (UserProject / `set_authz_context`) → **QBO mappings** (`BillLineItemBillLine`, `qbo.*.Id` ≠ `dbo.*.Id`). Design the test-DB approach (fixture DB / transactional rollback / sproc harness) first.
- [ ] **Follow-up — extract the CL bill consolidation math into a pure helper so the NUMBERS can be unit-tested (own Pass-1 review).** Today the Hours×Rate cost, `is_billable is not False` grouping, non-billable-still-A/P, and markup are inline in the DB-writing `generate_bills_for_vendor` — v1 covered them **structurally only**. A small behavior-preserving extraction (e.g. `consolidate_bill_line_items(items)` / A/P-total) makes the app's highest-risk money path numerically testable; it touches the financially-critical bill-gen path, so it needs its own correctness review (not a rider on a test-harness unit).
- [ ] **Test-env note:** `xlrd==2.0.2` (declared in `requirements.txt`) must be installed in whatever env runs pytest, or `tests/test_cl_parse.py` `importorskip`s (the `_parse_row` tests skip rather than run). `build.one.scheduler` is the same greenfield story if/when it gets logic. Full playbook: `build.one.team/prompts/test-automation.md`.

## ✅ RESOLVED 2026-07-10 — `master` reconciled to the prod line; it is now the canonical trunk

The former landmine (`master` was divergent + stale and would hard-regress prod) is **fixed**. On 2026-07-10 `master` was merged with `feat/model-cascade`, resolving the whole tree to the prod lineage, so `master`'s tree is now **byte-identical** to `feat/model-cascade` and the live `:latest` commit `4b13d1f` is an **ancestor of both** (`git merge-base --is-ancestor 4b13d1f master` → true). `master`/`origin/master` are converged and safe; **deploy from `master` going forward.** `feat/model-cascade` is now a redundant mirror carrying only the in-flight Path-B work below and is **slated for retirement** once that lands on `master`.

**Deploy discipline is unchanged:** `az acr build` builds the working tree, so still confirm HEAD is on the live `:latest` lineage before every deploy (`master` now is). The cascade/Foundry feature (`cc3965c`) remains an ancestor of `4b13d1f`, i.e. already live in prod.

## ✅ RESOLVED 2026-07-12 — Auth cross-site "hourly re-login" fix → Path B (custom API domain `api.bld-one.com`) — COMPLETE (API + web both live)

Fixes the forced-re-login-~hourly bug (web `TODO.md` Phase 0.5). **Path A (SameSite=Lax→None) was built then REVERTED**: Codex Pass-1 confirmed a **P1** — even with cookies transmitting cross-site, the CSRF double-submit can't be satisfied because the web (`app.bld-one.com`) can't read the host-only `token.csrf` cookie set on the API host to echo it as `X-CSRF-Token` → `_require_csrf` 403s. Path A only trades a 401 for a 403. Pivoted to **Path B** (same-site via shared eTLD+1 `bld-one.com`).

**Done + verified:**
- DNS (Squarespace): `api` CNAME → `buildone-…azurewebsites.net` + `asuid.api` TXT (ownership proof) — both resolve.
- `api.bld-one.com` custom hostname bound to the `buildone` App Service + free managed TLS cert (SniEnabled, thumbprint `0A44A6FB79C6336A58D861D382CCFB7AF3765E79`); serves 200.
- API code: **`1294d1f`** scopes ONLY the non-HttpOnly `token.csrf` cookie to `Domain=bld-one.com` (prod-only via the secure gate; dev host-only) so `app.bld-one.com` can read it for the double-submit; access/refresh stay HttpOnly host-only; SameSite stays `lax`. Codex Pass-1 **PASS**.
- **✅ API DEPLOYED 2026-07-11** — landed on `master` (via the `feat/model-cascade` merge) and shipped: `master` `b23cc8e` → `buildone:latest` digest `3a0ef43…`, `az webapp restart` done, prod healthy (604 routes on both `api.bld-one.com` + `azurewebsites.net`). The CSRF-domain code is LIVE but **inert until the web flips** (it only activates when the web calls `api.bld-one.com`).

**✅ WEB FLIP DONE 2026-07-12 — Path B COMPLETE.** Web `VITE_API_BASE_URL` flipped to `https://api.bld-one.com` (`build.one.web` `main` `9654743`), rebuilt + SWA-deployed to `app.bld-one.com`. Verified: the live bundle references only `api.bld-one.com` (old host gone from `dist`); CORS preflight from `app.bld-one.com` → `api.bld-one.com/auth/refresh` returns 200 with `Allow-Credentials: true` + `Allow-Origin: https://app.bld-one.com` + `Allow-Headers: x-csrf-token`. `app` + `api` now share eTLD+1 `bld-one.com` → same-site → SameSite=Lax cookies + CSRF double-submit both work. **Remaining human check:** confirm a live browser session on `app.bld-one.com` stays logged in past ~60 min (the actual hourly-logout fix). The old `azurewebsites.net` host stays live (iOS / scheduler / MCP unaffected).
- **No user regression while deferred** — the API fix is inert until the flip, so users are exactly as before (hourly logout) until the web ships.

## 🟠 OHR2-CHAPEL-09 follow-ups (2026-07-09) — code/guard items from the $925.54 double-bill debrief

Playbook side already shipped (KI-41..44, Step 3a pre-delete checklist, Delta-re-run removal checklist, Phase-1 duplicate-pair + page-audit screens). These are the code/guard halves, in priority order:

- [ ] **P1 — CustomerProject mapping stability + safe Step-3a ordering.** The ONLY delete of `qbo.CustomerProject` is the customer-sync connector's mapping-exists-but-Project-read-empty branch ([integrations/intuit/qbo/customer/connector/project/business/service.py:107-111](integrations/intuit/qbo/customer/connector/project/business/service.py)) — it deletes the mapping and re-binds/creates by name, so it can also mint a duplicate `dbo.Project` when the name lookup misses. Investigate why the Project read returned empty mid-run on 2026-07-08 (OHR2-CHAPEL, project mapping present in Phase 1, gone hours later; suspects: transient read failure, dedupe delete, authz context in that code path). Fix direction: make that branch heal-don't-delete (re-bind by name FIRST, delete the mapping only when a replacement binding succeeded), and make `InvoiceInvoiceConnector.sync_from_qbo_invoice` auto-heal a missing CustomerProject mapping (Step 1 heal recipe) instead of raising — that removes the no-invoice window from the Step-3a reset entirely.
- [ ] **P1 — Attachment intelligence: extraction on QBO-attachable/receipt intake + durable vendor invoice number.** DI/text extraction runs ONLY on the email-intake path today; the 16 OHR2-CHAPEL attachments had `ExtractionStatus`/`ExtractedTextBlobUrl`/`AIExtractedFields` all NULL on `dbo.Attachment`, so content-level checks download+parse blobs ad hoc. Build: (a) run extraction (DI or text-layer) at intake for QBO-attachable and receipt paths, persisting onto the existing `dbo.Attachment` extraction fields; (b) a **sync-proof vendor-invoice-number field** (new column on Expense, or extracted-fields lookup) that the purchase re-sync can't clobber — today `reference_number = doc_number or "QBO-<id>"` is re-passed unconditionally on every pull ([integrations/intuit/qbo/purchase/connector/expense/business/service.py:77](integrations/intuit/qbo/purchase/connector/expense/business/service.py)), so manual dbo edits revert within a scheduler tick (KI-42 documents the QBO-DocNumber interim); (c) surface the durable number in the packet TOC + DETAILS instead of `QBO-#####`. **This item unblocks the automated guards for BOTH customer-facing failure classes:** invoice-number cross-compare across lines (KI-41 double-bill) and foreign-project marker scan (KI-40 page leak) become queryable instead of ad-hoc.
- [ ] **P2 — Duplicate-vendor detector / vendor-merge workflow (KI-41).** WillScot/Mobile Mini-style splits: two vendor records for one merged company defeat vendor-keyed dedup. Detector shape: candidate pairs from shared remit-to/address/domain + co-occurring similar amounts; surface for human merge/alias decision (no auto-merge). Needs a vendor alias/merge primitive (map both to one canonical dbo.Vendor or an AliasOf column).
- [ ] **P2 — Scheduler↔interactive coordination primitive (KI-44).** Interactive reconciliation sessions race the 15-min QBO pulls (OHR2-CHAPEL: dates shifted mid-correction; ReferenceNumber reverts). Design: a short-TTL "interactive hold" the sync endpoints honor (DB flag or `sp_getapplock` the pull scripts respect, auto-expiring so a crashed session can't wedge the scheduler), scoped realm-wide or per-project. NOT the Flex `Disabled` flag (de-registers the function — runbook'd gotcha).
- [ ] **P2 — First-class line-removal / packet-regeneration delta path (KI-39/KI-36 adjacent).** Propagate a QBO line removal to all five surfaces in one call: dbo ILI delete + un-bill, column-H clear in BOTH DETAILS copies, orphan line-PDF cleanup in SP + Box draw subfolders (attachment-sharing aware), packet regeneration. Requires a Box workbook row-edit primitive (fill-in-place, versioned) — fold in the durable throttle fix (batch a project's DETAILS edits into ONE workbook version per run; see `reference_box_workbook_version_throttle`).

## ✅ RESOLVED (2026-07-09) — `ms.outbox.upload.link_failed` was a dead DB write-back, not a SharePoint upload failure

**Root cause (corrects the original framing).** The SharePoint upload *succeeds*; the failure is the non-fatal DB write-back that links the uploaded file to its Attachment. `ms.DriveItemAttachment.MsDriveItemId` is `BIGINT` (FK → `ms.DriveItem.Id`, a local PK), but both callers — `MsOutboxWorker._handle_upload_sharepoint_file` and `BillCreditCompleteService` — passed `uploaded_item["id"]`, the **Graph DriveItem ID string**. `DriveItemAttachmentConnector.link_driveitem_to_attachment` then runs `read_by_ms_driveitem_id(<string>)` against a BIGINT column → SQL type error → `DatabaseOperationError`, thrown before the guarded `create()` and swallowed by the caller. Failed **100% of the time**; never worked (Feb code). No `ms.DriveItem` record is created on the upload path either.

**Impact was nil.** The `ms.DriveItemAttachment` mapping is **write-only — nothing reads it** (`get_driveitem_for_attachment`/`get_attachment_for_driveitem` have zero callers). Only effect was one swallowed `DatabaseOperationError` per SharePoint upload polluting `AppExceptions`. No lost documents, no retry loop, no drain failures. (The earlier "retry loop / failed drains / wasted Flex" framing was the ACR-incident backlog, unrelated; and the `DatabaseOperationError` type was *correct*, not a mislabel — it genuinely is a DB error from bad input.)

**Fix applied (Option 1):** removed the dead link-back calls in `integrations/ms/outbox/business/worker.py` (+ the now-orphaned `_link_attachment_if_requested` method) and `entities/bill_credit/business/complete_service.py`. Noise gone, zero functional loss.

- [ ] **Optional cleanup (low priority):** `DriveItemAttachmentConnector` + the `ms.DriveItemAttachment` table/repo/sprocs are now fully orphaned (no callers). Drop them in a dedicated schema-cleanup change if desired; left in place here to keep this fix migration-free. If per-attachment SharePoint links are ever needed, build it properly (upsert `ms.DriveItem` → link by PK) **with** its consumer.

## Box outbox drain — hardening (2026-07-08)

Surfaced by a `drain_box_outbox` alert during a transient Box **503 `unavailable`** window on `box.excel.upload_version`. Box recovered on its own and nothing was lost, but the incident exposed two config smells that will re-bite on the next slow-Box spell:

- [ ] **Retry budget is self-defeating for Excel uploads.** A single hung `upload_version` attempt ran ~40s before Box returned 503, which exhausted the ~40s retry budget in *one* attempt (`remaining_budget_seconds=0.0`) — so a genuinely transient blip dead-letters with effectively zero retries. Either raise the Excel-upload retry budget or cap the per-request timeout so one slow response doesn't consume the whole budget. (`integrations/box/base/retry.py::execute_with_retry`.)
- [ ] **30s drain cadence vs ~40s lock hold → self-inflicted lock-timeout noise.** While a slow upload holds the `box_outbox_drain` applock (~40s), the next 30s timer ticks fail with `box.lock.timeout` (`integrations/box/base/locking.py`) and count as failed scheduler requests — amplifying the failure rate and tripping `buildone-sched-failed-invocations` faster than the real error count warrants. A lock-timeout means "another tick is still working," not a failure; don't count it as one (or align the drain cadence / lock-wait to the real upload duration).

## ✅ FIXED (deployed a241b73, 2026-07-09) — QBO sync HTTP path runs without system-admin authz context → poison-batch retry loop (2026-07-08)

**Fix:** made `_require_drain_secret` an `async` dependency (`shared/api/admin.py`) so `set_authz_context(is_system_admin=True)` runs in the request coroutine's context instead of a discarded anyio threadpool worker; it now propagates through `_timed`'s `asyncio.to_thread` into the sync. Deployed image `2143bf9b` / `:latest`. **Verified:** post-deploy `access-denied=0` (was: every row), purchase sync `success=True failed_count=0`, purchase watermark advanced `02:37→11:38`. Covers all four project-scoped syncs + reconcile.

Also deployed `4b13d1f`: **`?attachments=false` query param on `/admin/sync/qbo/{entity}`** (bill/vendorcredit) — runs an attachment-free pass to help clear a slow backlog; no-op elsewhere.

### 🟠 OPEN follow-up — bound the QBO bill/purchase pull so a backlog drains under the 240s gateway cap

Verification exposed a **pre-existing, separate** problem (NOT the authz bug — post-fix `access-denied=0`): a bill backlog can't clear via HTTP. As of 2026-07-09 ~12:55 UTC the `bill` watermark is still frozen at `02:31` with **~60 bills** pending (grew from 29 over the day as new QBO modifications accumulate on the held watermark). **All 60 are already fully synced** (verified: BillLineItems + `qbo.BillLineItemBillLine` + `qbo.BillBill`), so the loop does *no useful work* — it just reprocesses complete bills every 15 min, 504s at Azure's 240s gateway before the end-of-run watermark update, and never advances. Confirmed a full pass exceeds 240s **even with `attachments=false`** (60 bills × module-upsert + per-project Excel + per-bill SharePoint + Box enqueue). Harmless (no data loss; no brownout now that access + purchase + drains are healthy) but wasteful — pins one of two workers ~240s every 15 min + re-hits QBO. **Chris's call 2026-07-09: leave the loop; build Part A as a reviewed+tested change (do NOT blind-deploy; do NOT hand-advance the watermark).**

**Part A spec — bounded pull (needs testing against real QBO before deploy):**
- **Complication found:** the `QboBill` mirror does **not** carry `Metadata.LastUpdatedTime` (it's dropped in the external→mirror mapping — see [integrations/intuit/qbo/bill/external/schemas.py:121](integrations/intuit/qbo/bill/external/schemas.py) `metadata` vs [.../business/model.py](integrations/intuit/qbo/bill/business/model.py)). The watermark filter is strictly `>` ([external/client.py:153](integrations/intuit/qbo/bill/external/client.py)) and QBO returns bills **ordered by Id, not LastUpdatedTime**. So a naïve "cap N + advance watermark to the Nth bill's timestamp" is **unsafe** — it'd silently skip any bill sharing that same second.
- **Two safe implementation options:**
  1. **QBO-native pagination:** add `ORDER BY Metadata.LastUpdatedTime` + `MAXRESULTS=<cap>` to the pull query; capture the page's max `LastUpdatedTime` from the raw response; advance the watermark to it only after processing the *complete* same-timestamp group (never split a second). Bounds ALL per-bill work, not just attachments.
  2. **Idempotent-skip convergence (no watermark-semantics change):** in the per-bill loop, skip bills already fully synced (existing `qbo.BillBill` + line-maps) so re-runs get cheap; with per-bill commits persisting partial progress, a run eventually processes only the un-synced tail + skips the rest → completes < 240s → watermark advances to now normally. Lower data-loss risk (worst case = redundant work, not skipped bills); still needs the un-synced tail to fit one run.
- Add `SYNC_BILL_MAX_PER_RUN` (env, default preserves today's behavior for small batches). Mirror into `sync_qbo_purchase`. **Cannot be verified from a dev laptop** (no `ENCRYPTION_KEY`/QBO creds) — verify in the deploy env or a QBO sandbox.
- **Interim clear (when someone's in the deploy env):** `python scripts/sync_qbo_bill.py --skip-attachments` (no gateway cap, runs as system-admin via `__main__`) drains the backlog in one pass and advances the watermark — safe, no DB surgery. **DONE 2026-07-09 13:46 UTC** — ran the CLI `--skip-attachments`, 56 bills module-synced, `failed_count=0`, watermark advanced `02:31 → 13:46`; the failing-loop / `sched-failed-invocations` noise is cleared. (Attachment sync is idempotent — reuses existing Attachment rows by QboAttachable mapping — so a `--skip` run strands nothing and no cruft is created.)
- **Dominant cost = attachable full-realm prefetch (fix this in Part A).** `QboAttachableService.sync_attachables_for_bill` pulls the **entire realm Attachable list (~11k+ rows, paginated 1000/page, several minutes)** into a per-run cache ([integrations/intuit/qbo/attachable/business/service.py:101-105](integrations/intuit/qbo/attachable/business/service.py)) even to sync a handful of bills — this one-time prefetch is the main reason a with-attachments pass blows past 240s. The client already has a filtered `query_attachables_for_entity(entity_type, entity_id)` ([external/client.py:213](integrations/intuit/qbo/attachable/external/client.py)); use it per-bill for small incremental batches instead of prefetching all 11k.

## 🟠 P0 — QBO→dbo bill onboarding minted header-only "zombie" bills in prod (2026-07-07) — CODE FIX SHIPPED (U-006), deploy + data-repair pending

**Update 2026-07-13 (U-006):** compensating rollback shipped in code (pending Gate-2 deploy). The line-failure *source* was the authz bug already fixed + deployed as `a241b73` (see the ✅ FIXED section above); U-006 adds the missing *defense* so any FUTURE permanent line failure can no longer strand a header-only bill. Two items remain open below: **deploy the code fix** and **repair the 2 remaining live zombies** (separate owner-gated prod step).

Surfaced running the BR-MAIN-26 InvoiceAgent playbook: **30 `dbo.Bill` rows were created 2026-07-07 19:16–21:32 UTC as headers only — correct `TotalAmount` + `qbo.BillBill` parent map, but ZERO `dbo.BillLineItem` rows.** Only 4 bills created that day had line items. The invoice's 22 source bills were mostly in this state, so nothing linked/reconciled until repaired.

- **Root cause / mechanism.** `BillBillConnector.sync_from_qbo_bill` ([integrations/intuit/qbo/bill/connector/bill/business/service.py](integrations/intuit/qbo/bill/connector/bill/business/service.py)) commits the header (`BillService.create`) + `qbo.BillBill` mapping in separate service-layer transactions, then calls `_sync_line_items`, which **raises** if any line fails to project. The raise leaves a committed header + parent-map with no lines and no `qbo.BillLineItemBillLine` — a zombie. **Line creation works fine from the current local checkout** (re-ran `BillLineItemConnector.sync_from_qbo_bill_line` per line → clean BLI with SCC + Price + Project every time), so the failure is **prod-side**: the deployed API code (or its container tag — see `docs/runbooks/deploy-tag-pinned.md`) is behind, throwing on the line-create path. **The prod scheduler's periodic bill sync will keep producing zombies until the prod image is redeployed.** First action: diff deployed image vs `main`, redeploy `:latest`, confirm a fresh QBO bill onboards WITH lines.
- [x] **Compensating rollback (U-006, 2026-07-13).** `sync_from_qbo_bill` NEW-bill path now wraps `_sync_line_items` — on any raise it deletes the just-created header (`bill_service.delete_by_public_id`) + `qbo.BillBill` mapping, then re-raises (watermark holds; next re-pull rebuilds cleanly, idempotent). Shared helper `integrations/intuit/qbo/base/compensation.py::rollback_orphan_header`. Same fix mirrored into the two same-root-cause siblings: Purchase→Expense + VendorCredit→BillCredit. Existing-mapping re-sync branches deliberately NOT rolled back. Tests: `tests/test_qbo_zombie_rollback.py` (4). Codex xhigh review PASS. **⛔ Pending Gate-2 deploy** (`az acr build … buildone:latest` → restart).
- [ ] **Repair the 2 remaining live zombie bills** — separate owner-gated Gate-2 prod-data step, AFTER the code fix deploys (down from 14; most already repaired). Detection query:
  ```sql
  SELECT b.Id, b.BillNumber, b.TotalAmount, v.Name
  FROM dbo.Bill b LEFT JOIN dbo.Vendor v ON v.Id = b.VendorId
  WHERE NOT EXISTS (SELECT 1 FROM dbo.BillLineItem WHERE BillId = b.Id)
    AND EXISTS (SELECT 1 FROM qbo.BillBill bb WHERE bb.BillId = b.Id);
  ```
  Still-broken as of 2026-07-07 (all `qboMap=1`, 0 lines): `19271` Ferguson #SC80489 $120.15 · `19290` Integrated Power #14714 $29,632.50 · `19291` Liaison Home Automation #9953 $24,358.81 · `19292` The Clean Living Co #794 $9,413.56 · `19293` The Iron Grove #2480 $9,359.00 · `19294` SledgeCraft #107836 $1,293.82 · `19295` SledgeCraft #107840 $493.26 · `19296` Siteworks #26-0161 $53,577.25 · `19297` Green Acres #21028 $28,000.00 · `19299` SavATree #001927012 $406.00 · `19300` Southern Staircase #ISA000010726 $906.95 · `19301` Cobra #C10659 $2,182.50 · `19302` Alley-Cassetty #000655919 $28.32 · `19303` Walker Lumber #226574 $615.31.
  **Repair recipe** (per zombie bill, from the current checkout, under `set_authz_context(is_system_admin=True)`): resolve `dbo.Bill.Id` via `qbo.BillBill.BillId`, then for each `qbo.BillLine` on the mapped `qbo.Bill` call `BillLineItemConnector().sync_from_qbo_bill_line(dbo_bill_id, qbo_line)` — it creates the BLI (SCC from ItemRef, Price=Amount, Project from CustomerRef) **and** the `qbo.BillLineItemBillLine` mapping. Verify each bill's `SUM(BLI.Amount) == Bill.TotalAmount` afterward. These are QBO-onboarded bills → lines project but **attachments do not**; only pull QBO attachables if/when a bill is invoiced (CRITICAL #5). Draft-collision variant (a promoted email-intake draft already holds the number) → Step 3b.i of the invoice playbook.
- [ ] **Audit scope beyond 2026-07-07.** The detection query is date-agnostic — run it unbounded to catch zombies from earlier failure windows, and add it (or a `qbo.ReconciliationIssue` check) to the daily QBO reconcile so future zombies are flagged automatically instead of discovered mid-invoice-run.

### U-006 follow-ups (born from the compensating-rollback unit, 2026-07-13)

- [ ] **Invoice connector line-sync SWALLOWS per-line failures (different bug, deliberately out of U-006 scope).** `InvoiceInvoiceConnector._sync_line_items` ([integrations/intuit/qbo/invoice/connector/invoice/business/service.py:330-334](integrations/intuit/qbo/invoice/connector/invoice/business/service.py)) logs-and-continues on a line failure instead of raising — so it never strands a *zombie* (no compensating rollback needed, and a load-bearing comment now documents that coupling), but it CAN silently persist an invoice with missing lines. Decide: keep swallow (accept silent partial) or make it raise + adopt `rollback_orphan_header` like the other three. Coupled to invoice line-adopt/gap-detect logic — needs its own unit.
- [ ] **(Optional, low-value) Fold the two pre-existing inline compensating-deletes into `rollback_orphan_header`** — purchase mapping-failure rollback ([purchase/connector/expense/business/service.py:139-150](integrations/intuit/qbo/purchase/connector/expense/business/service.py)) + invoice phantom-cleanup ([invoice/connector/invoice/business/service.py:245-261](integrations/intuit/qbo/invoice/connector/invoice/business/service.py)) predate the shared helper and now duplicate its idiom. Consolidate for single-point-of-change (log level / future `ReconciliationIssue` emission). Deferred out of U-006 to avoid churning working, already-reviewed paths in a P0 fix.

## Contract-labor billing → QBO follow-ups (2026-07-06)

Surfaced while billing OVH / BR-MAIN contract-labor and pushing to QBO.

- [ ] **Map WVA (proj 46) + HA (proj 128) to their QBO customers in `qbo.CustomerProject`.** Both are unmapped today — the same gap that made the 14 OVH bills push as `NotBillable` with no CustomerRef until we created `CustomerProject#187` (proj 145 → `qbo.Customer#202`). When WVA/HA contract-labor bills get completed → pushed, they'll land under no project / not-billable-to-client until mapped. Their QBO sub-customers almost certainly already exist (OVH's did — `qbo.Customer#202` "Steve Richardson : OVH - 2031 Overhill Drive"); confirm + insert the two mapping rows so those pushes are correct on the first try. **Broader:** audit all actively-billed projects for a missing `qbo.CustomerProject` row before their first QBO push.
- [ ] **Harden the QBO bill sync: fail loudly when a billable line has no CustomerRef mapping.** `_build_qbo_line` ([integrations/intuit/qbo/bill/connector/bill/business/service.py:807-823](integrations/intuit/qbo/bill/connector/bill/business/service.py)) currently logs a warning and silently downgrades `BillableStatus` to `NotBillable` when `_get_qbo_customer_ref(project_id)` returns None. That silent downgrade is what let 14 OVH bills push in the wrong state unnoticed. A billable line with a project but no mapping should raise (like the missing-Item-mapping path two lines up does) so the push dead-letters instead of quietly producing a not-billable bill.
- [ ] **Reassign OVH (proj 145) off the local "0 - Temp Customer" placeholder** (`dbo.Project.CustomerId = 1`) to its real client (Steve Richardson / the correct `dbo.Customer`). The QBO push works regardless (mapping is project→QBO-customer directly), but the local project pointing at the temp customer is wrong and will confuse any local customer-scoped reporting. Check other projects for the same temp-customer placeholder.
- [ ] **Refresh the stale local `qbo.BillLine` mirror for the 14 re-pushed OVH bills** (QboIds 72822–72835). `update_has_been_billed_in_qbo` updates the live QBO bill + the `qbo.Bill` header sync-token but does NOT re-store the `qbo.BillLine` rows, so the local mirror still reads the pre-fix `NotBillable` / no-CustomerRef state. Live QBO is correct; only the local mirror is stale. A `scripts/sync_qbo_bill.py` pull over those bills (or a targeted re-store) reconciles it — do it before any reconciliation job runs on those bills or it'll flag false drift.
- [ ] **Back-fill earlier missed non-billable A/P.** Before the 2026-07-06 fix, `generate_bills_for_vendor` silently skipped non-billable-only SCC groups, so the contractor was under-billed for those hours (e.g. Ricky Moreno's `HE7`/`HE12` non-billable lines on his OVH CL never got an A/P bill). The code now bills them going forward; identify + back-fill any already-`billed`/closed CLs whose non-billable lines were dropped. Query: CL line items with `IsBillable=0` and `BillLineItemId IS NULL` on CLs whose other lines are billed.
- [ ] **Remove the temporary SQL firewall rule** `claude-temp-session-access` on server `bchristopher` (rg `buildone_group`) once the current DB-work session is done — `az sql server firewall-rule delete --resource-group buildone_group --server bchristopher --name claude-temp-session-access`. (Session-authorized on 2026-07-06 to restore DB access after the IP rotated off the allow-list.)

## Structured Alias entities — ProjectAlias, VendorAlias, SubCostCodeAlias, CostCodeAlias (2026-06-26)

Today's email_specialist live-run kept tripping on entity resolution across all four parent types:
- **Project**: invoices reference projects by ship-to address, homeowner name, marketing nickname ("Bluebird"), or street-number-with-letter-suffix (`7550C Buffalo` vs `7550 Buffalo Rd`) — none of which line up cleanly with `Project.Name`. Current workaround is the Bluebird→OHR2 line we hand-appended to `Project.Notes` yesterday.
- **Vendor**: senders use DBA names, abbreviations, parent-company names, and divisional brands — e.g., emails from `accounting@leebp.com` say "Lee Masonry Products" but the vendor row is "Lee Building Products"; Cobra invoices come from `stacey@cobrallc.net` (FromName "Stacey Downs" — the AR clerk, not the vendor name).
- **SubCostCode**: reviewers and vendors reference SCCs by old numbering (`62.0` vs canonical `62.00`), name shorthand ("trim labor" → 44.00 Trim Labor), or vendor-internal codes ("HVAC rough" → 25.00 HVAC). `SubCostCode.Aliases` is a single comma-separated string today — brittle and lacks provenance.
- **CostCode**: same story one level up — division shorthand ("trade labor", "site work") needs structured lookup.

All four currently rely on either free-text Notes / Aliases columns or substring-fuzz against the canonical Name. That's brittle: no provenance, no confidence, no way to record what an alias originated from. **One unified entity pattern** across all four parents — same shape, same agent tool family, same UI affordance.

**Proposed shape (template — applied to all four entities):**
```
entities/{project|vendor|sub_cost_code|cost_code}_alias/
  api/router.py
  business/service.py
  persistence/repo.py
  sql/dbo.{project|vendor|sub_cost_code|cost_code}_alias.sql
  intelligence/tools.py  -> record_{parent}_alias(parent, type, value, source, confidence)
```

`dbo.{Parent}Alias` table (one per parent):
- `Id BIGINT PK`, `PublicId UUID`, `RowVersion`, `CreatedDatetime`, `ModifiedDatetime`, `CompanyId`, `CreatedByUserId`
- `{Parent}Id BIGINT FK NOT NULL`
- `Type NVARCHAR(40) NOT NULL` — per-parent enum (see "Type vocabularies" below)
- `Value NVARCHAR(200) NOT NULL` — the actual hint string
- `NormalizedValue NVARCHAR(200) NOT NULL` — case-folded, punctuation-stripped, persisted for lookup
- `Source NVARCHAR(40) NOT NULL` — `manual` | `agent_inferred` | `vendor_supplied`
- `Confidence DECIMAL(5,4) NULL` — only populated when `Source = 'agent_inferred'`
- `IsActive BIT NOT NULL DEFAULT 1` — soft-delete for retired aliases
- Filtered unique index `UQ_{Parent}Alias_Value_Type_Parent_Active` on `(NormalizedValue, Type, {Parent}Id)` where `IsActive = 1`

**Type vocabularies (per parent):**
- **ProjectAlias.Type**: `address_alias` | `nickname` | `vendor_referent` | `homeowner_name` | `mailing_label` | `lot_number`
- **VendorAlias.Type**: `dba_name` | `abbreviation` | `division_brand` | `parent_company` | `vendor_internal_code` | `domain_alias`
- **SubCostCodeAlias.Type**: `legacy_number` | `name_variant` | `vendor_referent` | `reviewer_shorthand`
- **CostCodeAlias.Type**: `legacy_number` | `name_variant` | `division_shorthand`

**Upgrade the `Find*ForInvoice` sproc family** to JOIN against the new alias tables and score structured matches HIGHER than the existing free-text fuzz:
- `1.00` `alias_exact` — `{Parent}Alias.NormalizedValue` exactly matches lookup chunk
- `0.95` `alias_substring` — alias is a substring of the lookup hint
- `0.90` existing `name`/`abbreviation` exact (current top-of-rankings)
- lower tiers — existing prefix/substring fuzz (kept as fallback)

For SubCostCode + CostCode lookup (called by bill_specialist + reviewer_reply paths), upgrade `FindSubCostCodeForReply` similarly: alias_exact > number > name > current alias-string LIKE fuzz.

**Agent tools** (one per parent — same handler shape):
- `record_project_alias(project_public_id, type, value, source, confidence)`
- `record_vendor_alias(vendor_public_id, type, value, source, confidence)`
- `record_sub_cost_code_alias(sub_cost_code_public_id, type, value, source, confidence)`
- `record_cost_code_alias(cost_code_public_id, type, value, source, confidence)`

When an agent resolves a parent via fuzzy match + the user later confirms (via [Review] reply approving the draft Bill / line coding), the agent auto-persists the fragment as `agent_inferred` with the resolved confidence. Next invoice referencing the same fragment hits `alias_exact` instead of fuzz — the agent gets compounding-smarter every time AP confirms a match. Manual UI entries → `Source='manual'`, `Confidence=NULL`.

**Why a child entity per parent, not just more columns:**
- A parent commonly has 3-10+ aliases (e.g., Cobra LLC: dba "Cobra", domain "cobrallc.net", AR name "Stacey Downs", etc.).
- Per-row provenance (`Source`, `Confidence`) doesn't fit into a single column.
- Soft-deletable so retired aliases (e.g., DBA renamed) don't bleed into matches.

**Backfill plan:**
- Migrate the Bluebird→OHR2 alias we hand-added to `Project.Notes` yesterday into a real `ProjectAlias` row (`Type='nickname'`, `Source='manual'`).
- Migrate the existing `SubCostCode.Aliases` comma-separated string into `SubCostCodeAlias` rows (one per token, `Type='name_variant'`, `Source='manual'`). Then drop the column on a follow-up migration.
- Future cleanup pass: parse `Vendor.Notes` + `Project.Notes` free-text on every row, surface alias-shaped lines for human review-then-promote.

**Mechanical pieces (per parent — 4× the same work):**
- New entity package (mirror Contact's child-of-parent shape, but single-parent FK).
- New module row per alias entity + RBAC grants on parent's module roles.
- React surface: inline section on parent edit page, similar to `<InlineContacts />` component → `<InlineAliases parentEntity="project" parentId={...} />`.
- Migration: `scripts/migrations/alias_entities_create.sql` covering all four tables + indexes + the `Find*ForInvoice` sproc upgrades + `SubCostCode.Aliases` backfill.

Driven by: 2026-06-26 live-run friction where parser missed project on 4 of 7 vendor invoices (project lookup) + missed vendor on 3 of 3 Cobra invoices (vendor lookup keyed on sender name not company name). Today's lookups are binary (match canonical Name substring or fail); a structured hint layer makes the agent compounding-smart across all four parent types (every successful match teaches next time's lookup). Apply the pattern uniformly so the alias-handling shape is one mental model, not four.

## BillView page load is slow (2026-06-25)

User reported: clicking the "View Bill in build.one →" button in an agent confirmation forward (Bill 25801, public_id `46F24E84-A149-421E-A232-BFE037C4E3EC`) landed on the right URL after the PWA service-worker cache was cleared, but the page took noticeably long to render the data details (Bill header, attachment image, line items, etc.). UX friction; not a correctness bug.

Likely culprits (need diagnostic data before optimizing):
- BillView fires multiple serialized API calls (header → line items → attachments → reviews → enrichment lookups) — classic React-page waterfall.
- Each individual endpoint may also be slow if heavy joins (e.g., `/api/v1/get/bill/{public_id}` JOIN to vendor + payment_term + project lookups; `/api/v1/get/bill-line-items/...` JOIN to sub_cost_code + attachment thumbnails; the attachment image fetch from blob storage is a separate round-trip).
- Initial React render may be heavy if BillView renders synchronously instead of streaming sections.

Next action: ask user to open DevTools → Network tab on the next BillView load + share the top 2–3 slow endpoints (or screenshot of the waterfall). 10 seconds of inspection saves hours of optimizing the wrong thing. Once we know which endpoint or which serial round-trip is the long pole, options are:
- **(a) One slow endpoint** → server-side fix (denormalized join, missing index, or N+1 inside the sproc).
- **(b) Too many serialized round-trips** → parallelize with `Promise.all` (web side) OR add a `/api/v1/get/bill/{public_id}/full` endpoint that returns Bill + lines + attachments + reviews in one envelope.
- **(c) Heavy initial render** → split BillView into independently-suspending sections (`<Suspense>` per section) so the page chrome renders immediately and each detail card streams as its data arrives.

Lives in build.one.web (BillView page) + potentially build.one.api (denormalized read endpoint or sproc tightening). Cross-repo coordination needed once the diagnostic narrows the fix surface.

## Build.One agent — centralized orchestrator + Cowork-hosted EmailAgent loop (2026-06-25)

Architecture shift to unify trigger sources behind a single orchestrator agent and move the recurring email-processing loop out of in-process APScheduler into Cowork. Once shipped, ANY trigger (email poll, ad-hoc chat, scheduled job, future MCP-initiated work) hands a structured `EntityActionEnvelope` to the Build.One orchestrator, which routes to the right entity specialist. Eliminates per-trigger delegate-tool sprawl on email_specialist and positions for any future Cowork-hosted agent to use the same MCP/API surface (no repo access needed).

**Target architecture:**
```
  email_specialist (Cowork)  ─┐
  chat user (Scout/Build.One) ─┼─► Build.One orchestrator ──► bill_specialist
  scheduled jobs              ─┘    (EntityActionEnvelope)  ├──► expense_specialist
                                                            ├──► bill_credit_specialist
                                                            ├──► expense_refund_specialist (new — defer)
                                                            ├──► invoice_specialist
                                                            ├──► bill_payment_specialist (new — defer)
                                                            ├──► contract_labor_specialist
                                                            └──► time_tracking_specialist (TimeEntry + TimeLog)
```

Entity types in scope (10): Bill, Expense, BillCredit, ExpenseRefund, Invoice, BillPayment, TimeEntry, TimeLog, ContractLabor, ContractLaborLineItem. Two specialists (ExpenseRefund, BillPayment) don't exist yet — they're separate units.

**Carve-out decision REVERSED 2026-06-25.** The original plan kept Step 1b/1bx reviewer-reply applies direct (no orchestrator hop). During the build Chris chose **route everything through Build.One** — one uniform path, accepting the extra orchestrator hop's latency on reviewer-replies — so 1b/1bx now go through `delegate_to_buildone_orchestrator` too.

### Sequenced units

- [x] **Step A — BuildOneAgent layer (build.one.api).** DONE 2026-06-25 (branch `feat/buildone-orchestrator`). Three sub-units, each two-pass reviewed + committed:
  - [x] Sub A1 (`rename(agents): Scout → Build.One`, api commit `bf2ab4f` + web `2a3d6ee`): `intelligence/agents/scout/` → `buildone/`; agent name + `credentials_key` → `buildone_agent`; `seed.buildone_role.sql` + idempotent `scripts/migrations/rename_scout_to_buildone.sql` (preserves password hash + user_id — **still NOT run against prod; gated**); full web tray rename; CLAUDE.md + memory refs.
  - [x] Sub A2 (`feat(buildone): orchestrator + EntityActionEnvelope routing`, commit `edcecc2`): `delegate_to_buildone_orchestrator` + routing table (entity_type → specialist) + failure-isolation contract in Build.One's prompt; added `delegate_to_contract_labor` + `delegate_to_time_tracking`; kept the per-specialist delegates on email alongside for A3.
  - [x] Sub A3 (`feat(email-agent): route entity creation through Build.One orchestrator`, commit `27be943`): email Steps 1b/1bx/1c/9/9b build an envelope + delegate to the orchestrator; removed the 3 per-specialist delegate tools; email is down to one delegation tool.
  - Bridging decision: shipped **(a)** — Build.One passes `payload.task_markdown` (the specialist-ready body the trigger source already builds) straight through, so specialists are UNCHANGED. Serialization hardened: JSON routing header + a SEPARATE fenced markdown block for the body (no escaping a multi-line body into a JSON string).
  - **Remaining before prod value:** the credential migration + `BUILDONE_AGENT_*` env vars + image deploy + the live email→fleet smoke are one coordinated, approval-gated checkpoint (buildone isn't invoked in prod until A3 ships, so deploy was deferred to a single step).

- [ ] **Step B — MCP tools for the EmailAgent (build.one.mcp).** Today email_specialist runs in-API and uses Python-side tool calls; for Cowork to run the same agent prompt, every tool needs an MCP wrapper. All are thin pass-throughs to existing API endpoints (`/api/v1/get/email-message/{id}`, `/api/v1/email-attachments/{id}/extract`, `/api/v1/email-attachments/{id}/bridge-to-attachment`, `/api/v1/email-messages/{id}/outcome`, etc.). Tools needed:
  - `read_email_message`, `read_email_thread`, `search_email_sender_history`
  - `compute_attachment_totals`, `gather_invoice_context`
  - `extract_email_attachment`, `bridge_email_attachment`, `record_extracted_fields`
  - `find_bill_by_conversation_id`
  - `delegate_to_buildone_orchestrator` (built in Step A2)
  - `mark_email_outcome`

  Per `build.one.mcp/CLAUDE.md`: MCP is thin pass-through, no agentic logic / no LLM calls / no business logic. New tools follow the existing snake_case convention + are wrapped in `@logged_tool` + use pydantic models for input/output in the same file as the handler. Quality gates: `ruff check`, `black --check`, `mypy` (strict), `pytest`. Manual live smoke is the test-against-prod path.

- [ ] **Step C — Cowork EmailAgent recurring loop config (operational).** No code. Configure a Cowork session that:
  - Runs on a recurring schedule (every 5 min, matching the current in-API poll cycle).
  - Auths to MCP via the long-lived `MCP_AUTH_TOKEN` (per `build.one.mcp/CLAUDE.md` AUTH-1).
  - Uses the email_specialist prompt verbatim.
  - Iterates pending EmailMessages, stamps outcomes, returns. No persistent state in Cowork.
  - Operational handoff: turn off the in-API agent runner once Cowork is verified stable.

### Open Qs to lock during Step A design pass

- **Cross-repo rename sequencing.** If a single Cowork/Code session can't touch all 3 repos (api + mcp + web), Sub A1 might split into 3 mini-units (one PR per repo). Decide based on Cowork's repo-access model when the build kicks off.
- **DB rename safety.** The Scout User row owns audit-trail records (Workflow.CreatedByUserId rows). UPDATE the same row's Name (don't create a new user); rely on the existing UserRole/UserModule joins. Migration file goes in `scripts/migrations/` with a date prefix. Don't auto-apply against prod — display proposed SQL first + stop for approval.
- **Concurrency on the email-processing loop.** Today the in-API agent runner has implicit serialization (single APScheduler tick). Cowork's loop needs an `sp_getapplock` or `ClaimNextPendingEmailMessage`-style sproc guard so two concurrent Cowork sessions don't double-process the same email. Already exists per `ClaimNextPendingEmailMessage` — verify it's wired through the MCP path.
- **Failure isolation.** RESOLVED in A2. Build.One is a pure router — it never touches entities or stamps the source record. It returns a status line: `ROUTED ok` (+ specialist answer) / `ROUTED error | reason=…` / `ROUTING not_routable` / `ROUTING parse_error`. The trigger source (email_specialist) reads the status line and owns its own record's outcome — on `ROUTED error` it stamps `needs_review` with the reason; on `parse_error`/`not_routable` it retries once then flags. The EmailMessage never silently stays pending: email always finishes with `mark_email_outcome`.
- **Step B follow-up (Cowork).** `intelligence/agents/email_specialist/cowork_email_agent_prompt.md` still describes the legacy "switch hats to bill_specialist" pattern + references the removed `delegate_to_bill_specialist`. Rework it to the Build.One envelope path when Step B (MCP wrappers) is built.

### Why this matters

- **Eliminates per-trigger delegate-tool sprawl.** Today email_specialist has `delegate_to_bill_specialist` / `delegate_to_expense_specialist` / `delegate_to_contract_labor_specialist`. As 7 more entity types come online (BillCredit, ExpenseRefund, Invoice, BillPayment, TimeEntry, TimeLog, ContractLaborLineItem), the tool surface explodes. With Build.One, email_specialist has ONE delegate tool.
- **Positions ALL future trigger sources to reuse the same orchestrator.** Scheduler jobs, MCP-initiated batch work, chat-initiated actions — they all hand an envelope to Build.One. Routing decisions live in one place.
- **Decouples the email pipeline from the API process.** Today the agent runner is a Python loop inside the FastAPI app. Moving it to Cowork frees the API to focus on serving requests, and lets the agent loop scale / retry / fail independently. Also means a Cowork outage doesn't take down the API and vice versa.
- **No repo access needed for the recurring loop.** Cowork's email loop is read-state-via-MCP + write-state-via-MCP. No git, no deploys, no cross-repo coordination during routine operation.

### Source material

- Full session prompt for Step A drafted during 2026-06-25 conversation (see transcript `~/.claude/projects/-Users-chris-Applications-build-one/89de231e-047c-443b-a749-23c5505b81e0.jsonl` — search for "Build.One" / "BuildOneAgent" / "EntityActionEnvelope"). The prompt includes worked examples, two-pass-review constraints, and report-back structure. Re-extract and refine when the build kicks off.

## HA-04 InvoiceAgent run — follow-ups (2026-06-24)

HA-04 (dbo.Invoice 1057 / qbo.Invoice 975 / $321,143.66 / 89 lines) ran end-to-end after the 2026-06-23 audit fixes. SCC back-fill held (0 NULL on prior 8 gaps), `InvoiceInvoiceConnector` adopt-path preserved Step 4 linkage on the re-run, no new same-day (vendor, billnumber) duplicates were minted. Two structural gaps surfaced that the run worked around manually — capture for proper fixes:

- [ ] **(P1) `BillCreditService.sync_to_excel_workbook` is missing.** `BillService.sync_to_excel_workbook` (`entities/bill/business/service.py:1862`) and `ExpenseService.sync_to_excel_workbook` exist, but the BillCredit equivalent doesn't. Step 6 reconciliation surfaced 2 BCLIs missing from DETAILS (White Cap Credit -$64.66 / pid `1e46aab0...`; Walker Lumber Credit -$346.70 / pid `96b36df1...`), and Step 7 couldn't enqueue them. Step 7d wrote H on 87 of 89 rows because the 2 BCLI pids have no DETAILS row to tag. Net effect: BillCredits appear in the customer-facing packet PDF (their attachments were filled) but not as a separate (negative-amount) row in the project's DETAILS spreadsheet. Build a parallel `sync_to_excel_workbook` on `BillCreditService` following the Bill pattern — same column shape (negative amount in N, BCLI public_id in Z), same outbox `Kind='insert_excel_row'`, same per-SubCostCode insertion. Then either re-run Step 7 for these specific BCLIs on HA-04, or accept the historical gap and only apply going-forward. Cross-check: same gap likely exists on every prior invoice that carried a BillCredit. Quick audit query: count invoices with `BillCreditLineItem`-sourced ILIs that have no matching column-Z entry in their project's DETAILS.

- [ ] **(P1) Dead `email-attachments/` blobs in `dbo.Attachment.BlobUrl`.** HA-04's first packet generation skipped 2 attachments (both `IN124AAB.pdf`, dbo.Attachment ids 4298 + 4299) because their blob URLs pointed at `attachments/email-attachments/AAMk...Graph-msg-id.../...pdf` paths that returned 404 BlobNotFound. Both originally entered via the email pipeline (`EmailAttachmentBridgeService` references the EmailAttachment's blob path instead of copying), and at some point those underlying email-attachment blobs were cleaned up — leaving the bridged `dbo.Attachment` rows pointing at dead URLs. The packet generator silently `skipped` them rather than halting. Two pieces:
  - **Audit**: count `dbo.Attachment` rows with `BlobUrl LIKE '%/attachments/email-attachments/%'`, HEAD-check each against blob storage, and surface the dead set. Each dead row likely has a still-pullable QBO twin (Invoice-keyed attachable) that can replace the blob — HA-04's recovery used `sync_attachables_for_bill` + `sync_from_qbo_attachable` + re-link to mint fresh `dbo.Attachment` rows and `UPDATE`d `BillLineItemAttachment.AttachmentId` to point at them. Generalize that into a reconcile script.
  - **Prevention**: when `EmailAttachmentBridgeService.bridge()` runs, the bridged `dbo.Attachment` should either (a) copy the email-attachment bytes into a stable blob path it owns, OR (b) carry a back-reference + a "blob may be ephemeral" marker so the packet generator can re-fetch on demand. Today the bridge pattern is "no blob copy" (per CLAUDE.md), which is fast but leaves us exposed to upstream cleanup. Decide which trade-off to take.

  Related: the packet generator's `skipped: N` is a silent signal — generate should `errors=[...]` for missing-blob cases too, so coverage check + packet step both halt rather than ship a packet with missing pages.

## Email agent — product gaps surfaced during 6/22 inbox live-run (2026-06-23)

Shipped this session (in-context, with deploys): `AgentInquiryService` (self-forward of source email to invoice@ with the agent's findings + ask as HTML preamble) + outbox-worker `forward_message_id` + `html_preamble` draft-then-send path + prompt Step 0 (self-loop guard) / Step 1f (internal coding-overlay reply detection) / Step 10 (mandatory `reason` on every needs_review). Live-ran the agent in-session over **87 polled 6/22 emails**; cross-verified against MS Graph (108 Graph - 21 self-loop Inbox copies filtered at poll layer = 87 in DB). Step 1b worked end-to-end: Austin's `RE: [Review] Walker Lumber #806990` auto-detected via fuzzy `find_bill_by_conversation_id` (bill_number_hint=806990), SCC `"65.1"` resolved to `65.01 Miscellaneous Materials`, `apply_reviewer_decision` flipped Bill 18793 to `review_status=Approved` with Austin (user_id=20) recorded as reviewer. Deferred:

- [ ] **(P0) ContractLabor reviewer-reply apply path is missing.** Today's batch had 3 instances of PMs replying to the agent's automated `Contract Labor - {Worker} - {Project} - {Date}` notification with `"Approved. <project>. <SCC text> - <number>. (<desc>)"` — Step 1b is Bill-only; ContractLabor has no equivalent. Stamped all 3 as `needs_review` + `reviewer_reply` because no apply path exists. Session prompt drafted in conversation transcript at `~/.claude/projects/-Users-chris-Applications-build-one/89de231e-047c-443b-a749-23c5505b81e0.jsonl` (search for "Step 1bx"). Design: mirror Bill flow — new `find_contract_labor_by_conversation_id` sproc + helper, `apply_contract_labor_reviewer_decision` service + tool, `Step 1bx` prompt branch. Open design Qs: multi-SCC split (5h Ricky reply had `Cleaning 62.0` + `Trim Labor 44.0` with no per-SCC hour split), description hide-on-customer-invoice (Cassidy's Mac McFarland reply asked for raw worker description to be hidden), rejection vs needs-clarification semantics. Real test fixture: EmailMessage `301B0F78-C327-4D85-84A6-AC3F2D3F2E40` (Cassidy Re: Ricky Moreno TB3 6/18).

- [ ] **(P1) `EmailAttachmentBridgeService.bridge` refuses inline attachments.** Today's `FW: Ace` (4 MB Ace receipt JPG inline in Mac McFarland's iPhone email, forwarded by Chris with explicit "enter as a Bill under vendor Mac McFarland" instruction) had DI extract cleanly via `force_inline=true`, but `bridge_email_attachment` raised `ValueError("Inline attachments cannot be bridged (no blob)")` because the inline bytes are pulled on-demand from Graph for DI and never persisted to blob storage. Same pattern blocked `FW: Moran: Hoover Paint reimbursement` (3 inline JPGs, $1,308.05 reconciled total) and `FW: prado outlets` (inline PNG). Fix: either (a) persist inline bytes to blob on first force-inline DI run, OR (b) extend bridge to fetch + persist on-demand when blob_uri is null but graph_message_id + graph_attachment_id are available. See [entities/email_message/business/service.py:1335](entities/email_message/business/service.py#L1335).

- [ ] **(P1) `create_bill` requires PDF-only attachment.** Per [entities/bill/api/schemas.py](entities/bill/api/schemas.py) `attachment_public_id` docstring: "must be a PDF". JPG/PNG receipts (Ace, Hoover, prado from today) can't be passed even if the bridge gap (P1 above) were fixed. Decide: relax to accept any common image type, OR auto-convert JPG/PNG → PDF inside the bridge service (Pillow / reportlab one-pager wrap). Receipt-shaped intake is where this hurts most — counter purchases are nearly always photographed, not scanned-to-PDF.

- [ ] **(P2) Step 1e excludes `Fw:`/`Fwd:` subject prefix.** Carve-out was for the agent's own outbound inquiry forwards (Step 0 self-loop case) but also excludes Chris's manual `FW: Ace` / `FW: Hoover Paint` / `FW: prado outlets` forwards that DO carry AP-actionable instructions in the body. The detection should narrow the exclusion to `from=invoice@ AND subject starts with Fw:` (the actual self-loop shape) and let internal-domain `Fw:` from non-invoice@ senders fall through. See [intelligence/agents/email_specialist/prompt.md](intelligence/agents/email_specialist/prompt.md) Step 1e criteria.

- [ ] **(P2) Step 1f's vendor-domain-sibling requirement misses some manual-Fw shapes.** When Cassidy starts the thread by forwarding a vendor invoice to herself + invoice@ (Concept Coatings 1859 pattern) rather than Chris forwarding from invoice@, the vendor original lives in a different ConversationId and isn't a sibling of the focal Re:. Step 1f's "thread contains a vendor-domain sibling" check then fails and the focal falls through to standard flow. Loosen: also fire 1f when the thread shows ANY internal-domain `Fw:` of an external invoice subject (drop the requirement that the vendor-domain message be a sibling). See [intelligence/agents/email_specialist/prompt.md](intelligence/agents/email_specialist/prompt.md) Step 1f detection criteria.

- [ ] **(P3) DALE INC scanned PDF returned empty DI.** Document Intelligence extracted zero text from `Scanned_from_a_Lexmark_Multifunction_Product06-22-2026-112110.pdf` (65 KB). Likely low-res scan / image-only PDF / no embedded OCR layer. No autonomous classification possible. Optional improvement: fall back to `prebuilt-read` model (image OCR) when `prebuilt-layout` returns empty, OR surface a clearer "DI couldn't read this" signal so the agent reason explicitly mentions OCR failure instead of a generic ambiguous-content stamp. Test fixture: EmailMessage `7A888DF5-7710-4CE1-B8A6-8650D753982A`.

- [ ] **(P3) Personal-reimbursement Fw branch (Step 1g).** Today's `FW: Ace` (Mac McFarland) + `FW: Hoover Paint` (Sarah Rogers) + `FW: prado outlets` (Austin Rogers via personal CC) all follow the same pattern: internal employee uses a personal card on a project purchase, vendor sender is unknown / external Gmail / no-such-vendor, AP creates a Bill under the *employee* as a "personal-reimbursement vendor" with the original receipt as documentation. Mac uses `YYYY.MM.DD` monthly-bundle bill_number convention (Bill 2026.04.30 $118.75, 2026.05.31 $198.47). Worth a dedicated `Step 1g — Internal personal-reimbursement Fw` branch that parses Chris's preamble instructions + force-inline-DIs the receipt + delegates to bill_specialist with the reimbursement-vendor binding. Pre-req: bridge inline + JPG support (P1s above).

## TimeEntry auto-submit (deterministic prior-day sweep) — follow-ups (2026-06-23)

Shipped: `TimeEntryAutoSubmitService` + `POST /api/v1/admin/time-tracking/auto-submit-prior-day` (DRAIN_SECRET-gated, `TIME_AUTOSUBMIT_MODE` off|dry_run|on) + scheduler timer `auto_submit_prior_day` (18:00 UTC = noon CST). Deterministic submit-if-clean / flag-else for the prior day; excludes test/agent + duplicates. Adversarial review applied the Id-tie-break, business-tz `today`, NULL-status guard, actions cap, sargable WorkDate predicate. Deferred:

- [ ] **(medium) Root-cause `Id` tie-break on the shared status-read sprocs.** `ReadCurrentTimeEntryStatus` (canonical `dbo.time_entry.sql`) + the migration copies (`001`/`002`/`time_entry_view_team.sql`) + the embedded correlated subqueries in `ReadTimeEntriesPaginated`/`ReadTimeEntries` all do `ORDER BY CreatedDatetime DESC` with NO `Id` tie-break. `TimeEntryStatus.CreatedDatetime` is DATETIME2(3); `reject()` writes `rejected` then `draft` in two `SYSUTCDATETIME()` calls that can collide at ms → non-deterministic "current status" (a reject→draft entry can read as `rejected`). The auto-submit service's own discovery query was fixed (`, s.Id DESC`), but `submit()._validate_transition` still reads via the flawed sproc, so a same-ms reject→draft entry can throw a spurious transition error (caught + counted as `errors`, no bad write). Fix all status-read sprocs with `, s.Id DESC` for system-wide determinism.
- [ ] **(medium) Sweep-level `sp_getapplock`.** The auto-submit sweep has no concurrency guard; a manual `?work_date=` re-run overlapping the daily timer (or a worker re-fire) can double-write `submitted` status rows + double-enqueue the review outbox (NOT a double-bill — `AggregateTimeEntryOnSubmit` is `SourceTimeEntryId`-idempotent). Wrap `run_for_work_date` in `sp_getapplock('time_autosubmit_sweep')`, bail with `skipped: already_running` if not acquired.
- [ ] **(low) dry_run worker-linkage pre-flight.** `_has_labor_row` runs only under `mode=='on'`, so a `dry_run` report can't surface "would submit but worker has no Vendor/Employee linkage → aggregation no-ops" (the Parker-Hazen class). Add a read-only XOR-linkage check + `would_be_unaggregated` count to dry_run.
- [ ] **(low) Optional `work_date` 400-validation** on the endpoint (malformed date → opaque 500 today; matches the digest endpoint's existing shape).

## QBO pull-race guard — follow-ups (2026-06-23)

Shipped this unit: `integrations/intuit/qbo/base/pull_race.py` (`guard_lines_present` + `read_lines_riding_out_race` + `header_has_amount`) wired into all 3 QBO connectors (Bill/Purchase/VendorCredit), the 3 production pull scripts, and the backfill — fixes the cross-process pull-race that minted half-built (header-only / missing-line) entities under load. Connector raises a last-resort guard; pull scripts re-read past the race then DEFER (skip → watermark advances, no stall). Deferred:

- [ ] **No QBO reconciler for Purchase/VendorCredit.** The daily `/reconcile/qbo` runs `reconcile_bills` only (`shared/api/admin.py`); `ReconciliationService` has no `reconcile_purchases`/`reconcile_vendorcredits`. So a deferred RARE genuinely-line-less (TotalAmt>0 / no Line array) Purchase→Expense or VendorCredit→BillCredit is logged (WARNING) but NOT auto-recovered. Build the two detectors mirroring `_reconcile_bill_qbo_missing_locally`; optionally surface `deferred_count`/ids in each sync's result dict for observability. (Bills are counted by the existing reconcile; auto-recreate gated on `QBO_RECONCILE_BILL_AUTOFIX`, default off.)
- [x] **Half-built cleanup DECIDED + bill-dups DONE (2026-06-23).** Sweep: 141 half-built Bills + 286 Expenses. **0 customers over-billed** (every invoiced dup referenced only the keeper line) → INTERNAL cleanup, no customer-facing corrections. **Bill DUP cleanup done**: `scripts/cleanup_halfbuilt_dups.py` (95 bills / 101 dup lines, balanced, reversible backup `/tmp/halfbuilt_dup_backup.jsonl`, adversarial review wf_48d75cc2 caught+fixed 4 defects incl. the ContractLabor-FK budget-double-count). Remaining sub-tasks:
  - [x] **Expense DUP cleanup DONE (2026-06-23)** — `scripts/cleanup_halfbuilt_dups_expense.py` (commit d1fd9c7): 160 cleaned / 2 edges skipped, 0 dangling/double-mapped, restorable backup, adversarial review wf_76b5dc4d (staging-staleness rail + full-row backup + balance recheck). Keeper=enriched/invoiced, orphan=raw; sub-cases unmapped-orphan(122)/both-mapped-staging-dup(38).
  - [x] **MISSING-line heal DONE (2026-06-23)** — `scripts/heal_halfbuilt_missing.py` (commit 3512db8): 73 healed (6 bills + 67 expenses), all balanced, 0 new dups. (Dup cleanup shrank the set vs the earlier estimate.)
  - [x] **NULL-header Bill 16981 + 5 DRIFT-resyncable bills DONE (2026-06-23)** — connector update-path re-sync from QBO (QBO=source of truth), all balanced. (16981 → $1,500; 16178/17091/17202/17424/17425 re-synced.)
  - [ ] **Idiosyncratic half-built remainder (~30 records, internal-only, 0 customer impact) — per-record review, low priority.** Clean automatable patterns are exhausted; each needs judgment:
    - **~22 over-stated bill dups** the dup executor skipped: ~1 ContractLabor-referenced (deleting hits the budget-link FK), ~2 fit the size-2 shape but the dup line carries its OWN separate attachment (hash-verify before deleting the link, else move it to keeper), ~19 complex shapes (group size >2 / both-mapped / non-2× ratio). Over-statement double-counts in budget trackers but is otherwise internal.
    - **5 DRIFT bills NOT QBO-resyncable** — header, dbo lines, AND staging all disagree (staging Σ != header); needs a per-record truth call.
    - **2 expense dup edges** (9879 both-enriched; 1 non-identical) — both lines look real.
    - **6 pre-existing dangling `qbo.BillLineItemBillLine` mappings** (→ already-deleted lines) — deleting risks re-projection on next sync (qbo line re-creates the dbo line, unbalancing); leave or handle per-record.
- [ ] **(true root cause) Bill/Purchase staging line upsert is delete-then-reinsert** — that's the cross-process empty-read window. VendorCredit was changed to upsert-in-place (why it has 0 half-built credits). Make `_upsert_bill_lines`/`_upsert_purchase_lines` atomic/upsert-in-place to eliminate the race itself (the guard then becomes a pure never-fires safety net).
- [ ] **(low) `header_has_amount` raises ValueError on a non-numeric string total** (unreachable today — `total_amt` is `Decimal|None` everywhere). If a future caller passes a raw string, the defer check (outside the per-row try) would abort the sync. Make it defensive if a string-total path ever appears.

## QBO invoice phantom fix — follow-ups (2026-06-23)

Shipped: gap-detect/adopt in `InvoiceInvoiceConnector.sync_from_qbo_invoice` (stops minting phantom `-N` duplicate invoices) + don't-swallow `create_mapping` failure (rollback the orphan) + a one-time cleanup of 46 empty phantom headers. Adversarial review (wf_518ad3e3 → wf_4736eef0) hardened it: adopt only on a header **fingerprint** (total within $0.01 + non-blank matching date), never re-sync lines onto an already-populated invoice, loud rollback-failure. Deferred:

- [ ] **(P2, pre-existing) UPDATE-path `_sync_line_items` double-adds when line mappings are lost.** `sync_from_qbo_invoice`'s mapped-UPDATE branch re-projects QBO lines unconditionally; if an invoice's `InvoiceLineItemInvoiceLine` line-mappings are gone but the lines are source-backed (Bill/Expense, not Manual), the Manual-only fingerprinter can't match them → every QBO line is re-created as a phantom Manual duplicate. Pre-existing (not introduced by the phantom fix; the fingerprint guard makes a *wrong*-invoice adopt that triggers this ≈impossible). Fix: in the line connector, when a QBO line has no line-mapping AND no Manual fingerprint match, don't create a Manual dup on an invoice that already carries source-backed lines for that QBO line — reconcile against the source FKs or skip + warn.
## QBO cross-entity attachables P1 (2026-06-23)

- [x] **Inline same-entity fix DONE + deployed (commit a35c033, build ca6d).** `_query_attachables_with_fallback` now pulls the full realm list once (cached per sync run) + exact (type,id) filter — fixes the unsupported-WHERE 200-trust + the single-page (>1000) miss that produced false "no attachments" on pull. Unit-tested.
- [ ] **Historical backfill of the 1,448 Invoice-keyed attachables → source lines is NOT viable by filename fingerprint (proven via dry-run: 2/1448 resolve).** 1,013 have opaque filenames (UUIDs / `invoices/rogers-build-inc/<uuid>.pdf` email-intake uploads) with no metadata; 384 have an amount that matches no source line (markup/partial/different total); 36 already have a doc. Forcing it risks mis-linking a receipt onto the wrong customer-packet line. Options if revisited: (a) accept as not-auto-recoverable (docs remain in QBO; inline fix prevents future same-entity loss); (b) link them to the local INVOICE (InvoiceAttachment) — faithful to QBO, no ambiguity, but doesn't flow to the per-line packet without extending the packet generator; (c) OCR the blobs to extract amount/vendor (heavy, still ambiguous). Going-forward cross-entity coverage = the deferred daily all-realm reconcile timer (Component 3, build.one.scheduler).
- [ ] **ROUND BACK: attachable file-integrity + data-integrity initiative (the 1,448 + beyond).** Filename-fingerprint can't recover the historical 1,448, but they shouldn't just be written off — treat as a dedicated integrity pass when we revisit:
  - **File integrity:** verify each of the 1,448 (and ideally all 3,301 staged attachables) has a healthy, downloadable blob — exists in QBO (FileAccessUri/TempDownloadUri live), download succeeds, sha/size matches, and where already linked locally the blob in our storage is present + uncorrupted (reuse `AzureBlobStorage.exists()` / the Step-3 health-filter pattern). Surface dead/missing blobs.
  - **Data integrity (linkage):** build a proper attachable↔source-line reconciliation beyond filename amount-match — candidate signals: QBO `LinkedTxn` traversal (Invoice→source Bill/Purchase), OCR-extracted vendor+amount+doc# from the blob, and a human-review queue for the residual ambiguous set. Produce a report of **doc-less source lines** (the inverse view: which BillLineItem/ExpenseLineItem/BillCreditLineItem feeding completed invoices have zero attachment join rows) so packets' missing-doc exposure is visible even before recovery.
  - **Going-forward:** the deferred daily all-realm reconcile timer (Component 3, build.one.scheduler) to catch new cross-entity docs.
  - Decision framing for that pass: accept-as-QBO-only vs link-to-Invoice (faithful, needs packet-generator extension) vs OCR-resolve-to-line. Internal-only (no customer over-billing); prioritize the doc-less-source-line report first (cheap, high signal).
- [ ] **Component 3 — cross-entity attachable reconcile (daily timer; cross-repo).** Concept: every QBO doc is attached to ONE entity via `EntityRef (Type, Value)`. *Same-entity* docs (receipt on its own Bill) are now captured inline (the deployed fix). *Cross-entity* docs are attached to a DIFFERENT entity than they document — the 1,448 receipts attached to the customer **Invoice** that actually belong to an underlying **Bill/Expense/BillCredit** line. Per-entity sync structurally can't find these (it asks "what's on this Bill?" and the receipt is on the Invoice), so those source lines look doc-less and packets ship without them. Fix = a **whole-realm reconcile**: pull `query_all_attachables()` once (~3.3k rows), and for each Invoice-keyed attachable resolve the underlying source line + link the blob via the existing linkers. **Why a daily timer, not the per-entity loop:** it's a whole-realm question (can't be derived from one entity's id); per-entity would be N attachable-queries × the QBO 500-req/min ceiling, whereas one paginated all-list pull amortizes cheaply and is idempotent (`_upsert_attachable`/`sync_from_qbo_attachable` are hash/qbo_id-keyed). Shape: new `build.one.api` admin endpoint (e.g. `/admin/reconcile/qbo/attachables`, DRAIN_SECRET-gated) + a low-frequency timer in `build.one.scheduler`. **Blocked on the resolution problem** (above): filename fingerprint only resolved 2/1448, so this needs the better resolver (LinkedTxn / OCR / review-queue) from the file/data-integrity round-back first — the timer is just the delivery vehicle.
- [x] **(P1, from audit) SCC back-fill DONE (2026-06-23).** Data: 20 NULL-scc BillLineItems with a resolvable QBO `ItemRef → ItemSubCostCode` mapping back-filled (incl. the 8 on Invoice 1057 → 13.01/21.00/8.01/19.00, matching the audit's expected SCCs); 0 remaining; 0 on Expense/BillCredit (parity confirmed clean). Targeted scc-only UPDATE, reversible backup `/tmp/scc_backfill_applied.jsonl`. Code: `reconcile_project.py`'s direct mapping-create now back-fills scc from the QBO line's ItemRef (mirrors the connector resolution; targeted, scc-only) so the out-of-band path can't reintroduce the gap. (Manual tool — no deploy.)
  - [x] **(audit Finding 1 upstream) bill_specialist SCC-at-draft — WON'T FIX (2026-06-23, data-driven decision).** Analysis: SCC is genuinely a per-bill cost *classification*, not a vendor constant — of 312 vendors with history, 59% have NO dominant SCC (<70%), only 18% are ≥90%-confident. So an agent that "always resolves SCC at draft" would be WRONG for the majority, and a wrong SCC silently corrupts budget tracking (worse than NULL, which gets correctly assigned in human review). Live exposure is tiny (10 draft bills with a NULL-scc line), and the historical/QBO path is already fixed (connector translates ItemRef→scc + reconcile_project back-fills + the 20-line data back-fill). The only safe form would be a high-confidence-only vendor-dominant-SCC default (helps ~18%), judged not worth the agent-change risk vs. the existing QBO-backfill + review coverage. Revisit only if NULL-scc email-only (never-pushed-to-QBO) bills become a real pain point.

## TimeEntry/TimeLog agency agent (Claude Code / Cowork) — follow-ups (2026-06-19)

Shipped this unit: MCP tools `search_time_entries` / `get_time_entry_by_id` / `submit_time_entry` (build.one.mcp), `cowork_time_agent_prompt.md` (evaluate → submit-if-fully-clean / else flag + leave draft), and the `claude_agent` Time Tracking grant folded into the seed (Read + Update + CanViewTeam on the stacked `Time Tracking Specialist` role; **no** can_approve). Surfaced during the adversarial review, deferred:

- [ ] **`RoleModule.CanViewTeam` lives only in `scripts/migrations/time_entry_view_team.sql`, not canonical `entities/role_module/sql/dbo.rolemodule.sql`.** The migration ships `CanViewTeam`-aware `CREATE OR ALTER` versions of the RoleModule CRUD sprocs (`ReadRoleModuleBy*` / `CreateRoleModule` / `UpdateRoleModuleById`); the canonical file defines the same sproc names *without* it. Whichever runs last wins — re-applying the canonical file after the migration silently drops `CanViewTeam` from the sproc SELECTs, so `shared/rbac.py`'s `getattr(rm,'can_view_team',False)` reads False and any non-admin relying on team visibility (incl. a demoted claude_agent) goes blind on team rows. No bulk auto-apply runner exists, so it's a manual footgun, not a live regression. Fix: fold `CanViewTeam` (column ALTER-if-missing + all six CRUD sprocs) into canonical `dbo.rolemodule.sql`. Same "canonical SQL drifted from live migrations" class as the Expense/Bill drift noted below.
- [ ] **(low) `claude_agent` is `IsSystemAdmin=1` in prod**, so the new Time Tracking grant + CanViewTeam are currently **inert** — claude_agent already bypasses module + row scoping (verified via live query: isa=1, 135/135 UserProject, 0/36 TimeLog projects missing). The grant is the correct least-privilege fallback if claude_agent is ever demoted (as Apple Reviewer was 2026-05-12). If the time agent should run as a non-admin, demote claude_agent and re-run `gap1_agent_user_project_backfill.sql` so draft discovery still returns rows.

## ExpenseAgent (BillAgent parity) — follow-ups (2026-06-18)

Surfaced during the Unit 1 adversarial correctness hunt. Deferred (out of the immediate unit scope) but tracked:

- [ ] **`BillService.create` has the same partial-attach rollback bug just fixed in `ExpenseService.create`.** On attach-link failure the rollback does a bare `self.repo.delete_by_id(bill.id)` while the committed placeholder `BillLineItem` still references it — `FK_BillLineItem_Bill` has no `ON DELETE CASCADE`, so the parent delete raises 547, is swallowed, and both rows orphan (then wedge retries on the (vendor, bill_number) unique constraint). Mirror the Expense fix: delete the placeholder line item (cascade-aware `delete_by_public_id`) before deleting the bill. Latent today because bill creates rarely hit the partial-failure window.
- [ ] **Canonical `entities/expense/sql/dbo.expense.sql` is drifted** (warning header added 2026-06-18). `CREATE TABLE dbo.Expense` omits `IsCredit`/`CreatedByUserId`/`SourceEmailMessageId`; `ReadExpenses`/`ReadExpensesPaginated`/`CountExpenses` lack the live `@ActorUserId`/`@ActorIsSystemAdmin` Gap-1 RBAC params. Re-running the file from scratch would build a broken table and regress per-user scoping. Reconcile the canonical file to match the live sprocs (pull live defs, fold the migration history in). Same drift class likely on `bill`/`bill_credit`/`invoice` canonical SQL.
- [ ] **Inline line-item create is not Project-access-gated.** `ExpenseLineItemService.create` (and `BillLineItemService.create`) resolve `project_public_id → project_id` without `assert_can_access_project`, so a non-admin agent can attach a receipt line to a Project they have no `UserProject` membership on. Pre-existing cross-cutting pattern — fold into a dedicated line-item access-gating pass (decide QBO-connector + manual-UI exemptions first, since both legitimately stamp projects).

## Time-entry email digest — follow-ups (2026-06-16)

Daily per-worker prior-day summary email shipped (off by default). See SESSION_NOTES 2026-06-16. Remaining:

- [ ] **Go live (send).** Deployed + flipped to `TIME_ENTRY_DIGEST_MODE=draft` 2026-06-16; the 2026-06-15 batch was drafted, reviewed, and the template refined (see SESSION_NOTES). When ready, set `TIME_ENTRY_DIGEST_MODE=send` so workers receive it each morning (~8am Central, per the 2026-06-20 timer shift). (`ALLOW_MS_WRITES=true` already gates it.)
- [x] ~~**Verify clock-time timezone assumption.**~~ RESOLVED 2026-06-16. iOS stores TimeLog clock times in **local wall-clock** (verified against a real entry: Elmer Cordova 07:29→17:03 = 9.56h Central). The first draft batch rendered them UTC→Central (~5h shift); fixed `TimeEntryDigestService._fmt_clock` to render verbatim (no conversion). `BUSINESS_TIMEZONE` now only computes "yesterday".
- [ ] **Backfill worker Contact emails.** Field workers without a `Contact.Email` are silently skipped (logged `time_entry_digest.unreachable`). Add emails as needed. (All 6 workers in the 2026-06-15 batch already resolved — 0 skipped — but that's not the full roster.)
- [ ] **(low) Dead-lettered digest won't auto-retry.** Idempotency counts any-status outbox rows, so a digest that dead-lettered (5 failed sends) won't be re-attempted by a later sweep. Reset via the MS dead-letter recovery script if it ever matters.
- [ ] **(low) No tests.** Repo has no test infra; digest logic was smoke-tested standalone only. First candidate if a `tests/` convention lands.

## Box integration — go-live follow-ups (2026-06-16)

Box Excel-in-Box + document push went LIVE for the SharePoint-linked projects (24 workbook mappings, 46 ProjectFolder mappings across 23 projects; both pipelines prod-proven). Remaining:

- [ ] **ML (project 74) doc-push.** Its Box project folder (`ML 4524 Millrace Ln (2021)--2019--04`) has NO `14 - Invoices` / `15 - Draw Requests` subfolders (old 2019 project predating the convention). Create them in Box, then `POST /box/map-project` for both `doc_class`. Excel sync for ML is already live. Until then its bill/expense/credit PDFs skip cleanly (`skipped_unmapped_project`).
- [ ] **~65 non-SharePoint-linked projects.** No Box Excel (`[box].[ProjectWorkbook]`) or doc (`[box].[ProjectFolder]`) mappings → they skip. Deliberate later pass: unlike the SP-linked set (already on automated sync), these carry higher hand-entry duplicate risk, so confirm each isn't actively hand-entered before mapping. Workbooks are name-discoverable in `225 - Budget Trackers` (388262995180); doc folders under `200 - Rogers Build Projects` (388262164461) + the RBP/RBCC trees.
- [ ] **SharePoint sync still double-writes.** The MS/SharePoint Excel sync + module-folder doc upload still fire alongside Box (additive). Once SharePoint is confirmed retired, unlink: `DriveItemProjectExcelConnector.unlink_excel_from_project` (Excel) + delete the `[ms].[DriveItemProjectModule]` rows (docs). Harmless until then (writes to abandoned files).
- [ ] **Observability for Box events.** The App Insights *classic* `traces` query returns empty for `buildone` (workspace-based AI). Confirm the Log Analytics `AppTraces` query path before relying on it to monitor the first real completions + the daily `reconcile_box`.
- [ ] **(low) Re-stamp Box metadata.** The mapping runs created `[box].[ProjectFolder]`/`[box].[ProjectWorkbook]` rows but the best-effort `buildone_project_public_id` metadata stamp on the Box folders/files was skipped (the local mapping process had `ALLOW_BOX_WRITES` unset → write-gated POST swallowed). Re-stamp when convenient (purely informational Box-side linkage; not required for sync).

## Budget entity follow-ups (2026-06-11; plan: umbrella memory project_budget_entity.md)

Phase 0 + 1 shipped to the prod DB 2026-06-11; Phase 2 (variance) shipped 2026-06-12 (schema + sprocs + seeds + indexes live; code awaits combined commit + container deploy). Remaining:

- [x] **Phase 2 — variance engine** — **SHIPPED 2026-06-12** (`entities/budget/sql/dbo.budget_variance.sql` + `GET /get/budget/{public_id}/variance` + list rollups + `scripts/migrations/budget_variance_support_indexes.sql`; 3-agent adversarial verification: 455 rows × 9 legs recomputed independently across 4 projects, zero mismatches at ±$0.02; see SESSION_NOTES 2026-06-12). Residuals split out below.
- [ ] **Variance re-verification when data lands** (coverage gaps — prod data couldn't exercise these): budget legs beyond the zero-budget case, the EmployeeLabor leg (ELLI is EMPTY in prod), unpriced-hours with non-NULL Hours, IsOverhead=1 with non-NULL ProjectId. Re-run the per-leg recompute when real rows exist.
- [ ] **NULL-ProjectId line ops report/backfill**: ~$6.0M of cost-side line value (BLI 1,438 rows / $2.04M; ELI 6,795 / $3.93M; BCLI 23 / $28.8K; CLLI 14 / $5.3K) is invisible to EVERY project's variance because ProjectId is NULL. Policy-documented blind spot, but the magnitude warrants an ops sweep to assign projects (or explicitly write off).
- [ ] **Phase 3 — web UI** (after Phase 2): /budget/* routes in the trimmed app with a revived desktop layout route element (decision made 2026-06-11); BillEdit .li-card grid keyed by public_id; react-query conventions; flush+saveAll-bool before activate/approve; autosave `enabled` gated on revision status. Check whether the SWA deploy is live before shipping a new tab.
- [ ] **Lifecycle unit tests** — plan called for them; repo has no test infra so none shipped (deviation recorded). The rules are pure-Python branches in the three services + SQL guards (smoke-tested live 2026-06-11). If/when a tests/ convention lands, this is the first candidate.
- [ ] **QBO invoice re-pull clobbers ILI source links** (pre-existing, hurts Phase 2 drawn resolution): `connector/invoice_line_item/business/service.py` update path passes `source_type='Manual'` unconditionally — InvoiceAgent-linked lines degrade back to Manual on re-pull, migrating drawn dollars to Uncategorized over time. Guard the update path to preserve existing source FKs.
- [ ] **Voided QBO invoices** — Invoice has only IsDraft; a voided invoice pulled from QBO stays IsDraft=0 with its lines. Check how voids appear in qbo staging before trusting drawn totals (Phase 2 acceptance item).
- [ ] **Budget 'archived' flow** — status value + filtered unique index support re-baselining, but no archive endpoint/UI exists yet. Unscoped until a real re-baseline need arrives.

## TimeEntry list N+1 — fix before raising iOS timeouts (2026-06-12)

- [ ] **(P1) `GET /api/v1/time-entries` took 13.5s for 35 rows** (observed in prod 2026-06-11, App Insights). Root cause: per-row `read_current` status lookup in `_entry_dict_with_current_status` (the documented N+1) + count + project-ids batch. iOS now runs a 15s inter-byte request timeout — 1.5s headroom; the pull will intermittently fail on cellular until this is fixed. Batch the status lookup (one sproc returning latest status per entry id set, mirroring `ReadDistinctProjectIdsByTimeEntryIds`). Target <5s. Do NOT solve this by raising the iOS timeout (it bounds the worst-case UI freeze window).

## Time-tracking round-2 review — API findings (2026-06-10)

From the cross-repo invariant sweep (see build.one.ios/TODO.md for the 66 iOS findings). These break the iOS sync recovery logic from the server side:

- [x] **(CRITICAL)** Database unique constraint violations return 500 instead of 409, breaking iOS duplicate-claim detection — **FIXED 2026-06-10** (422 + original message; 409 deliberately avoided — iOS maps it to conflict flow)
  `build.one.api/entities/time_entry/api/router.py:491` — When a unique constraint violation occurs during CreateTimeLog (e.g., duplicate UX_TimeLog_TimeEntry_ClockIn key), the pyodbc.Error is caught by TimeLogRepository.create(), mapped to a DatabaseOperationError by map_database_error(), and bubbles up as an unhandled exception. FastAPI converts this to…
- [x] **(CRITICAL)** UpdateTimeLogById and UpdateTimeEntryById sprocs in the 2026-05-26 migration inherit the same NULL-overwrite bug — **FIXED 2026-06-10** (guards in scripts/migrations/time_log_update_guards_and_unique_indexes.sql; run at deploy)
  `build.one.api/scripts/migrations/time_entry_view_team.sql:824` — The 2026-05-26 time_entry_view_team.sql migration rewrote 6 TimeLog/TimeEntry mutation sprocs to add RBAC scope (the @ActorCanViewTeam branch). While threading actor scope, the sproc authors copied the original base-entity sprocs wholesale WITHOUT fixing the NULL-overwrite pattern. UpdateTimeLogById…
- [x] **(HIGH)** *(latent)* TimeLogService._actor_scope() does not thread @ActorCanViewTeam to all repo calls — **RESOLVED 2026-06-10**: panel itself verified code is correct today; fail-closed required-param signature deferred to sync-layer consolidation
  `build.one.api/entities/time_entry/business/time_log_service.py:21` — The service-layer _actor_scope() (lines 21-35) correctly reads the can_view_team flag from the ContextVar (line 34). However, not all repo calls in the service pass all three scope components. The pattern is inconsistent: some calls (e.g., read_by_public_id line 126-131) pass all three; but the muta…
- [x] **(HIGH)** *(latent)* No uniqueness constraint on (TimeEntryId, ClockIn) pair — duplicate-detection path never triggers — **SCRIPTED 2026-06-10**: index creation in the same migration, gated on dedup; run cleanup.time_entry_duplicates.sql first (prod HAS live duplicates: 12 auto-mergeable log twins, 3 conflict groups, 11 dup entry days)
  `build.one.api/entities/time_entry/sql/dbo.time_entry.sql:43` — iOS claims include duplicate-detection via timestampsMatch (iOS TimeEntryService.swift:121-125) — if a CREATE fails due to network drop and is retried, the client queries the server for matching (TimeEntryId, ClockIn) and claims the existing row instead of re-POSTing. This duplicate-recovery path de…

**Note on the unique index:** prod `dbo.TimeLog` may already contain duplicate (TimeEntryId, ClockIn) pairs from the unprotected retry window — audit + dedup BEFORE creating the index or the CREATE will fail.

---


## TimeTracking integration follow-ups (Phases 4 / 5 / 6 / 8 / 10 — deferred from 2026-05-28 session)

Phases 0–3 + 9 of the [Phase 1–3 schema sketch](docs/integration-timetracking-contractlabor-bill-phase-1-3-schema.md) shipped 2026-05-28. End-to-end aggregation proven against Brayan's TimeEntry id=24 → ContractLabor 513 / line 366 / $410.85. See SESSION_NOTES.md "TimeTracking → ContractLabor / EmployeeLabor end-to-end (2026-05-28)" for the full log. Remaining work:

- [ ] **Phase 4 — VENDOR_CONFIG dict cleanup.** `entities/contract_labor/business/bill_service.py:33-76` still hardcodes per-vendor rate + markup + address. Rewrite `_get_rate_for_vendor()` (and any caller) to read from `Vendor.HourlyRate` + `Vendor.Markup` (default) + `VendorProjectRate` (override) via `dbo.ReadEffectiveRateForVendorProject`. Keep the dict as a temporary fallback during the rewrite if cutover risk is high; delete in Phase 10.
- [ ] **Phase 5 — Invoice EmployeeLabor support.** When an Invoice completes, EmployeeLabor rows don't get marked `invoiced`. Two missing pieces in `entities/invoice/business/service.py`:
  1. `_mark_source_as_billed()` needs a `SourceType='EmployeeLabor'` branch: resolve `InvoiceLineItem.EmployeeLaborLineItemId → EmployeeLaborLineItem.EmployeeLaborId → set EmployeeLabor.Status='invoiced'`.
  2. New `InvoiceService.create_lines_from_employee_labor(invoice_id, period_start)` to pull `EmployeeLabor` rows where `Status='ready'` and emit InvoiceLineItems with `EmployeeLaborLineItemId` set. (Mirror of how Bill/Expense lines are added today.)
  3. `entities/invoice/business/enrichment.py::enrich_line_items()` needs an `employee_labor` source branch fetching Employee.Firstname/Lastname + Project + SubCostCode + EmployeeLabor.WorkDate. No attachment (EmployeeLabor has no PDF source).
- [x] ~~**Phase 6 — time_tracking_specialist agent tools + scheduler timer.**~~ Shipped on master. Tools `validate_time_entry_completeness` + `flag_time_entry_for_human_review` live at `entities/time_entry/intelligence/tools.py:63,95`; `flag_*` calls `dbo.StampTimeEntryReview` via `entities/time_entry/persistence/repo.py:543`. Function App timer `process_time_tracking` (30s cadence) registered + Running on `build-one-scheduler` (Azure, `function_app.py:209-218`). Inherits the `function:drain_outbox=1` always-ready pin per the Flex Consumption discipline.
- [ ] **Phase 8 — React UI work.**
  - **`/user/:id` Profile**: new "Worker linkage" section. Two radio options (Employee vs Vendor) with picker; XOR enforced client-side; backend FK is `User.EmployeeId` XOR `User.VendorId`; calls `UpdateUserWorkerLink` sproc via a new admin endpoint.
  - **Vendor edit page**: add `HourlyRate` + `Markup` fields. Add a `VendorProjectRate` overrides table (list / inline add+edit / delete) — schema + sprocs already live.
  - **Employee CRUD pages**: list / view / edit / create. None exist yet (entity is brand new).
  - **`/contract-labor/:id` review page**: when `SourceTimeEntryId IS NOT NULL`, hide the Excel-only fields (EmployeeName / JobName / TimeIn / TimeOut / BreakTime / RegularHours / OvertimeHours / ImportBatchId / SourceFile / SourceRow) and surface a "Source TimeLogs" subsection showing clock-in/out + per-log note (reverse-lookup `ContractLaborLineItem.SourceTimeEntryId → TimeEntry → TimeLogs`).
  - **`/employee-labor/:id` page** (new): mirror of ContractLabor edit but for the W2 path.
  - **`/time-entry/:id` view**: surface "Aggregated into ContractLabor #X" or "EmployeeLabor #Y" link when `dbo.ReadTimeEntryBilledLineage` returns rows.
- [ ] **Phase 10 — Final cleanup.** After Phase 4 ships, delete the VENDOR_CONFIG dict from `bill_service.py` entirely (no fallback needed once DB rates are proven for at least one billing cycle). Audit `bill_service.py` for the dict's `BILL_TO` constant — that's separate (company-side bill-from address) and should stay or move to Settings.

Open items / minor cleanups discovered in 2026-05-28 session:
- [ ] `entities/time_entry/persistence/repo.py:506-508` uses `float(r.TotalHours)` / `float(r.HourlyRate)` / `float(r.Markup)` in `aggregate_for_billing` return dict — Decimal precision regression. Used only for log messages today (not DB writes), low priority, but worth fixing to `str(...)` or returning Decimals.
- [ ] The aggregation sproc body is `cursor`-based (FAST_FORWARD LOCAL). Works for ≤ ~10 buckets per TimeEntry; could be set-based via OUTPUT clauses if performance becomes a concern. Not blocking.
- [ ] `dbo.AggregateTimeEntryOnSubmit` has `SET XACT_ABORT ON` but no explicit `BEGIN TRANSACTION` — per-statement auto-commit means a partial failure mid-cursor could leave some buckets committed and others not. Low risk in practice; wrap in transaction if you ever see drift.
- [ ] Brayan's TimeEntry A4F5FA03 is now `approved` but the aggregated ContractLabor 513 is in `pending_review` with no SCC. PM needs to open it and assign `SubCostCodeId` to flip it to `ready`. UI work to make this easy lands in Phase 8.

---

## Audit 2026-05-20 — API findings

Full multi-repo audit memory: `~/.claude/projects/-Users-chris-Applications-build-one/memory/project_audit_2026_05_20.md`. The items below are the API-side findings extracted from it. Order is rough severity; pair with that memory for context, cross-repo patterns, and rationale on what NOT to change yet.

### Critical
- [x] ~~**VendorCredit float() corrupts currency on every QBO round-trip.**~~ [integrations/intuit/qbo/vendorcredit/persistence/repo.py:92,154,177,184](integrations/intuit/qbo/vendorcredit/persistence/repo.py) — `float(vendor_credit.total_amt)` and similar for Amount/UnitPrice/Qty. Bill/Purchase repos correctly use `Decimal(str(...))`; VendorCredit regressed. Silent off-by-cents on every credit memo sync. Fix: replace with `Decimal(str(...))` (or pass Decimal directly; SQL Server DECIMAL accepts it). Add unit test round-tripping a 3-decimal value.
- [x] ~~**BillCredit complete uses float() for amounts + total + Excel filename string.**~~ [entities/bill_credit/business/complete_service.py:124,175,422](entities/bill_credit/business/complete_service.py). Writes precision-lost values to SharePoint filenames + Excel cells. Same fix shape as above.
- [x] ~~**ProcessEngine references non-existent `context.metadata`**~~ at [core/workflow/api/process_engine.py:433](core/workflow/api/process_engine.py). `if context.metadata.get("is_reply")` — TriggerContext has only `payload`. Latent: no live producer per memory, but first re-wire of email intake = AttributeError. Fix: `context.metadata.get("is_reply")` → `context.payload.get("is_reply")`.
- [x] ~~**Latent SQL injection in `shared/access.py::_check()`**~~ — `f"SELECT dbo.{udf_name}(?, 0, ?)"` interpolates UDF name into SQL. All call sites hardcoded today; one refactor away from exploitable. Fix: whitelist dispatch dict `{entity_type → udf_name}`.

### High
- [x] ~~**`current_user_id` ContextVar not set in `InstantWorkflowHandler.execute()`.**~~ [core/workflow/business/instant.py:334-339](core/workflow/business/instant.py). Tenant_id is threaded; user_id is not. Scheduler-triggered instant workflows write NULL for `CreatedByUserId` on every downstream sproc that depends on the ContextVar fallback. Fix: `token = current_user_id.set(context.user_id); try: method(**kwargs); finally: current_user_id.reset(token)`.
- [x] ~~**Workflow row + WorkflowEvent write are NOT atomic.**~~ Fixed 2026-05-27. Repos now accept an optional `conn` parameter; `create_workflow()` and `transition_to()` open one connection and pass it to both the Workflow write and the WorkflowEvent write. Also removed a redundant DB persist from the `state` setter (transitions library sets state in-memory; `transition_to` persists atomically with the event). Bonus: eliminated one extra DB round-trip per transition.
- [x] ~~**Workflow event `_json_serial` missing UUID + bytes handlers.**~~ [core/workflow_event/persistence/repo.py:24-30](core/workflow_event/persistence/repo.py) and [core/workflow/persistence/repo.py:24-30](core/workflow/persistence/repo.py). Any payload carrying `public_id` (UUID) or `row_version` (bytes) silently fails the audit write with `TypeError`. Fix: add `uuid.UUID → str(obj)` and `bytes → base64.b64encode(obj).decode('ascii')` handlers.
- [ ] **OAuth refresh race — QBO + MS both vulnerable.** [integrations/intuit/qbo/auth/business/service.py:134-248](integrations/intuit/qbo/auth/business/service.py) and [integrations/ms/auth/business/service.py:232-268](integrations/ms/auth/business/service.py). `sp_getapplock` acquired AFTER reading the auth row → two callers can each POST the pre-lock refresh_token to Intuit/Graph; whichever writes the DB row last wins, and the fresher token is silently overwritten. Fix: acquire the lock BEFORE the read so the pattern is "lock → read → check freshness → refresh if still stale → write".
- [ ] **`_run_complete_bill()` (and Expense/Invoice/BillCredit peers) is fire-and-forget BackgroundTask with no watchdog.** [entities/bill/api/router.py:365-378,381-403](entities/bill/api/router.py). Crash post-finalize / pre-outbox = bill stuck `IsDraft=False`, no QBO/SharePoint/Excel, no recovery sweep. Per `project_one_file_per_tick.md`, this should be outbox-driven. Short-term: add a scheduler watchdog that detects `IsDraft=False AND no completion_result AND age > 5min` and surfaces them.
- [x] ~~**Double-click on Complete bypasses idempotency.**~~ [entities/bill/api/router.py:381-403](entities/bill/api/router.py) — guard check + BackgroundTask enqueue are not atomic; rapid second click enqueues a second task that re-enqueues all QBO/SharePoint/Excel outbox rows. Fix: re-check `is_draft` inside `_run_complete_bill()` itself before enqueueing side effects (return early if already complete).
- [x] ~~**Completion-result endpoints leak across tenants.**~~ [entities/bill/api/router.py:186-194](entities/bill/api/router.py) — gates on `Modules.BILLS` only, no `assert_can_access_bill()`. Same shape almost certainly on `/get/expense/{id}/completion-result`, `/get/invoice/...`, `/get/bill_credit/...`. Fix: fetch via service `read_by_public_id` (which goes through `assert_can_access_*`) before returning the result.
- [x] ~~**JWT validation missing `aud` + `iss` checks.**~~ [entities/auth/business/service.py:92-115](entities/auth/business/service.py). `jwt.decode` validates signature + exp + `token_type` only. Add `audience="build.one.api"` + `issuer="build.one"` to settings and to `jwt.encode`/`jwt.decode`.
- [x] ~~**`POST /admin/auth/set-credentials/{user_public_id}` missing `_require_csrf`.**~~ [entities/auth/api/router.py:188-214](entities/auth/api/router.py). Compare with `/auth/refresh` which does call `_require_csrf` when cookie auth is present. Cookie-authed admin visiting attacker site = CSRF'd password reset on any user.
- [x] ~~**No web logout endpoint**~~ — only `/mobile/auth/logout` exists. Web clients clear localStorage but can't revoke the refresh token server-side. Add `POST /api/v1/auth/logout` that revokes the refresh cookie + clears cookies, requires CSRF.
- [x] ~~**`shared/access.py::_check()` doesn't explicitly close the cursor** — under list-page scans (cursor-per-row), pyodbc cursor handles leak. Wrap in try/finally + `cursor.close()`.~~
- [x] ~~**MS mail draft 2-step PATCH has no rollback on PATCH failure.**~~ Fixed 2026-05-27. On PATCH failure, the orphan forward draft is now DELETEd before re-raising. Failure to delete is logged but doesn't mask the original error.
- [x] ~~**Excel 423 (Locked) treated as non-retryable in MS base client.**~~ [integrations/ms/base/client.py:853-869](integrations/ms/base/client.py). Concurrent Excel writes dead-letter immediately instead of waiting. Add `MsLockedError(MsGraphError, is_retryable=True)` and map 423 to it.
- [x] ~~**`decrypt_if_encrypted` masquerades ciphertext as plaintext on EncryptionError.**~~ Fixed 2026-05-27. New `_looks_like_fernet()` structural check (base64 + version byte `0x80` + min length). If decryption fails on something that looks like Fernet, raises `EncryptionError` with a logged `field_name` instead of silently passing ciphertext through. All 6 callers (MS + QBO auth repos + QBO client repo) now pass `field_name` for diagnostics. Plaintext passthrough preserved for non-Fernet values.

### Medium
- [ ] **SP NULL-overwrite anti-pattern.** Only `IsDraft` uses `CASE WHEN` guard. Every other UPDATE sproc unconditionally `SET`s — any optional column passed as `None` is silently NULLed in the DB. Sweep `entities/*/sql/` for `Update*.sql` files and add `CASE WHEN @Field IS NOT NULL THEN @Field ELSE [Field] END` per optional column.
- [x] ~~**`enrich_line_items()` silently drops unknown `source_type`.**~~ Fixed 2026-05-27. Added `_KNOWN_SOURCE_TYPES` set and `logger.warning()` when an unknown source_type is encountered.
- [x] ~~**`ExpenseLineItemAttachment` create silently returns existing on duplicate.**~~ Fixed 2026-05-27. Added logging + warning when duplicate create has mismatched `attachment_id` vs existing record.
- [ ] **AttachmentService blob delete failure doesn't block DB record delete** → orphan blobs in Azure (storage cost grows). Add an `OrphanedBlob` audit table + a cleanup job.
- [ ] **`useCompletionPolling` in React swallows all errors.** Move to API side: ensure completion-result endpoint returns clear 401/403/500 distinct from 404 ("not ready"). Web fix already in `build.one.web/TODO.md`.
- [ ] **QBO `BillLineItemBillLine` mapping skipped silently when `qbo_line.line_num` is NULL or unmapped.** [integrations/intuit/qbo/bill/connector/bill/business/service.py:406-417](integrations/intuit/qbo/bill/connector/bill/business/service.py). Orphan-row risk on QBO tax/total/shipping lines. Log warning + persist orphan reference to a reconciliation table.
- [ ] **Outbox `MAX_ATTEMPTS` enforced in Python only.** Add DB CHECK constraint on `[qbo].[Outbox]` + `[ms].[Outbox]` to fail closed if a future drainer regresses.
- [ ] **No rate limiting on `/auth/login` or `/admin/auth/set-credentials`.** Brute-force / credential-spam unthrottled. Add `slowapi` or similar.
- [x] ~~**Refresh token `REVOKED_GRACE_SECONDS = 60` is too generous.**~~ Fixed 2026-05-27. Lowered to 15s.
- [ ] **Permission cache 5-min TTL with best-effort invalidation = TOCTOU window between role revoke and next-tick flush.** Consider versioning the cache key (`permission_schema_version`) so invalidation is immediate.
- [x] ~~**Switch-company cache invalidation runs AFTER new tokens returned.**~~ Fixed 2026-05-27. Swapped order: invalidate first, then mint and return new tokens.
- [x] ~~**Scheduler `process_bill_folder` timer has no error handling around `_post()`.**~~ Fixed 2026-05-27 (commit `eadc741` in build.one.scheduler). Wrapped `_post()` in try/except inside the drain loop; transient failures break the loop cleanly instead of crashing the timer.
- [x] ~~**`recover_stuck_email_processing` + `auto_fail_stale` log-and-swallow on DB error.**~~ Fixed 2026-05-27. `auto_fail_stale` now re-raises after logging so the tick surfaces as failed in Function App logs.
- [x] ~~**DI `analyze_document_bytes()` has no max file-size check.**~~ Fixed 2026-05-27. Added 50 MB cap (`_MAX_DOCUMENT_BYTES`) with clear `DocumentIntelligenceError` on violation.
- [ ] **DI confidence threshold not enforced on hoisted KV pairs.** Agent prompt should be explicit; OR `_hoist_and_validate` should filter < 0.70 confidence.
- [x] ~~**Anthropic timeout is global request timeout, not per-chunk.**~~ Fixed 2026-05-27. Split into per-phase `httpx.Timeout(connect=10, read=self._timeout, write=30, pool=10)`.
- [ ] **AgentSession can be left in `Status='running'` forever** if the background task is cancelled mid-stream. [intelligence/loop/session_runner.py:71-82](intelligence/loop/session_runner.py). Add a scheduler tick that resets running sessions older than 30 min (mirror EmailMessage `recover_stuck`).
- [x] ~~**Email body content flows into LLM tool results unescaped.**~~ Fixed 2026-05-27. Added `[EXTERNAL EMAIL CONTENT START/END]` injection markers around `body_content` in `_read_email_message` tool results.
- [ ] **Hardcoded model IDs across every agent definition** (`claude-sonnet-4-6`, `claude-haiku-4-5-20251001`). Anthropic deprecation = simultaneous fleet outage. Move to config (env var `{agent}_MODEL_OVERRIDE`).

### Low
- [ ] Workflow event has no retention/TTL policy → unbounded growth. Define retention + add a cleanup job.
- [ ] PII redaction in workflow event payloads — workflow rows persist user input verbatim in JSON.
- [x] ~~`print()` debug statements in [core/workflow/api/process_engine.py:389,401](core/workflow/api/process_engine.py) — replace with `logger.debug`.~~
- [x] ~~Signup endpoint leaks username existence ("Username already exists" vs "Invalid registration code"). Use generic error.~~
- [x] ~~TimeEntry approve/reject route gates on `can_update`, should gate on `can_approve`.~~ Fixed 2026-05-27. Changed to `require_module_api(Modules.TIME_TRACKING, "can_approve")`. Verified existing RoleModule grants already have `can_approve` correctly set.
- [x] ~~`orchestrator.create_workflow()` returns a tuple but type hint says `Workflow`.~~ Fixed 2026-05-27. Return type hint corrected to `tuple[Workflow, bool]`.
- [x] ~~Missing entries in `SYNCHRONOUS_TASKS` registry fail at request time, not startup.~~ Fixed 2026-05-27. Added import-time `RuntimeError` check validating SYNCHRONOUS_TASKS entries exist in PROCESS_REGISTRY.
- [x] ~~`DRAIN_SECRET` timing-leak (missing vs wrong-secret takes different code paths). Always perform HMAC compare.~~

---

## iOS v0.1.1 (target: distribute to Field SMEs by Friday May 1, 2026)

- [ ] **(P0, blocks Austin) Replace closure-of-services with `Resettable` protocol.** Three closures in [AppEnvironment.swift](../build.one.ios/BuildOne/App/AppEnvironment.swift) (`companyService.onCompanySwitch` lines 174-198, `authService.onLogout` lines 210-233, `authService.onUserLogout` lines 234-252) each list ~16 weak service references and call `resetForLogout()` / `prepareForLogout()` on each one. Adding a new service requires updating all three closures — and if you miss one, stale data silently leaks across logout / company switch (this exact bug class would have caught the v0.1.0 CoreData state-bleed bug if it had existed). **Fix**: define `protocol Resettable: AnyObject { func resetForLogout(); func prepareForLogout() async }`, adopt on every service (they already have these methods), expose `private var resettableServices: [Resettable]` on `AppEnvironment`, replace the three closures with `services.forEach { $0.resetForLogout() }` (or async-for-loop with prepareForLogout). New service → adopt protocol (compiler enforces), add to array once. ~1 hr work. Eliminates a whole class of bug.
- [ ] **Crash reporter (Sentry / Firebase Crashlytics / roll-your-own `POST /api/v1/admin/crash-log`).** Today if a tester's app crashes at any point during AppEnvironment.init (CoreData corruption, Keychain failure, schema migration error) we have zero visibility. Every "the app didn't work" report from a field SME is a black box. Even a 100-line roll-your-own (NSSetUncaughtExceptionHandler + a POST endpoint) catches Swift fatalErrors and gives us a way to find out what's happening. Misses signals (SIGSEGV etc.) but good enough for v0.1.x beta — upgrade to Sentry/Crashlytics in v0.2+. Cost: low. Value: huge.
- [ ] **(P0) Carousel must render before `permissionsReady` gate.** Currently `RootView` in [BuildOneApp.swift](../build.one.ios/BuildOne/App/BuildOneApp.swift) blocks the onboarding carousel on `permissionsReady` becoming true. Any 403 or network failure during `loadCorePermissions()` leaves first-time users stuck on a black `PermissionsLoadingView` with a red toast — Austin Rogers hit this on 2026-04-30 launch when his Owner role lacked some startup module grants. Carousel itself needs no permissions to render (it's 4 slides about Location/Notifications/Face ID/Loading) and the LoadingSlide already calls `bootstrapForLoggedInUser()` which fires both `loadCorePermissions()` and `loadAccountData()` in parallel — by the time the user finishes the carousel, perms are loaded. **Fix**: reorder the conditional so `!hasCompletedOnboarding → OnboardingView` is checked BEFORE `!permissionsReady → PermissionsLoadingView`. Returning users (post-onboarding) still get the brief loading screen — that path is unchanged. Consider time-boxing the gate (after ~5s, render TabView anyway and let `hasEntityAccess`/`NoModulesView` handle the failure state) — defer if v0.1.1 scope is tight.
- [ ] **TimeEntry CoreData scoping fix.** `TimeEntryService.loadFromCache()` calls `CDTimeEntry.fetchAll()` with no `userId` filter, so on user-switch the prior user's logs flash on the Time tab before the API response replaces in-memory state. CDTimeEntry already has a `userId` Int64 attribute. Fix: add `NSPredicate(format: "userId == %@", currentUserId)` to all `fetchAll()` reads in TimeEntryService; tag every `upsert(...)` with `cd.userId = tokenStore.userId` if not already set. CDTimeLog filters via parent TimeEntry (no separate userId needed). CDTimeEntryStatus already has userId — same filter pattern. **Do NOT delete CoreData rows on logout** — would destroy field workers' offline-queued edits on session expiry. Leave `resetForLogout()` clearing in-memory `@Observable` state only. After fix, bump `MARKETING_VERSION` 0.1.0 → 0.1.1 + `CURRENT_PROJECT_VERSION` 1 → 2 in `BuildOne.xcodeproj/project.pbxproj`, archive + validate + upload + submit Beta App Review.
- [ ] **Module/Role/Project cache scoping (deferred from v0.1.1, plan for v0.1.2 or later).** Same state-bleed pattern in `ModuleService` / `RoleService` / `ProjectService` cache pre-populate, but the user-scoped API endpoints wired in iOS commit `655a69c` correctly replace in-memory state on response. Visible flash of stale data is brief and not a security issue. Full fix requires CoreData model migrations (CDModule / CDRole / CDProject have no `userId` attribute today) — defer until either (a) field reports complain about the flash, or (b) we're already touching the model for another reason.
- [ ] **Pre-release multi-user smoke test in iOS release checklist.** Sign in as User A → populate state (clock in, edit) → sign out → sign in as User B → verify clean state. Single-developer single-device testing missed the v0.1.0 state-bleed bug. Codified in memory `feedback_ios_multi_user_testing.md`.
- [ ] **Carousel must show before permission API loads (UX bug from 2026-04-30 launch).** Current `RootView` in [BuildOneApp.swift](../build.one.ios/BuildOne/App/BuildOneApp.swift) gates the carousel on `permissionsReady` — if any startup API call 403s during `loadCorePermissions()`, the user sees a red toast over a "Loading…" spinner instead of the onboarding carousel. Austin Rogers hit this on first login (Owner role lacked USERS + VENDORS module access; iOS calls `GET /user/{public_id}` and `GET /contacts/user/{user_id}` at startup, both module-gated). **Fix**: reorder the conditional to `if !hasCompletedOnboarding → OnboardingView` first, then `if !permissionsReady → PermissionsLoadingView`, then `if hasEntityAccess → TabView`. Carousel fires immediately on first login while permissions load in parallel. By the time the user finishes Location/Notifications/Face ID slides, permissions are usually loaded; any 403 then surfaces on the Time tab where the user has actionable next steps, not a black "Loading" screen.
- [ ] **Self-read endpoints should be auth-only, not module-gated.** `GET /api/v1/get/user/{public_id}` is gated on Modules.USERS and `GET /api/v1/get/contacts/user/{user_id}` on Modules.VENDORS — wrong gates for self-data. Field workers shouldn't need broad USERS/VENDORS read just to load their own profile + contacts at app startup. **Workaround applied 2026-04-30**: granted Owner + Project Manager roles read-only access to USERS and VENDORS RoleModule rows. Cleaner long-term: relax these endpoints to auth-only when the requested `user_id` matches the caller's `current_user["user_id"]`, fall back to module check only when reading another user's data. Same pattern for the Contact-by-user endpoint. After the fix lands, drop the workaround grants from Owner / PM (or leave as harmless additive read).

## Cross-repo: Idempotent CREATE pattern via client UUID (v0.2, spans API + iOS)

Filed 2026-05-20 after the iOS-sync duplicate-TimeLog incident on 2026-05-19 was patched in iOS build 4 (commit pending). Build 4 closed the immediate field-worker pain via per-entity `hasAttemptedCreate` flags + natural-key pre-check on TimeEntry/TimeLog, plus the DB `UX_TimeLog_TimeEntry_ClockIn` unique index. That works but is fragile and per-entity bespoke. The proper fix is a universal client-UUID idempotency pattern.

**Problem.** Today an offline-or-poor-coverage iOS client that POSTs a CREATE can lose the response (network drop, app backgrounded mid-await, OS killing a background task). The local CD row still carries `hasPendingCreate=true`, so the next drain retries — and if the original request actually succeeded server-side, the retry creates a duplicate row. Real incident: 3 field workers got duplicate TimeLog rows on 2026-05-19. Cleanup required manual SQL across 5 users.

**Build 4 mitigation (shipped, narrow).** `CDTimeLog.hasAttemptedCreate` + `CDTimeEntry.hasAttemptedCreate` boolean flags persist across app launches. On retry, iOS pre-queries the server's existing rows for the entry/date and claims a matching one (by `(timeEntryId, clockIn, logType)` for TimeLog, `(userId, workDate)` for TimeEntry) instead of re-POSTing. Plus duplicate-key error recovery in the catch block. Plus the DB unique index on `(TimeEntryId, ClockIn)` as defense-in-depth.

**Limits of the build-4 approach:**
- **Byte-exact natural-key matching is fragile.** Any clockIn timestamp serialization drift (timezone `Z` vs `+00:00`, milliseconds present/absent) → pre-check misses → re-POST → unique-index rejection → claim path runs, but it's a brittle round-trip.
- **Only TimeLog + TimeEntry are protected.** Bill, BillLineItem, Project, Role, Module, Company, Organization, Contact, BillLineItemAttachment all have the same structural race — lower volume but same bug shape. iOS will eventually need to grow more sync surface area for field workers.
- **Per-entity matching logic.** Every new entity needs its own natural-key shape in iOS + pre-check fetch wired in.

**Proposed deeper fix — client-UUID idempotency:**

1. **API: Add `ClientId UNIQUEIDENTIFIER NOT NULL` column to every entity with user-driven CREATE.** Lightweight migration: `ALTER TABLE … ADD ClientId UNIQUEIDENTIFIER NOT NULL DEFAULT NEWID()` then a one-time backfill, then drop the default once iOS is sending it. Tables: TimeEntry, TimeLog, TimeEntryStatus (the manual-submit case), Bill, BillLineItem, BillCredit, BillCreditLineItem, Expense, ExpenseLineItem, Invoice, InvoiceLineItem, ContractLabor, ContractLaborLineItem, Project, Customer, Vendor, Contact, Role, RoleModule, Module, UserModule, UserProject, UserRole, Organization, Company, OrganizationCompany, UserOrganization, UserCompany, Attachment, BillLineItemAttachment.

2. **API: Update every CREATE sproc to be MERGE-shaped on ClientId.**
   ```sql
   IF EXISTS (SELECT 1 FROM dbo.X WHERE ClientId = @ClientId)
       SELECT existing row by ClientId
   ELSE
       INSERT … VALUES (@ClientId, …);
       SELECT inserted row;
   ```
   Plus unique index per table on ClientId. Retried CREATEs with the same ClientId are deterministic no-ops.

3. **API: Accept `client_id: UUID` on every CREATE endpoint body.** Pydantic schema; pass through to service → repo → sproc.

4. **iOS: Send `cd.publicId` (already a generated UUID) as `client_id` in every CREATE payload.** The local publicId becomes the canonical idempotency key. The server's response still returns its own `public_id` for downstream FK relationships — iOS just upserts as today.

5. **iOS: Drop the `hasAttemptedCreate` flags + per-entity natural-key matching once #4 is in.** Simpler, no bespoke code per entity.

**Cost estimate.** ~1 day API (column + sproc + endpoint payload threading on ~25 tables, mostly mechanical), ~half day iOS (CREATE payload threading + drop the bespoke pre-check logic), ~half day testing + migration. Worth doing in one focused project, not piecemeal — partial coverage doesn't help because the bug is per-entity.

**Recommended timing.** Before second customer onboards, or before substantially expanding iOS field-worker mutations (e.g., letting them create Expenses from the field, edit Projects, etc.). Not blocking, not urgent post-build-4. Note: keep the build-4 narrow protections in place until this lands — they're cheap and provide belt + suspenders.

**Pair entry in iOS TODO.md** (same section header) — keep them in sync if scope or timing changes.

## iOS service architecture review (v0.2.0+)

- [ ] **CoreData reads should happen on background contexts, not the main actor.** Paired with the lazy-cache refactor below. Today every service's cache-populate happens on `@MainActor` because `AppEnvironment` is `@MainActor @Observable` and every service init runs there. Cumulative 30 services × 30ms fetch = ~900ms of main-thread blocking before LoginView renders. Refactor: use `NSManagedObjectContext.perform { ... }` on a background context for the fetch, hop back to main actor with the results for UI binding. Do this at the same time as the lazy-cache refactor below — they touch the same code.
- [ ] **Move cache pre-populate out of service `init` to first-access.** Today every service in iOS `AppEnvironment.init` does a synchronous `CDX.fetchAll(in: coreData.viewContext)` to pre-populate its in-memory state. With 16 services on `@MainActor` this is fine (~100-400ms cold launch), but the cost is linear in service count + cache size. At v0.2 with 30+ services and bigger caches, this crosses the user-perceptible 1s threshold and starts blocking the LoginView from appearing. **Fix**: convert each service's init-time `fetchAll` into a `private func ensureCachePopulated()` called lazily at the top of any `loadX()` / computed-property read. Init becomes ~zero cost; cache pre-populate happens when the relevant view actually appears. Field workers who never touch Bills/Expenses/Invoices stop paying for those caches every cold launch. ~30 min per service when already touching the file. Zero behavior change for end users — they only pay for caches they navigate to.
- [ ] **Don't introduce a DI framework yet.** For 30 services, plain init injection in `AppEnvironment` is still cleaner than Swinject/Factory overhead. Revisit at ~100 services or when cold-launch construction exceeds 2s after the lazy-cache fix above.
- [ ] **Defer feature-container split until forced.** Splitting `AppEnvironment` into `CoreEnvironment` (always) + per-tab `FeatureContainer` (instantiated on tab open) is the right shape if memory ever becomes a constraint or service count reaches ~50. Until then, the eager-init pattern is right — predictable, view bodies don't nil-check, closure capture works at init time. Don't refactor pre-emptively.
- [ ] **Service init failure recovery (CoreData corruption).** Today if `CoreDataStack.init` fails to load the persistent store — corrupted file, schema-migration failure, disk error, post-iCloud-restore mismatch — the iOS app crashes inside `AppEnvironment.init` before LoginView renders. User has no in-app recovery path; only fix is delete + reinstall, which **destroys every unsynced offline edit**. For a field-worker app that's offline-first, that's the worst possible outcome. **Fix**: wrap `loadPersistentStores` in a recovery path — on failure, log the error (via a crash reporter, see next item), delete the corrupted store file, recreate fresh. User loses local cache for that session but the app launches. Pair with telemetry so we know when it's happening in the wild. Consider an "in-memory CoreData fallback" so writes stay usable for the current session even if persistence is broken. **Add a crash reporter** (Sentry / Firebase Crashlytics / a simple `POST /api/v1/admin/crash-log` endpoint) — today we have no visibility into launch crashes on testers' phones.
- [ ] **Company-switch reset must sync offline edits first.** `AppEnvironment.companyService.onCompanySwitch` (lines 174-198) calls `resetForLogout()` on every service — clears in-memory state but doesn't sync pending edits before context flips. User-initiated logout (`authService.onUserLogout`) correctly uses `prepareForLogout()` which syncs first; company switch should be at least as careful. **Scenario at Phase 5+** when CompanyId scoping is API-enforced: user in Company A clocks in offline → switches to Company B (JWT cid=B) → network returns → `observeConnectivity` fires `syncPendingUpdates` → pending clock-in POSTs with cid=B → API either rejects (edit stuck forever in CoreData) or accepts and writes the row into the wrong tenant (silent corruption). Neither outcome is good. **Fix two layers**: (1) call `prepareForLogout()` not `resetForLogout()` on company switch — same chain `onUserLogout` uses, syncs first if connected; (2) tag every pending CoreData row with a `pendingCompanyId: Int64` set at the moment of the edit, so the sync path can defer pending rows until the right Company is active again. Lands alongside Phase 5 sproc-level CompanyId enforcement on the API. Today: not a bug — only one Company exists. The moment a second Company arrives, becomes a real risk.

## RBAC architectural review (v0.2.0+)

- [ ] **Decide whether to keep role-based module grants.** User direction during the v0.1.0 launch session: "no blanket access for anyone — manually select Projects, Modules, CRUD/Review/Approval per user." Current model: `User → UserRole → Role → RoleModule → Module` with role granting all its modules to every user holding the role. Alternative: roles become labels, actual grants live on `UserModule` per-user-per-module with explicit CRUD/Review/Approval flags. **Touches**: API repos/services for RBAC, web UI module assignment forms, iOS auth flow, DB rows, the 4 user-scoped sprocs added 2026-04-29 (`ReadProjectsByUserId`, `ReadRolesByUserId`, `ReadModulesByUserId`, `ReadUserRolesByUserId`). Multi-day project. Sketch a migration path before scoping. UserModule additive-grant pattern (read-only layer on top of RoleModule, shipped 2026-04-29) is the partial step in this direction.

## Access Control Rebuild — Phase 5b + follow-ups (post-2026-05-06)

All 5 phases of the rebuild shipped 2026-04-30 → 2026-05-06. Carry-over items:

- [x] ~~**`ReadProjectsByUserId` doesn't honor `IsSystemAdmin` bypass**~~ (filed 2026-05-27). Caught while testing the new iOS time-tracking implementation — Christopher (`IsSystemAdmin=1`) saw only the projects he had explicit `UserProject` rows for, despite the bypass primitive being live everywhere else (Phase 3 TimeEntry, Gap 1 Bill/Expense/Invoice/ContractLabor service-layer access checks all bypass for system admins). The sproc at `entities/project/sql/dbo.project.sql` (or wherever the user-scoped read lives) is a hard `INNER JOIN dbo.UserProject` with no admin escape. **Workaround applied 2026-05-27**: backfilled `UserProject` rows so every `IsSystemAdmin=1` user (Christopher + Claude Agent) is on all 148 Projects — 159 new rows inserted. This needs re-running every time a new Project is created. **Permanent fix**: patch `ReadProjectsByUserId` to accept `@ActorIsSystemAdmin BIT` and skip the join when set (same shape as Phase 3 TimeEntry's `@ActorIsSystemAdmin` param). Repo-side: thread `current_is_system_admin` ContextVar through `entities/project/persistence/repo.py`. Drop the workaround backfill rows for `Claude Agent` after the sproc fix lands; Christopher's are harmless. Alternative: trigger on `dbo.Project` INSERT that auto-creates `UserProject` rows for every `IsSystemAdmin=1` user — simpler in SQL but adds a sproc footgun for anyone bulk-inserting projects.
- [ ] **Phase 5b — Multi-tenant enforcement** (deferred until 2nd Company arrives or preemptive enforcement is wanted). The 24 tenant-data tables now have `CompanyId NOT NULL` with `DEFAULT (1)` so existing sprocs/services keep working unchanged. To deliver actual isolation: (a) update Create sprocs to accept explicit `@CompanyId BIGINT` param; (b) thread CompanyId through services from `current_company_id` ContextVar; (c) drop the `DEFAULT (1)` constraints once explicit values are required; (d) update read sprocs with `@ActorIsSystemAdmin BIT` + filter clause (same pattern as Phase 3's TimeEntry). Plus reference-entity scoping decisions (Vendor/Customer/SubCostCode etc. — per-Company vs per-Organization for shared catalogs).
- [ ] **Phase 4c — React + iOS Company picker UI**. API endpoints already shipped (`/auth/me` `companies[]`, `POST /auth/switch-company`, `POST /mobile/auth/switch-company`). Land in `build.one.web` (header dropdown reading `me.companies[]`) and `build.one.ios` (Swift element using mobile variant). Today system admins can switch via direct API calls only.
- [ ] **Drop legacy fallback in `_enrich_payload_with_authz`.** Phase 2 set `JWT_CID_GRACE_DAYS=0` (default) — tokens missing `uid`/`cid` claims no longer fall back to a DB lookup. After ~1 week of stable prod operation, remove the `legacy_fallback_allowed` branch entirely. Keeps the auth path simpler.
- [x] ~~**Drop the inert legacy `Admin` Role row.**~~ Resolved 2026-05-07 by renaming instead of dropping. The legacy `Admin` row (Role.Id=1) carried 23 fully-populated RoleModule grants (full CRUD on every module) — exactly the shape Phase 2's seeded `Tenant Admin` (Role.Id=15) was meant to have but never got. Cleaner than drop + re-seed: deleted the empty seeded Tenant Admin row and renamed Role.Id=1 from `Admin` → `Tenant Admin`. Preserves the 23 grants and the 2 UserRole assignments (Christopher + Apple Reviewer; harmlessly redundant since they bypass via `IsSystemAdmin=1`). Migration: `scripts/migrations/rename_legacy_admin_role_to_tenant_admin.sql`.
- [ ] **Phase 1b — Drop `OrganizationCompany` + `UserOrganization` tables/entities.** Was deferred from Phase 1 because React `UserProfile.tsx` still reads them (Organizations section). Schedule once the frontend section is removed or moved to derive from `UserCompany → Company.OrganizationId` (the 1:N model is in place since Phase 0).
- [ ] **Assign RoleModule grants for the 6 non-admin human roles seeded in Phase 2.** Controller, AP Specialist, AR Specialist, Reviewer, Time Clerk, Auditor were seeded as Role rows with no module grants. Configure per-role grants via React `/role/:id` Modules tab when those roles get assigned to real users. (Tenant Admin has 23 full-CRUD grants from the legacy Admin rename, 2026-05-07. Project Manager already has grants from the review-notification work.)

## Multi-user RBAC review — Gap 1 / 2 / 3 follow-ups (post-2026-05-06)

Three gaps from the multi-user audit shipped 2026-05-06 (commits 64a2a05, 361d248, e2d3afb). Carry-over items:

- [x] ~~**Gap 1 tightening pass — direct-URL scoping on by-id / by-public-id / by-vendor-id / by-other-keys lookups.**~~ Shipped 2026-05-07. Service-layer post-fetch check using existing `dbo.UserCanAccess*` UDFs via `shared/access.py`. Gates `BillService.read_by_*`, `BillCreditService.read_by_*`, `ExpenseService.read_by_*`, `InvoiceService.read_by_*`, `ContractLaborService.read_by_*` plus the four line-item child services. Includes Bill/BillCredit/Expense alt-key lookups (read_by_*_number_and_vendor_*) and ContractLabor list-returning methods (read_by_vendor / billing_period / status / import_batch — filtered post-fetch in Python). Mutations inherit gating via `read_by_public_id`. `EntityNotAccessibleError` → HTTP 404 (not 403). Smoke-tested as Cassidy (uid=18) — out-of-set Bill 17 → 404, in-set Bill 2910 → 200, admin bypasses, null-actor bypasses.
- [x] ~~**Gap 1 follow-on — outbox workers + CLI sync scripts must declare system intent.**~~ Shipped 2026-05-14 (workers) + 2026-05-15 (CLI). After the legacy `current_user_id is None → bypass` branch was removed, two non-HTTP entry points started raising `EntityNotAccessibleError` against guarded reads: (1) outbox worker `_process` methods drained by the in-process scheduler / REPL / any non-HTTP caller, (2) CLI sync scripts (`scripts/sync_qbo_*.py`) when invoked from terminal. Fix: `QboOutboxWorker._process` and `MsOutboxWorker._process` now wrap their body in `set_authz_context(is_system_admin=True)` (save → set → restore), and all 10 sync scripts call `assert_cli_system_admin()` (new helper in `scripts/sync_helper.py`) under `if __name__ == "__main__":`. 7 dead-lettered Bill outbox rows replayed successfully post-deploy. See `feedback_outbox_authz_boundary.md`.
- [x] ~~**Gap 2 service-layer `CreatedByUserId` threading.**~~ Shipped 2026-05-07 in 4 phases:
  - **Gap2-T** (`commit a80cff3`) — TimeEntry, TimeLog. Prioritized for iOS time-tracking relaunch.
  - **Gap2-Core** (`commit 8bb79c4`) — Project, Bill, BillCredit, Expense, Invoice, ContractLabor + 5 line-item children. Closed Gap 1 empty-bill edge case as bonus (`gap2_user_can_access_creator_clause.sql`).
  - **Gap2-Adjacent** (`commit f059ddc`) — Attachment + 3 line-item-attachments, EmailMessage/Attachment (upsert), Review, ReviewStatus, BillFolderRun/Item.
  - **Gap2-Reference** (`commit 465e9bd`) — Vendor, Customer, SubCostCode, CostCode, PaymentTerm, ProjectAddress.
  - Total: 30 entities, 29 Create + Upsert sprocs. Pattern: `@CreatedByUserId BIGINT = NULL` + `COALESCE(@CreatedByUserId, 17)` preserving DEFAULT-trick fallback for scheduler / system context. Migrations: `scripts/migrations/gap2_{core,adjacent,reference}_threading.sql`. Skipped: `ReviewEntry` (decommissioned, no Create sproc).
- [ ] **Rebake stale `entities/<x>/sql/<entity>.sql` base files (post-Gap 2).** Several entity base sproc files are now stale snapshots that don't reflect later migrations (e.g. `entities/time_entry/sql/dbo.time_entry.sql` doesn't carry Phase 3's actor params on the read sprocs). Re-running a stale base file rolls back its migrations — bit me during Phase Gap2-T. Cleanup: for each entity with migrations, consolidate base + migrations into a fresh base file representing the current sproc shape, then archive the migrations under `entities/<x>/sql/migrations/_applied/`. Tracking issue, not urgent — migrations stay safely additive in the meantime.
- [x] ~~**Gap 1 empty-bill edge case** — bills with no `BillLineItem` rows AND not created by the actor are invisible to non-admins.~~ Closed 2026-05-07 alongside Phase Gap2-Core. Updated `dbo.UserCanAccess{Bill, BillCredit, Expense, Project}` UDFs to add a "creator can access their own row" clause matching the Gap 1 v3 list-path filter shape. Empty drafts created by a user remain visible to that user. Migration: `scripts/migrations/gap2_user_can_access_creator_clause.sql`.
- [ ] **Optional: 2FA / MFA.** Out-of-scope per Q3.3 sign-off — separate multi-day project. Revisit when a customer asks or a compliance need surfaces. TOTP via authenticator app is the reasonable starting point; SMS / hardware key are bigger lifts.
- [ ] **React UI for managing UserProject memberships (admin-facing).** With Gap 1 enforcement live, non-admin users see only Projects + transactional rows on projects they have a UserProject row for. Today admins grant access via SQL. The User Profile page already has a "Projects" section that READS UserProject rows — it needs to gain CREATE / DELETE controls (probably a multi-select against the Project list). Without this, every "I can't see X" report from a user requires a SQL session. Existing `entities/user_project/api/router.py` POST/DELETE endpoints are ready; this is React work in `build.one.web/src/pages/users/UserProfile.tsx`.
- [ ] **UserProject PM/Owner role assignments are wrong at scale — Tanner Baker is PM on 129 projects.** Surfaced 2026-05-19 during manual backlog walk on Project 35 (FP - 4005 Franklin Pike). User confirmed Tanner is NOT the correct PM for FP — the correct PMs are Cassidy Andrews + Zach Rogers. After fixing Project 35 (one-off), a check showed Tanner is currently `UserProject.RoleId = 3 (Project Manager)` on **129 of ~132 active projects** — almost certainly a default/seed/backfill that was never refined per-project. **Impact**: every review-submit email notification for those projects routes to the wrong human as TO (`ResolveReviewRecipientsByBillId` walks Bill → BillLineItem → Project → UserProject.RoleId = PM); reviewer-reply automation also authorizes Tanner instead of the correct PM (`apply_reviewer_decision` checks UserProject.RoleId against sender). Until cleaned up, the entire notification + reviewer-reply chain is mis-routed for most projects. **Recommended fix**: a one-time audit pass with the user to map each Project → correct PM(s), driven from an admin React view that displays current PM/Owner per project alongside an editable picker. Pair with the "React UI for managing UserProject memberships" item above. Reference fix already applied for Project 35: delete `UserProject` row 2518 (Tanner), insert `(UserId=18 Cassidy, ProjectId=35, RoleId=3)`, insert `(UserId=19 Zach, ProjectId=35, RoleId=3)`.
- [ ] **Audit the 8 unroled humans on FP - 4005 Franklin Pike (and likely other projects).** Surfaced same session. Project 35 has UserProject rows for Brayan Marcia Salina, Elmer/Emilson/Selvin Cordova, Michael Jacobson, Parker Hazen, Randy Rogers, Wilmer Diaz — all with `RoleId = NULL`. RoleId-NULL means access (for time tracking on this project) but no review responsibility. Could be correct (crew / contract labor) or could be noise from the same backfill that gave Tanner PM everywhere. Audit alongside the Tanner-PM cleanup: for each Project, confirm whether each currently-linked human actually works there. If not, delete the UserProject row.

## iOS Profile redesign — battle-test follow-ups (2026-05-11)

Items audited and deferred from the pre-ship battle test. Six HIGHs + four MEDIUMs were fixed in the same session; the entries below are the ones we deliberately punted because they're rare, recoverable, or design-call.

- [ ] **M2 — Concurrent token-write race between `changePassword` + `AuthenticatedSession.refresh`.** Both write to `tokenStore.accessToken/refreshToken` without coordination. Realistic conditions: user is mid-modal typing while a background refresh fires AND the responses arrive in inverted order. Worst case = one extra forced re-login. Proper fix: a shared write lock between `AuthService.changePassword` and `AuthenticatedSession.refresh`, OR funnel both through a single token-write coordinator actor. File: `build.one.ios/BuildOne/Services/Auth/AuthService.swift::changePassword`.
- [ ] **M3 — Fast-nav race might surface stale `initialValue` in `TextFieldEditScreen`.** `.onDisappear` fires-and-forgets `Task { await onCommit }`. If the user pops + re-pushes the same field row before the optimistic local write lands on the MainActor queue, the new screen reads stale state. Self-correcting on next refresh. Proper fix: maintain a per-field "pending write" map in the VM so subsequent reads see in-flight values. File: `build.one.ios/BuildOne/Features/Profile/TextFieldEditScreen.swift`.
- [ ] **M5 — `TextFieldEditScreen` has no Cancel.** Chevron-back always commits the typed value (after validation in the caller). Per the Profile spec ("iOS Settings-style: tap row → push → edit inline → back auto-saves"), but iOS convention often pairs that with an explicit Cancel that pops without committing. Design call. If we add Cancel: ToolbarItem(.cancellationAction) that sets `value = initialValue` and then dismisses.
- [ ] **M7 — Theme + biometric-enabled `UserDefaults` keys persist across logout.** `build.one.theme.override` and `build.one.biometric.enabled` are device-scoped. User A picks Dark + enables Face ID → User B logs in fresh and inherits both. Theme is arguably correct as device-preference; biometric-enabled flag is more debatable (the underlying credential is `biometryCurrentSet`-bound so the security boundary is fine — it's the *visual indicator* that mis-states reality for User B's first peek at Profile → Security). Pre-existing pattern, not introduced by the Profile redesign. Fix would be either scoping the keys by userId or clearing them in `resetForLogout()`.
- [ ] **L2 — Verify `.preferredColorScheme` propagation to the iOS status bar** in the simulator across all three Appearance settings. Likely fine — SwiftUI's `.preferredColorScheme` is documented to drive `UIStatusBarStyle` — but worth eyeballing under each Theme override.

## iOS Profile Sessions list (deferred from 2026-05-11 Profile redesign)

The Profile redesign's Security screen draws a "Sessions" section (`This iPhone · Active now · San Francisco` + `Web · Chrome · 2 hours ago · Oakland`) per `design/HANDOFF-2026-05-11/.../screenshots/05-security.png`. **Shipped today as a single read-only "This iPhone" row** because there's no backend support for listing or revoking individual sessions. Picking this up properly requires:

- [ ] **Schema: capture device metadata on refresh tokens.** Add `DeviceLabel NVARCHAR(64) NULL`, `UserAgent NVARCHAR(256) NULL`, `LastSeenAt DATETIME2 NULL`, `LastSeenIP VARCHAR(45) NULL` to the refresh-token table. Migration via `scripts/migrations/sessions_metadata.sql` (additive, no DEFAULT needed since columns are nullable).
- [ ] **Issuance + refresh paths populate metadata.** `mobile/auth/login` + `auth/login` populate these on token issuance (label derived from `User-Agent` header — "iPhone" / "Web · Chrome" / etc.; IP from request). Refresh path bumps `LastSeenAt` + `LastSeenIP` so the "Last seen" timestamp stays accurate without per-request writes elsewhere.
- [ ] **New endpoint: `GET /api/v1/mobile/auth/sessions`** — returns `[{public_id, label, last_seen_at, last_seen_ip, is_current}]` for the caller's Auth row. `is_current` derived by matching the caller's refresh-token JTI.
- [ ] **New endpoint: `POST /api/v1/mobile/auth/sessions/{public_id}/revoke`** — revokes one session by ID. Existing `RevokeAuthRefreshTokenByPublicId` sproc or equivalent; reuse the per-token revoke path.
- [ ] **iOS: `SessionService` + `SessionEndpoints`** — list + revoke. Replace the single-row "This iPhone" placeholder in `Features/Profile/SecurityScreen.swift` with a real list + per-row revoke action (destructive confirm alert).
- [ ] **Optional v2: GeoIP labels.** Free MaxMind GeoLite2 DB gives "San Francisco" / "Oakland" style labels from the `LastSeenIP`. Adds a deploy artifact (~50MB) + monthly DB refresh. Ship the feature without it first — "Active now" / "2 hours ago" without location is still useful. Revisit if the visual gap matters in field feedback.

The existing `POST /api/v1/mobile/auth/logout-all-devices` (added 2026-05-11) covers the bulk-revoke case from the Sign out of all devices row — sessions UI is incremental on top of that.

## Dev/prod backend split (revisit before second customer)

- [ ] **Stand up a dev backend.** Currently web + iOS + API all share the prod Azure App Service + SQL DB. Apple-reviewer test data, schema changes, and breaking API changes all hit prod cold. Stand up `buildone-dev` (App Service + SQL) + build-time URL switching in iOS (Debug → dev URL, Release → prod URL); web is already env-driven so cheap. Cost ~$50-100/mo. **When**: just before a second customer signs on (currently single-tenant, mostly Chris's data). Test-org-in-prod pattern + additive-only schema discipline covers the gap until then.



## Email-agent + bill_specialist follow-ups (post-2026-05-06 reshape)

- [ ] **P0 — `EmailMessage` time-inversion + cross-vendor `EmailAttachment` contamination.** Surfaced 2026-05-26 processing EmailMessage 759 (`info@ampinc.net`, AMP invoice). EA 542 (`IN132AAC.pdf`) is a Walker Lumber phone-transaction invoice (DOC# 804627/1, ship-to 4406 Harding Pl) yet attached to the AMP email. **Deeper investigation revealed two coupled bugs**:

    **(A) `EmailMessage.CreatedDatetime < EmailMessage.ReceivedDatetime`** affects **162 emails** across the active 2-week window (2026-05-12 → 2026-05-26). Newest examples (today 2026-05-26) show ~5-6 hour inversions; older ones show 15-20 hour inversions. EmailMessage rows are being INSERTED into the DB hours-to-a-day BEFORE the actual emails are "received" per Graph's clock. The pattern is ongoing — not a one-time backfill. UpsertEmailMessage sproc sets `CreatedDatetime = SYSUTCDATETIME()` at INSERT and stores Graph's `receivedDateTime` verbatim in `ReceivedDatetime`. Only one Python caller of UpsertEmailMessage (`EmailMessageRepository.upsert`), so no rogue script. Suspect: either clock-skew between the polling Function App's machine and Graph, OR the poller is pre-fetching emails Graph has visibility to but hasn't fully "delivered" (e.g., scheduled delivery, shared-mailbox quirks).

    **(B) Walker Lumber attachments landing on wrong emails.** When the EmailMessage row is pre-created at time T (with future ReceivedDatetime T+15h), an EmailAttachment row is created ~1 sec later linking a Walker Lumber phone-transaction PDF to that pre-created EmailMessage. Then the real email (AMP, mobmatnash, HomeDepot, etc.) arrives at T+15h, the poller matches on GraphMessageId, and UPDATES the existing row — leaving the wrong Walker Lumber attachment in place.

    **Per-EA verification** (decoded the 4 IN132AAC.pdf phantoms by reading their blob content): each is a DIFFERENT real Walker Lumber phone-transaction PDF — DOC# 804627/1, 207445/1, 207443/1, 804628/1 — all dated 2026-05-12 morning, from clerks RZ/RB at Walker. Different SIZES (12,279 / 12,587 / 12,214 / 11,879 bytes). They share the filename `IN132AAC.pdf` because Walker's email-export apparently re-uses filenames per daily batch. **The 4 corresponding Walker Lumber emails are NOT in our DB** — only Walker email in window is Email 684 (Inv 206126) at 12:43 with its own `IN132AAA.pdf` attachment. The 4 Walker phone-transactions exist in our system only as orphan attachments on wrong-vendor emails.

    **ROOT CAUSE (Phase 1 investigation, 2026-05-27):** MS Graph **reuses `GraphMessageId` values in the `invoice@rogersbuild.com` shared mailbox** when emails are deleted-then-restored (soft-delete + later undelete, or retention-policy churn). Our `UpsertEmailMessage` MERGE is keyed only on `GraphMessageId` (`MERGE ... ON target.GraphMessageId = source.GraphMessageId`), so when a recycled ID arrives via a new email, the MERGE matches the previously-stored row and overwrites every metadata field (FromAddress, Subject, ReceivedDatetime, InternetMessageId, body, etc.) — but the `CreatedDatetime` column is INSERT-only, so it preserves the original insertion time. That creates the `CreatedDatetime < ReceivedDatetime` time-inversion. The EmailAttachment rows attached to the row's previous identity stay attached, becoming phantoms tied to the now-overwritten metadata.

    **Direct evidence** (controlled experiment 2026-05-27 via `mail_client.get_message`):
      - Emsg 759: DB has `InternetMessageId=<200C3045...@AMPTS>`, FromAddress=info@ampinc.net. Graph at the stored GraphMessageId returns `InternetMessageId=<...@mail1.triadinet.net>`, FromAddress=laura@walkerlumber.com, receivedDateTime=2026-05-12T20:05:03Z. Different RFC 5322 ids from different mail servers = different physical emails sharing one GraphMessageId.
      - Same pattern on Emsg 760, 762, 763.
      - Same pattern on TODAY's emails (Emsg 1349, 1348, 1347, 1346, 1345 — all show `internetMessageId` mismatch between DB and Graph for the same `GraphMessageId`). The bug is **ongoing**, not historical.

    **Why this affects `invoice@rogersbuild.com` specifically**: shared mailbox with aggressive cleanup (Sweep rules, retention policies, manual triage). Each delete-restore cycle frees the GraphMessageId for reuse. A personal mailbox without that churn wouldn't hit this rate of recycling.

    **Phase 3 fix (proposed):**
      1. **Switch `UpsertEmailMessage` MERGE key from `GraphMessageId` to `InternetMessageId`** (RFC 5322 globally-unique, immutable). Add unique index on `EmailMessage.InternetMessageId`.
      2. Treat `GraphMessageId` as a *mutable secondary field* that can change when Graph recycles ids. Update it on every successful poll-match.
      3. Defense-in-depth: detect identity drift — if upsert receives `(InternetMessageId=A, GraphMessageId=X)` but DB has `(InternetMessageId=B, GraphMessageId=X)`, log + INSERT a new row instead of updating. This is the path for legitimate ID recycling.
      4. Mirror the same logic on `UpsertEmailAttachment`: dedup keyed on `(parent InternetMessageId, file content hash)` rather than `(EmailMessageId, GraphAttachmentId)` — keeps EA linkage content-stable across parent overwrites.

    **Phase 2 reconciliation plan:**
      - For each of the 162 time-inverted EmailMessages, query Graph at the stored GraphMessageId. If the returned `internetMessageId` differs from our DB's stored `InternetMessageId` → the row was overwritten. Mark the existing row's metadata as **belonging to the OVERWRITER email** (the second arrival), and re-poll the **ORIGINAL email** (now visible at the same GraphMessageId in Graph) as a fresh row.
      - EA rows attached to overwritten EmailMessages need to be reassigned: their content was downloaded under the original email's identity, so they should be linked to the (newly created) original-email row, not the overwriter row.
      - Stretch goal: build a `scripts/reconcile_email_message_graph_id_drift.py` script that detects + repairs these cases interactively (operator confirms each one).

    **Affected EAs identified so far** (4-vendor `IN132AAC.pdf` case): EA 542 (Emsg 759), 543 (Emsg 760), 544 (Emsg 762), 545 (Emsg 763). Sweep also flagged similar shape on `IN125AAC.pdf` (5 EAs), `IN127AAC.pdf` (4 EAs), `IN141AAH.pdf` (4 EAs but all on cassidy@rogersbuild.com forwards — same-sender duplicates, **legit**, not cross-vendor). Distinguish carefully when triaging.

- [ ] **Multi-invoice transmittal PDFs: per-page vendor parsing.** Surfaced 2026-05-26 processing EmailMessage 758 (Vulcan Materials via Billtrust — combined PDF with 3 invoices). The third invoice's `send to:` / remit-to KVP was **Heritage Logistics, LLC** (Vulcan's freight subsidiary), not Vulcan Construction Materials. Same physical delivery, separate legal vendor for the freight portion. Initial draft of the agent's plan would have created all 3 Bills under Vulcan — silently mis-categorizing $554.03 of freight to the wrong vendor. The bill_specialist prompt needs an explicit rule: when DI returns a multi-page document where each page has its own `send to:` / `INVOICE NO:` block, **treat each as a separate invoice and resolve the vendor per page** (not per email envelope, not per document header). Bridge each page to its own Attachment, find_vendor_for_invoice with each page's remit name independently, create separate Bills. Update both the email_specialist prompt (so it identifies multi-invoice transmittals correctly and either splits-and-delegates or flags as `vendor_statement` if splitting is impractical) and the bill_specialist prompt (so when given a multi-invoice payload it knows to expect distinct vendors). PDF-split utility worked end-to-end for Email 758 — Bills 18549/18550 to Vulcan + Bill 18551 to Heritage. Reference fixture: Email 758 (PDF 140 KB, 3 pages, Bills 18549/18550/18551 linked to per-page Attachments PublicIds `666ED999-…` / `8638B216-…` / `2D96B368-…`).

- [x] **`Vendor.IntakeNotes` editing UI.** Done 2026-05-07. Renamed `Vendor.IntakeNotes` → `Vendor.Notes` (sp_rename, idempotent guard) for end-user clarity, threaded through model → repo → service → schemas → router, added textarea on Vendor Create + Edit pages (gated `Modules.VENDORS, "can_update"`) + display on View page. Bundled with the `Project.Notes` work below as a single Wave 1.
- [ ] **React UI for new `EmailMessage.Agent*` columns.** `AgentClassification`, `AgentClassificationReason`, `AgentDecidedAction`, `AgentClassificationConfidence`, `AgentSessionId` are all populated by the agent now but invisible in the React Email Messages list/detail. Surface them as columns in the list view (`Classification`, `Action`, `Confidence`) and as a "Agent Decision" panel on the detail view, with a link from `AgentSessionId` → AgentSession transcript. Useful for human triage of `awaiting_review` rows and for auditing the agent's work.
- [x] **Email-thread approval-reply workflow — Wave 3 Phases A + B shipped 2026-05-07.** New sprocs `FindSubCostCodeForReply` (segment-padding `"13.1"` → `"13.01"` + alias matching) and `ReadBillByConversationId`. New `BillService.apply_reviewer_decision()` orchestrator: validates draft, authorizes sender against the recipient set (PM/Owner via UserProject + Role), updates summary BillLineItem (SubCostCode + description) on approval, transitions Review state via existing advance/decline payloads, persists raw reply text in `Review.Comments`. New agent tools `find_bill_by_conversation_id`, `find_sub_cost_code_for_reply`, `apply_reviewer_decision`. New `reviewer_reply` classification + `applied_reviewer_decision` action vocabulary. email_specialist gained `find_bill_by_conversation_id` + Bills-read RBAC grant on the Email Specialist role. Multi-reviewer semantics: agent applies every authorized reply (latest wins) until the human presses Complete. Phase C (poll filter expansion) + Phase D (Bill list ReviewStatus column) remain queued.
- [x] **Wave 3 Phase C — poll filter expansion shipped 2026-05-07; ~~superseded~~ rolled back 2026-05-08.** Original Phase C built `(category) OR conversationId in (...)`; MS Graph rejects any `conversationId` filter on `me/messages` (both `eq` and `in` return 400 "invalid nodes" / "too complex"). The 400 was masked because `_timed` envelope wrapped it as HTTP 200 with an inner `status_code: 400`. Outage: ~19h on 2026-05-07/08 with no new EmailMessage rows. **Fix shipped 2026-05-08**: filter is now `receivedDateTime ge <watermark>` only — pull every inbox message newer than the most recent one we have in DB, agent does relevance classification (no longer pre-filters on Outlook category). New sproc `ReadMaxReceivedDatetimeByMailbox`. New repo method `EmailMessageRepository.read_max_received_datetime_for_mailbox`. `$orderby=receivedDateTime asc` + `ge` + idempotent GraphMessageId upsert + per-message commits guarantee no message loss across partial-batch crashes (residual gap: backdated `receivedDateTime` from quarantine-release / deferred-delivery rules — see delta-query follow-up below). Defensive outbound filter: skip messages where `from_email == mailbox_address`. Initial backfill window = 7d on a fresh mailbox. **Bonus side-fix shipped same commit**: latent gap2 mismatch — `UpsertEmailMessage` + `UpsertEmailAttachment` sprocs missing `@CreatedByUserId BIGINT` param while repo passed it; would have failed `8144 too many arguments` the moment the Graph filter was fixed. Folded `@CreatedByUserId` + COALESCE(17) into both sprocs per the gap2 pattern. **Admin handler hardened**: `/api/v1/admin/email/poll` now raises `HTTPException(502)` when the inner result has `status_code >= 400` so future regressions surface in App Insights instead of hiding inside HTTP 200.
- [ ] **Migrate poll to `/me/messages/delta`.** The current `receivedDateTime`-based watermark misses backdated messages (quarantine release / deferred-delivery rules / spam-folder moves) since they land with a `receivedDateTime` older than our high-water mark. Real-world incidence on `invoice@rogersbuild.com` is rare-to-never, so accepted as residual risk for now. Eventual fix: switch the polling primitive to `/me/messages/delta`, store the `@odata.deltaLink` token per mailbox, and let Graph drive change-detection server-side. Adds: delta-token storage (probably a `MailboxState` row keyed by mailbox), full-resync fallback when the token expires (~30d), and a once-per-token-expiry replay logic. Not urgent unless we see a missed-email incident.
- [x] **Sweep other admin handlers for the same wrapped-200 anti-pattern. DONE 2026-07-12.** Audited every scheduler-facing admin handler in `shared/api/admin.py` against the criterion "sync_fn return carries a *numeric* `status_code` (or per-part status) that CAN be ≥400 and is currently returned as HTTP 200." **Exactly one handler needed the fix: `sync/qbo/{entity}`** — all 10 `sync_qbo_*` scripts return `{"result": {…}, "status_code": 200|500}` where the 500 is a *caught-internally* exception (so `_timed`'s 500-on-exception never fires) → passed through as HTTP 200 → scheduler saw failed QBO syncs as success. Fixed by mirroring the email/poll template: after `_timed`, if `envelope["result"]["status_code"] >= 400`, raise `HTTPException(502)` with the inner `error`/`upstream_status` detail. Only the total-failure 500 raises; partial per-record failures still return `status_code:200` (watermark held + retried) and correctly pass through as 200. `_timed` left untouched. Test: `tests/test_admin_wrapped_200.py` (non-2xx→502, 2xx→200 envelope). Codex gpt-5.5/xhigh review PASS. **Audit result — handlers that did NOT need the fix, and why:**
  - `outbox/drain`, `/drain/qbo`, `/drain/ms` — return `{qbo/ms: "ok" | {"error":…}}` markers (deliberate per-integration failure isolation), no numeric `status_code`. LEFT (a separate design choice from wrapped-200; out of this unit's scope).
  - `box/drain` — `BoxOutboxWorker.drain_all` returns totals / `{skipped}` / `{paused}` / `{error}`; no numeric status_code. LEFT.
  - `reconcile/qbo`, `reconcile/ms` — services **raise** on error → `_timed` 500-on-exception already covers it. LEFT.
  - `reconcile/box` — `BoxReconcileService.run()` returns a canary report / `{error}`; no numeric status_code. LEFT.
  - `bill-folder/tick` — `process_single_item` returns `{"status":"skipped"/"completed"}` (string marker, no numeric status_code); MS-Graph non-200 **raises** → caught → `mark_failure` records the error on the item row by design (parent run keeps draining). LEFT.
  - `bill-folder/enumerate` — **already raises `HTTPException(502)`** on `BillFolderEnumerationError` (@931); its normal return has no numeric status_code. LEFT (already fixed — not double-wrapped).
  - `email/process_one` — does not use `_timed`; **already raises** 503/500 on real failures; success payload has no status_code. LEFT.
  - `email/recover_stuck` — count-summary merge; **raises** → `_timed` 500. LEFT.
  - `email/extract` — `extract_by_public_id` returns `{"status":"skipped"/"failed"}` string markers and **raises** ValueError on not-found; no numeric status_code. LEFT.
  - (`email/poll` — the original fix / template @494–521.)
- [x] **Wave 3 Phase D — Bill list ReviewStatus column shipped 2026-05-07.** `/get/bills` response now includes `review_status`, `review_status_is_final`, `review_status_is_declined` per bill (batched via new `ReadCurrentReviewsByBillIds` sproc + `ReviewRepository.read_current_by_bill_ids` — single SQL call per page, no N+1). React Bills list shows Draft + Review columns side-by-side with color-coded badges (declined / approved / in-review / dash for no review). AP can now spot `Draft=true AND ReviewStatus=approved` (= "PM signed off, ready for AP to Complete") at a glance.
- [ ] **Bill folder ↔ email pipeline back-link.** When the same Bill arrives via both bill_folder AND email pipelines, `LinkBillSourceEmailMessage` (shipped 2026-05-06) handles the email-side backfill IF the email-driven create_bill runs after the folder-driven one. The reverse case — folder run after email — would still leave the Bill un-linked. Address by: (a) on bill_folder pipeline create, opportunistically check for an `EmailMessage` whose attachment hash matches the file being processed, and stamp the link; OR (b) accept the gap and document as "use the email pipeline path when both are options."
- [x] **Pre-existing `dbo.bill.sql` migration breakage.** ~~Running `python scripts/run_sql.py entities/bill/sql/dbo.bill.sql` fails with `Invalid column name 'ExpiresAt'` on the `BillCompletionResult` query.~~ **RESOLVED 2026-07-12** (same root cause as the "Bill SQL drift" item below — see there). Root cause: `dbo.bill.sql` carried a stale *duplicate* `BillCompletionResult` block (old `ExpiresAt`/1h-TTL design); the canonical, prod-matching definition already lived in the sibling `entities/bill/sql/dbo.bill_completion_result.sql` (CompletedAt + idempotent ExpiresAt→CompletedAt migration). Deleted the duplicate from `dbo.bill.sql`; no prod migration needed (prod already correct). Codex xhigh review PASS.
- [ ] **Bill memo template expansion if minimal proves insufficient.** Current template (2026-05-06): `"DOC#:{raw_invoice_number} | Ref:{po_or_job_or_reference}"` — minimal, only fields not in typed columns. If users find themselves wanting to grep memos for vendor name / total / project / etc. (because cross-system search via QBO export, Excel, etc. doesn't have access to the joined columns), revisit and switch to the denormalized format. Track real usage; don't speculatively expand.
- [x] ~~**Server-side guard: `Bill.DueDate >= Bill.BillDate`.**~~ **Shipped 2026-07-12 as the stronger EQUAL rule.** The `>=` phrasing here is superseded by the authoritative rule (umbrella memory `feedback_bill_duedate_equals_billdate.md`, confirmed 2026-05-27): **`Bill.DueDate` must ALWAYS EQUAL `Bill.BillDate`.** Enforced at the sproc choke point (not the service) so ALL callers — manual UI, email/bill agents, QBO sync, scripts — are covered by one change. `dbo.CreateBill` + `dbo.UpdateBillById` now persist `DueDate = @BillDate` and **ignore** the passed `@DueDate` (param kept for back-compat; force/mirror, not reject). Migration `entities/bill/sql/migrations/005_bill_duedate_mirror_billdate.sql`, ported into **both** base `CreateBill` definitions (`dbo.bill.sql` + `dbo.bill_create_source_email.sql`) and `UpdateBillById`. Codex xhigh review PASS. (Note: existing `DueDate != BillDate` rows — 4,940 at ship time — are NOT backfilled; see the backfill follow-up below.)
- [x] ~~**Agent due-date default when DI doesn't extract one.**~~ **Largely mooted 2026-07-12** by the sproc-level DueDate mirror above. Since `dbo.CreateBill` / `dbo.UpdateBillById` now force `DueDate = @BillDate` regardless of what the agent supplies, whatever the agent computes (null, copy-from-DI, or a `bill_date + payment_term.days` fallback) is ignored for the persisted `DueDate` — the produced value is always valid by construction. `PaymentTermId` is still stored separately and remains the source of truth for when payment is actually owed; no agent change is needed for DueDate correctness. Close.
- [ ] **Backfill existing bills where `DueDate != BillDate` (data cleanup, separate unit).** Filed 2026-07-12 alongside the migration-005 sproc guard, which only fixes NEW create/updates. A read-only prod count at ship time found **4,940** `dbo.Bill` rows with `CONVERT(date,DueDate) <> CONVERT(date,BillDate)`. Backfill would `UPDATE dbo.Bill SET DueDate = BillDate WHERE DueDate <> BillDate` — but treat as its own reviewed unit: scope/verify the row set first, run in batches, and confirm no downstream consumer keys off the historical DueDate before mutating. Explicitly OUT OF SCOPE of the migration-005 unit.
- [ ] **Reconcile pre-existing `CreateBill` drift across the two base files (low-risk, latent).** Surfaced 2026-07-12. `CreateBill` is defined in BOTH `entities/bill/sql/dbo.bill.sql` (older variant, **lacks** `@CreatedByUserId`) and `entities/bill/sql/dbo.bill_create_source_email.sql` (live variant, has `@CreatedByUserId` + `COALESCE(@CreatedByUserId,17)`). A full base re-run applies whichever runs last; if the older `dbo.bill.sql` variant wins, new bills' `CreatedByUserId` falls to the table `DEFAULT 17` rather than the passed user (harmless today — 17 = Christopher — but silent). Both now carry the DueDate=BillDate fix, so this is NOT a DueDate risk. Fix later by collapsing to a single canonical `CreateBill` definition (drop the stale one from `dbo.bill.sql`, keep the source-email variant).
- [x] ~~**`FindVendorForInvoice` + `FindProjectForInvoice` sprocs return phantom rows with NULL Name/PublicId.**~~ Closed 2026-05-26 — could not reproduce. Direct `EXEC FindVendorForInvoice @VendorName='Puente''s Drywall', @SenderDomain=NULL` returned a single fully-populated row (`VendorId=749`, `VendorName='Puente's Drywall, Inc'`, `VendorPublicId='A48AE4DD-...'`, `Strategy='prefix_name'`, `Confidence=0.85`). Same shape on `FindProjectForInvoice @AddressHint='4005 Franklin Pike'` — two fully-populated rows (`ProjectId=35` FP + `ProjectId=36` FP-POOL). Verified via the production service path (`VendorService.find_for_invoice`) as well. Deployed sproc `OBJECT_DEFINITION` exactly matches the canonical `entities/{vendor,project}/sql/` files; git log shows no edits since 2026-05-07 (12 days before the bug was reported). Likely explanation: transient state during the prior agent walk (concurrent scheduler interleaving, or the prior session misread a different unrelated row). Locked contract via new `scripts/verify_find_for_invoice_sprocs.py` — exercises both sprocs across known cases, asserts every row with non-null Strategy carries non-null Name + PublicId + Confidence. If the bug ever recurs, that script reproduces it.
- [x] ~~**`BillRepository.read_by_id` drops `SourceEmailMessageId` from its projection.**~~ Shipped 2026-05-26. Audit found 8 affected sproc projections in `entities/bill/sql/dbo.bill.sql`, not just `ReadBillById`: `ReadBills`, `ReadBillById`, `ReadBillByPublicId`, `ReadBillByBillNumber`, `ReadBillByBillNumberAndVendorId`, `ReadBillsPaginated`, plus the OUTPUT clauses of `UpdateBillById` and `DeleteBillById`. All 8 fixed via additive `[SourceEmailMessageId]` column include — purely additive, no Python changes required (model dataclass + `_from_db` getattr already accommodated the column). Applied via `entities/bill/sql/migrations/003_read_bill_source_email_message_id.sql` (working around the pre-existing `BillCompletionResult.ExpiresAt` breakage in the canonical file — TODO line 196). Verified on the original repro case: `BillService.read_by_id(18545)` now returns `source_email_message_id=752` (Puente's Drywall #811). Regression locked by `scripts/verify_bill_source_email_projection.py` — exercises `read_by_id` + `read_by_public_id` + `read_by_bill_number_and_vendor_public_id` against the most-recent Bill with a non-null SourceEmailMessageId. Out of scope: Expense + BillCredit have the column on the table but no Python wiring yet (per the cross-entity migration's "extended when Phase 2 email agent actually starts populating these" note — revisit when those entities gain email-driven creation).
- [x] ~~**No type-enforcement separating `EmailAttachment.PublicId` from `Attachment.PublicId`.**~~ Lightweight fix shipped 2026-05-26. `BillService.create()` now auto-bridges when the supplied `attachment_public_id` misses the Attachment table but hits the EmailAttachment table: lazy-imports `EmailAttachmentRepository` + `EmailAttachmentBridgeService`, calls `bridge()`, logs an `INFO` line `"BillService.create auto-bridged EmailAttachment X → Attachment Y"`, and continues with the bridged Attachment's public_id. Hash dedup in the bridge service makes repeated calls idempotent (no duplicate Attachment rows). End-to-end test: created draft Bill 18546 with EmailAttachment `1A03CB8D-…` (Puente Drywall #811 attachment); BillLineItemAttachment correctly linked to bridged Attachment `3DEA5C45-…`; cleanup succeeded. Regression locked by `scripts/verify_bill_attachment_auto_bridge.py` — read-stable (uses already-bridged EmailAttachment as fixture; hash dedup short-circuits to existing row, no new rows). CLAUDE.md updated. **Stronger type-distinction fix deferred** — the lightweight fallback is sufficient for v1 and Expense/BillCredit don't have the same path yet.
- [~] **Reviewer-reply ConversationId match is too strict — Phase 1 shipped 2026-05-26.** New sproc `FindBillForReviewerReply(@ConversationId, @BillNumberHint, @ProjectHint)` adds a deterministic fuzzy fallback: when the conv_id match misses AND both hints are present, looks up Bills where `IsDraft=1 AND BillNumber matches exactly AND any BillLineItem.ProjectId → Project.Name LIKE '%project_hint%'`. Single-result requirement — 0 or 2+ candidates return null, preserving the existing `flagged_needs_review` outcome for ambiguous cases. Returns extra `MatchKind` column (`'conversation'` | `'fuzzy'`) for telemetry. Single-result-set design (resolves matched id into a local variable, emits one final SELECT) avoids the pyodbc-fetchone-reads-first-resultset trap that initially masked fuzzy hits. Server side: migration `entities/bill/sql/migrations/004_find_bill_for_reviewer_reply.sql` + canonical `dbo.bill_create_source_email.sql`; new `BillRepository.find_for_reviewer_reply()`; HTTP `GET /get/bill/find-by-conversation-id` gained two optional query params. Tool side: `find_bill_by_conversation_id` schema extended with optional `bill_number_hint` + `project_hint`; backward-compatible (old callers pass only conv_id and behavior is unchanged). Prompt side: email_specialist's step 1b updated to instruct the agent to extract both hints opportunistically from subject + body and always pass them on the tool call (`/N` suffix trimming noted). Regression: `scripts/verify_reviewer_reply_fuzzy_fallback.py` exercises three paths (conv miss + correct hints, conv miss + no hints, conv miss + bogus hints) — PASS. Sanity-check on the original Email 750 repro confirms null result preserved (Bill #206640 doesn't exist, so no false positive). **Phase 2 — `PendingReviewerReply` table + React queue** for the multi/zero-candidate cases (parsed payload surfaced as "Unmatched Reviewer Replies" with Bind/Discard controls) **and Phase 3 — auto-create stub draft from approval payload** remain deferred until Phase 1 has field exposure.
- [~] **Contract laborer timesheet emails have no pipeline path** — **(1) Vocabulary shipped 2026-05-26.** Added `contract_labor_timesheet` to `AgentClassification` (no schema change needed; column was already a bare NVARCHAR(50) with no CHECK constraint). Threaded through everywhere it has to land: `ReadEmailSenderHistory` sproc gained a `ClassContractLaborTimesheet` counter (migration `entities/email_message/sql/migrations/002_contract_labor_timesheet_vocab.sql`, canonical sproc body also updated); `EmailMessageRepository`'s sender-history hydrator surfaces it as `by_classification["contract_labor_timesheet"]`; the `mark_email_outcome` tool's `classification` description now lists it with a one-line explanation; email_specialist prompt's controlled-vocab table updated; CLAUDE.md doc-of-record list updated. Verified end-to-end: re-stamped Email 769 (JR Scruggs `Work Hours 5/11`) from `unknown` → `contract_labor_timesheet`; sender history for `jrscruggs07@gmail.com` now reports `contract_labor_timesheet: 1` via both the raw sproc and the Python service path. Until routing (item 2) ships, the agent stamps `contract_labor_timesheet` + `flagged_needs_review` so the row surfaces clearly in the awaiting_review queue instead of being miscategorized as `non_actionable` + `marked_irrelevant`. **Routing decision locked 2026-05-26**: **automated `contract_labor_specialist` agent** (matches bill_specialist pattern). **(3) Sender → Vendor mapping pattern decided 2026-05-26**: Option B — populate `Contact.Email` per CL vendor, look up via `Vendor INNER JOIN Contact WHERE IsContractLabor=1 AND LOWER(Contact.Email)=LOWER(@sender)`. No new column. **First fixture seeded 2026-05-26**: Vendor 1175 (John Randall Scruggs) flipped to `IsContractLabor=1` + new `Contact` row (Id 19, PublicId `5136F8B7-…`) binding `jrscruggs07@gmail.com`. Lookup verified — single Vendor returned. The other 7 pre-existing CL vendors (Brayan Marcia Salina, Denis Marcia Izaguirre, Elmer/Emilson/Selvin Cordova Tercero, Michael Jacobson, Wilmer Diaz) have NO Contact email — they'll need per-worker backfill before the agent can route their timesheets. Surface as a one-shot admin task or React-driven worker-by-worker entry. **(2) Still open: agent build itself** — package `intelligence/agents/contract_labor_specialist/` with definition + prompt + __init__; User + Auth + Role + RoleModule grants; new tools (`find_contract_labor_vendor_by_email`, `create_contract_labor_line_item`, reuse `delegate_to_project_specialist`); email_specialist gains `delegate_to_contract_labor_specialist` tool + prompt branch when classification = `contract_labor_timesheet`; new `delegated_to_contract_labor_specialist` action vocab (sproc counter, hydrator, tool desc, CLAUDE.md). Estimated ~7-10 hours; pick up in a fresh session.

## Agent feedback loop — knowledge + resolution UI (post-2026-05-06 needs_review review)

Surfaced from reviewing the `flagged_needs_review` + `marked_irrelevant` pile after the Haiku swap. The agent flags items it can't confidently handle, but today there's no friction-free way for a human to (a) resolve those flags and (b) capture the knowledge that would have prevented the flag — so the same flag fires again on the next similar email. These items close that loop.

- [x] **`Project.Notes` (NVARCHAR(MAX))** — added 2026-05-07. Idempotent `ALTER TABLE ADD` guard, threaded through `Create*` / `Update*` / `Read*` sprocs, surfaced in `FindProjectForInvoice` projection (so bill_specialist sees it during invoice processing), and propagated to model → repo → service → schemas → router. Bundled with Vendor.Notes as Wave 1.
- [x] **`Vendor.Notes` + `Project.Notes` editing UI.** Done 2026-05-07. Textarea on Vendor + Project Create/Edit pages, display field on Vendor + Project View pages. Empty-string-on-PUT clears the field to NULL; field omitted preserves the existing value. Both bill_specialist and project_specialist prompts updated to read `notes` from the find-for-invoice results.
- [x] **(Side-bug fixed) `ProjectRepository.find_for_invoice` / `update_by_id` / `delete_by_id`** — these methods were accidentally nested inside the module-level `_bit()` helper (4-space mis-indentation), making them inaccessible on the class. Verified inaccessible via `hasattr` before fix. Moved back into the `ProjectRepository` class. Project edits in the React UI couldn't have worked end-to-end before this fix.
- [x] **(Side-bug fixed) `dbo.project.sql` regression risk** — the canonical file was missing the gap1 RBAC scoping params (`@ActorUserId` / `@ActorIsSystemAdmin`) and the gap2 `@CreatedByUserId` threading. Both lived only in side-migration files (`scripts/migrations/gap2_*.sql` and `entities/project/sql/migrations/001_gap1_scope_by_user_project.sql`), so a re-run of `dbo.project.sql` would silently regress prod. Folded both into the canonical file so re-running is now safe and idempotent.
- [ ] **Project alias / address-alias structured matching** *(deferred — start with notes-only)*. If `Project.IntakeNotes` text matching by the agent isn't reliable enough, promote address aliases to a structured `ProjectAlias` table (Id, ProjectId, Alias) + extend `FindProjectForInvoice` to match against it. Don't pre-build; add when notes-based matching produces a real false-negative we can point at.
- [x] **Email Inbox view in React (read-only first).** Done 2026-05-07 (Wave 2 v1). New `/email-message/list` with status filter (default `awaiting_review` for triage), search, 50/page pagination. Detail page `/email-message/:id`: header card (from / to / cc / received / status / Outlook link), Agent Decision panel (classification / action / confidence / reason / `agent_session_id`), **Linked Bill panel** (slim Bill lookup via new `ReadBillSlimBySourceEmailMessageId` sproc + filtered index `IX_Bill_SourceEmailMessageId` — one-click email→Bill nav), Attachments table (filename / size / extraction status / DI typed fields / bridged?), Body viewer (sandboxed iframe for HTML emails, `<pre>` for plain). Module row's `Route` updated from `/email/list` → `/email-message/list` to match the entity-aligned URL pattern.
- [ ] **Inbox action surface — re-run with hint, override, manual create, mark resolved.** Layered on the read-only inbox view above. Action buttons per row:
  - **Re-run with hint** — text box, kicks off a fresh email_specialist run with the human's hint (e.g., `"This is the Bluebird Landing project — Project Id=42"`) prepended to the system prompt. Killer feature: lets a human resolve a `needs_review` while simultaneously teaching the agent (write the same text into `Project.IntakeNotes` so next time it's autonomous).
  - **Override classification** — manually set `AgentClassification` + `AgentDecidedAction` and flip status to terminal (e.g., reclassify a misclassified email).
  - **Manual create Bill** — opens the standard React Bill create form pre-populated with DI extraction values (vendor, number, total) + the email's bridged Attachment, for cases the agent couldn't auto-bridge.
  - **Mark resolved / Done** — terminal status flip, no agent action; for cases like duplicate forwards (id=10, id=14) that the agent correctly de-duplicated and just need clearing from the queue.
- [ ] **Bill list page: Draft + ReviewStatus side-by-side columns.** Today the React Bills list shows the Draft flag but not the ReviewStatus. After the email-agent reshape + reviewer-reply feature, the meaningful "ready for final Complete" signal is `Draft=true AND ReviewStatus=approved` — needs both columns visible (sortable + filterable) so AP can spot which drafts the PMs have signed off on. Surfaced 2026-05-06 during the reviewer-reply design discussion; pure UI change.

## Intelligence layer

- [x] **Parallel tool dispatch in `runner.py`.** (API `bd327b5`, 2026-04-25) Refactored the dispatch loop into `_dispatch_tools_concurrently` — handlers run via `asyncio.create_task`, events forward live via a shared queue, result_blocks land in original call order. Verified ~2x speedup on the synthetic two-tool case.
- [x] **Add `read_cost_code_by_number` tool.** (API `bd327b5`, 2026-04-25) Endpoint `GET /api/v1/get/cost-code/by-number/{number}` + agent tool added (service + repo + sproc were already in place).
- [ ] **UI lanes for concurrent sub-agent events.** With parallel dispatch live, forwarded sub-agent events from concurrent delegations now interleave on Build.One's stream. Per-sub-agent chronology is preserved (each event carries its session_public_id), but visually they mix together. Group sub-agent events into their own lane in the tray (collapsible / tabbed / indented) so the user can read each specialist's flow coherently.
- [ ] **Entity expansion: next specialist.** SubCostCode / CostCode / Customer / Project / Vendor all live. Future picks: Bill, Expense, Invoice, ContractLabor, etc. Same template per entity (~20-30 min each).

## Frontend perf

- [x] ~~React Query caching for `/lookups`, `/vendors`, `/projects`~~ — shipped 2026-04-23 via TanStack Query (build.one.web `42e0a90`). staleTime 5min, gcTime 10min, refetchOnWindowFocus off.
- [ ] Consider replacing the "dump all vendors" pattern with a searchable/paginated select when tenant size grows past a threshold.
- [ ] Verify `X-Cache: hit` vs `miss` shows up in dev tools on `bill-folder-summary` after back-to-back loads (we saw 292ms once; want to confirm pattern). If misses are frequent due to per-worker cache with `-w 2`, consider Redis.

## Frontend pre-launch (build.one.web)

- [x] **Graceful token refresh — stop the 30-min lockouts.** (web `36573c6`, 2026-04-24) `client.ts` now calls `POST /api/v1/auth/refresh` on 401 and retries once before wiping localStorage. Covers `request`, `uploadFile`, `fetchViewAttachmentBlob`.
- [x] **Patch `src/agents/sseClient.ts` to use the same refresh machinery.** (web `5b9aaa9`, 2026-04-24) `start`, `continue`, `approve`, and `events` all now route through the shared `fetchWithRefresh` helper exported from `client.ts`. `cancel` stays raw but proactively refreshes first.
- [ ] **Dev-gate the TanStack Query devtools.** `src/main.tsx` currently renders `<ReactQueryDevtools />` unconditionally, so the floating panel ships to prod. Before launch, wrap in `{import.meta.env.DEV && <ReactQueryDevtools ... />}`. Keep the dev-only version for local debugging.

## Bill list page review — `build.one.web/src/pages/bills/BillList.tsx` (2026-05-06)

Findings from a focused review of the Bills list page. Listed in priority order; first three are real bugs, rest are UX / hardening.

- [ ] **Vendor / Project names render blank past the dropdown's first page.** [BillList.tsx:134-135](../build.one.web/src/pages/bills/BillList.tsx#L134-L135) calls `useIdNameMap` against `/api/v1/get/vendors` and `/api/v1/get/projects` with no `?page_size=`; [useIdNameMap.ts:14](../build.one.web/src/hooks/useIdNameMap.ts#L14) just `getList`s and builds the map from one page. Any bill whose vendor/project ID isn't on that first page falls back to `""` — both the cell *and* the sort-by-vendor key go blank silently. Fix options: (a) pass `?page_size=10000` (cheap; tenant has ~1100 vendors); (b) add a slim id+name endpoint; (c) join `vendor_name` / `project_name` into the bill list response server-side and drop the maps entirely (best long-term).
- [ ] **Sort is page-local, not global.** [BillList.tsx:146-165](../build.one.web/src/pages/bills/BillList.tsx#L146-L165) sorts `resolvedItems`, which is the current 50-row page only. Clicking a `sortable-th` header looks like a global sort but page 2 sorts independently of page 1 — misleading. Fix: lift sort to the API (`?sort=column&dir=asc|desc` query params on `GET /api/v1/get/bills` → through service → into the paginated sproc's `ORDER BY`). Pairs naturally with #1 if the list response gains `vendor_name` / `project_name`.
- [ ] **Folder-processing poll never cancels on unmount.** [BillList.tsx:92-119](../build.one.web/src/pages/bills/BillList.tsx#L92-L119) uses recursive `setTimeout(poll, 2000)` with no AbortController, no `useRef` cancel handle, and no `useEffect` cleanup. Navigating away during folder processing keeps polling and calls `setState` on an unmounted component (React warns). Fix: store the timer id in a `useRef` + clear in a `useEffect` cleanup, or convert to an `AbortController` and check `signal.aborted` before each setState.
- [ ] **Search input has no debounce.** [BillList.tsx:386-388](../build.one.web/src/pages/bills/BillList.tsx#L386-L388) calls `setSearch` on every keystroke. `usePaginatedList` re-fetches on each change — typing "vendor" fires 6 requests against the prod API. Add a 250–300ms debounce inside `usePaginatedList` (or at the input). Same fix would benefit other list pages that consume the hook.
- [ ] **Disallowed file types are dropped silently in the drop zone.** [BillList.tsx:170](../build.one.web/src/pages/bills/BillList.tsx#L170) filters by `ACCEPTED_TYPES` and skips anything else with no feedback. Drop a `.docx` and nothing happens. Add a per-file error row (`error: "Unsupported type"`) so the user knows why their file disappeared.
- [ ] **Brittle sort comparators on `_date` and `is_draft`.** [BillList.tsx:158-162](../build.one.web/src/pages/bills/BillList.tsx#L158-L162) does `Number(aStr)` first, falls through to `localeCompare`. Booleans (`"true"`/`"false"`) and ISO dates work *coincidentally* — if `bill_date` ever becomes `MM/DD/YYYY` the date sort silently breaks. Move to typed comparators per column (a `getSortValue(col, row)` map returning `number | string`). Lower priority — wait until #2 lifts sort to the server, which makes this moot.
- [ ] **Inconsistent persistence: `sortKey` / `sortDir` use sessionStorage, `statusFilter` does not.** [BillList.tsx:49](../build.one.web/src/pages/bills/BillList.tsx#L49) vs [:50-55](../build.one.web/src/pages/bills/BillList.tsx#L50-L55). Pick one: persist all three, or persist none. Persisting all three is the user-friendlier choice (matches how `usePaginatedList` already persists `page` + `search` via `sessionPersistenceKey`).

## Jinja purge — migration waves (started 2026-04-23)

Phase 0 audit done. Phase 1 scaffolding (`/auth/me` + SSE `/auth/me/changes` + React `useCurrentUser` + Sidebar gating) shipped.

Phase 2 — delete Jinja per entity in waves, one commit each, verify in UI before next wave:

- [x] **Wave A — leaf entities:** address_type, payment_term, review_status, vendor_type, taxpayer, classification_override, dashboard. (2026-04-24, `f34b63b`; 21 routes retired, 622 → 601)
- [x] **Wave B — reference entities:** cost_code, sub_cost_code, customer, vendor, address, organization, project, company, module. (2026-04-24, `f6cecf2`; 36 routes retired, 601 → 565)
- [x] **Wave C — identity surface:** user, role, role_module, user_module, user_project, user_role, vendor_address, project_address. (2026-04-24, `e0ddc32`; 28 routes retired, 565 → 537. `project_address` controller was dead code — never registered in `app.py`.)
- [x] **Wave D — transactional:** bill, bill_credit, expense, invoice. (2026-04-24, `7758405`; 17 routes retired, 537 → 520. Extracted `_enrich_line_items` → `entities/invoice/business/enrichment.py` as `enrich_line_items` since the invoice packet PDF generator depends on it; dropped legacy `/expense/edit/{id}` redirect.)
- [x] **Post-Wave-E admin rip (2026-04-24):** removed the React admin surface E4 had just built and the dead workflow-approval plumbing. Deleted `entities/admin/` (api + schemas + empty business/persistence stubs), `core/workflow/business/admin.py` (`WorkflowAdmin`), `core/workflow/business/pending_actions.py`, `core/workflow/business/execute_pending_action.py`, `core/workflow/api/pending_action_router.py`, React `pages/admin/AdminDashboard.tsx` + `WorkflowDetail.tsx`, and their App.tsx routes. All action buttons (retry/approve/reject/cancel) gated on workflow states (`awaiting_approval`, `failed`, `needs_review`) that no production code transitions to anymore — the email-intake workflow that used them was decommissioned months ago and agents use their own approval system on `AgentSession`. Kept the workflow **data layer** (`orchestrator`, `instant`, `models`, `WorkflowRepository`, `WorkflowEventRepository`, `execute_synchronous`) — every entity CRUD still records a `Workflow` + `WorkflowEvent` row for audit. Routes: 484 → 472 (−12). If email-intake returns (planned "inbox rebuild"), the admin UI gets rebuilt for the new workflow shape anyway.
- [x] **Wave E — shared-infra deletion (2026-04-24):** deleted all remaining Jinja web controllers (auth, integration, 5 QBO under `integrations/intuit/qbo/*/web/`, sync, plus 4 empty-stub controllers on taxpayer_attachment / invoice_line_item / invoice_attachment / invoice_line_item_attachment), the entire `templates/` tree, the `static/` directory, and all Jinja helper code — `get_current_user_web`, `require_module_web`, `WebAuthenticationRequired`, `RefreshRequired`, the two FastAPI exception handlers in `app.py`, and the `StaticFiles` mount. Dropped `Jinja2==3.1.6` from `requirements.txt` + `requirements-prod.txt`. The QBO OAuth callback now returns a self-contained HTML landing page instead of redirecting — since React runs locally only, there's no public web host to bounce to, so the API renders a "QuickBooks connected, close this tab" page inline via `HTMLResponse`. CSRF helpers kept (React's silent refresh relies on them). `entities/auth/business/service.py` pruned (~100 lines gone, 7 orphan service imports removed). Routes: 508 → 484 (−24). Password reset deferred — ship as its own small wave when needed; it doesn't block anything now.
  - [x] **E4 — admin in React (2026-04-24):** new `AdminDashboard.tsx` (6 stat cards + searchable/filterable workflows table via `/api/v1/admin/stats` + `/admin/recent-workflows` + `/workflows/search`) and `WorkflowDetail.tsx` (meta header, related-entities links, full event timeline, and retry / approve / reject / cancel actions with inline modals). Dropped the Jinja "Inbox Quality" tab entirely — it referenced `/api/v1/admin/inbox-stats` + `/api/v1/classification-overrides*` endpoints that were never implemented (email-intake surface was decommissioned earlier). Also deleted the 4 orphan React `ClassificationOverride*` pages + their App.tsx routes + the `ClassificationOverride` type — the entity was purged from the API months ago. Deleted `entities/admin/web/` + `templates/admin/` (view.html, workflow_detail.html, overrides.html). Routes: 510 → 508 (−2). `static/css/admin.css` is now orphaned and will go with Wave E5.
  - [x] **E3 — contract_labor Import + Bills in React (2026-04-24):** new React pages `ContractLaborImport.tsx` (drag-and-drop Excel upload, results display) and `ContractLaborBills.tsx` (per-vendor card grid, per-vendor + bulk generate, collapsible day summary, auth-aware PDF preview). Added Import/Bills links to `ContractLaborList`. Extracted ~200 lines of vendor-aggregation business logic from the old Jinja `bills_page` into `entities/contract_labor/business/bill_summary.py::build_bills_summary`; exposed at `GET /api/v1/contract-labor/bills-summary?billing_period=...`. Deleted entire `entities/contract_labor/web/` + `templates/contract_labor/` (including the already-redundant `/list`, `/edit`, `/{id}` Jinja routes). Routes: 515 → 516 (+1 summary endpoint) → 510 (−6 web routes) = **510 net**.
  - [x] **E2 — React signup (2026-04-24):** added `SignupPage.tsx` + `signup()` on `AuthContext`, routed at `/signup`, cross-linked from `LoginPage`. Reuses existing `POST /api/v1/signup/auth`; on success redirects to `/user/create` to complete the User entity. Deleted Jinja `GET /auth/signup` route and `templates/auth/signup.html` (routes 516 → 515). Two stale `/auth/signup` links remain in `templates/shared/partials/header.html` + `templates/auth/login.html` — both go with Wave E5 shared-infra deletion, low impact since nobody lands on those Jinja pages.
  - [x] **E1 — dead-code + quick wins (2026-04-24):** deleted `entities/attachment/web/controller.py` (unreachable duplicate of `/api/v1/view/attachment`), deleted entire `entities/legal/` + `templates/legal/` and added React `EulaPage`/`PrivacyPage`, wired `<InlineContacts>` into `UserEdit`/`UserView` (Company already had it). Routes 520 → 516 (−4). (An OAuth `?success=&message=` toast was also added to `IntegrationList` but removed later the same day — React runs locally only, so the callback renders a self-contained HTML landing page on the API host rather than cross-redirecting.)
- [x] **Contact UI re-implementation (2026-04-24).** `<InlineContacts>` React component is wired into all 5 parent edit pages (Vendor, Company, Customer, Project, User) and all 5 parent view pages (readOnly). Old `shared/partials/contacts_*.html` partials are unreachable and will be removed with shared-infra deletion.

Follow-ups / post-purge:

- [ ] **Promote SSE profile events from B-lite to B-full** (cross-worker). Add `[auth].[ProfileChangeEvent]` table + 2s poll in the SSE handler. Needed once we scale past `-w 2` single instance, or once we have a user base that hits cross-worker edges often.
- [ ] **Add `IsNavigable BIT NOT NULL DEFAULT 1` to `dbo.[Module]`.** Today every Module row that exists for RBAC permission gating also shows up in the sidebar. `Attachments`, `Pending Actions`, and `Time Tracking` are legitimately-scoped permission modules but have no top-level UI page; today they render as broken sidebar links. Splitting the flag lets the backend keep RBAC scopes distinct from navigation. `_resolve_me_payload` should then omit non-navigable modules from the `modules[]` array (or the sidebar should filter on the flag).
- [x] **Admin-driven password reset.** (2026-04-29, API `81443a1`, web `311c3bc`) New `POST /api/v1/admin/auth/set-credentials/{user_public_id}` (Modules.USERS, can_update) lets an admin create-or-update Auth (username + password ≥ 8) for any user and link it via `Auth.UserId` in one call, plus `GET /api/v1/admin/auth/by-user/{user_public_id}` returns `{username, has_auth}`. Wired as a Credentials card on the new UserProfile page. Closes the onboarding gap (admin creates User → admin sets credentials → user logs in).
- [ ] **Self-service password reset / "forgot password".** Public flow on the login page — email a reset token, accept the new password from an unauthenticated form. No implementation anywhere (no token schema, no email send, no React page). Ship as its own wave when needed.
- [ ] **Self-service "change my password" while logged in.** No settings page yet. Only an admin can change a user's password today. Add when there's a real user-facing settings surface to drop it into.
- [x] ~~**Revoke refresh tokens on password change.**~~ ✅ **DONE** (Gap 3, commit `64a2a05`, 2026-05-06; re-verified 2026-07-11 — `set_credentials_for_user` now calls `revoke_all_refresh_tokens_for_auth` on every password change). Historical rationale: `AuthService.set_credentials_for_user` updated the password hash but didn't touch existing `[auth].[RefreshToken]` rows for that user. A user whose password is reset stays logged in (their access token works for ~15min, refresh token for ~30d) until tokens expire. Security best practice: on password change, revoke all refresh tokens for the target Auth. One sproc + one call inside `set_credentials_for_user`. Not blocking onboarding but worth shipping before launch.

## User Profile / authz follow-ups (2026-04-29)

- [x] ~~**Per-endpoint enforcement of `accessible_project_ids` (list paths).**~~ Shipped 2026-05-06 via Gap 1 (`commit e2d3afb`) for Bill / BillCredit / Expense / Invoice / ContractLabor / Project list + paginated + count sprocs. By-id direct lookups still leak — tracked separately as "Gap 1 tightening pass" above.
- [ ] **Fine-grained `UserModule` permissions (optional schema upgrade).** Today `UserModule` is a `(UserId, ModuleId)` join with no flag columns; we interpret it as additive `can_read=true`. If admins want per-user create/update/delete grants beyond what the user's role provides, extend the table with the same 7 flag columns `RoleModule` has and update the merge in `shared/rbac.py::_resolve_permissions_from_db`. Skip until there's a real ask — the read-only grant covers most of the visibility cases.
- [ ] **Auto-cleanup of orphaned `UserCompany` rows when a User's Organization is unassigned.** Today the Profile filter only restricts NEW Company picks to companies linked to the user's Orgs via `OrganizationCompany`; existing UserCompany rows are left alone if their Organization is removed. May or may not be desired — punted intentionally in Wave 4. Decide policy when there's user data to look at.
- [ ] **Reverse view on Company page: "which Organizations is this Company a member of?"** `OrganizationCompany` has a `…ByCompanyId` read endpoint already; just needs a small Organizations section on `/company/:id` (mirror of the Companies section that landed on `/organization/:id` in Wave 4).

## Review entity follow-ups

- [ ] **Decide whether to gate completion on review approval** (per parent: Bill, Expense, BillCredit, Invoice). V1 of the `Review` entity ships records-only — `complete_*` endpoints are NOT guarded by `ReviewService.is_approved(...)`. The hook is a 1-line change at each completion router (`if not review_service.is_approved(parent_type, parent_public_id): raise 409`). Decision is per-parent, not all-or-nothing — a customer may want to require review on Bills but not on Expenses. Round back once the entity is in production use and we've seen real review data.
- [ ] **`Review.is_approved` semantics with soft-deleted parents.** When the four parents (Bill/Expense/BillCredit/Invoice) move to soft-delete-only, decide whether `is_approved` should require parent `IsDeleted=False`. My take: yes — a voided/soft-deleted bill shouldn't read as "approved" for completion-gate purposes. Wire when the gate lands, not before.

## Agent fleet — restore bill creation after universal-PDF rule lands

- [x] **Bill agent flow restored after the `attachment_public_id` requirement landed (2026-04-27).** `entities/bill/intelligence/tools.py::CreateBillArgs` now declares `attachment_public_id` as REQUIRED, and `bill_specialist`'s prompt teaches it to pass the bridged Attachment public_id verbatim from the email_specialist's task description (for email-agent flows) or to ask the user to upload via `/upload/attachment` first (for human chat flows). Verified end-to-end against a real Walker Lumber invoice — the agent's create_bill approval card now carries `attachment_public_id`. **Expense / BillCredit specialists do NOT yet need this** — only Bill has adopted the universal-PDF rule. Re-open if/when those entities adopt it too.

## Email-agent pipeline (2026-04-27)

- [ ] **Phase 3 — Pending Approvals React UI.** Without it, approval requests created by the email_specialist sub-session (or any unattended agent run) sit in `AgentApprovalRequest.Status='pending'` invisible. Backend endpoint: list pending approvals where the parent session has no live SSE listener. Frontend: new tab in `BuildOneTray` (or a header bell) reusing the existing `ApprovalCard.tsx`. Cross-session approve/reject/edit POSTs to the existing `/runs/{public_id}/approve` URL on the matching sub-session.
- [x] ~~**Wire `mark_email_outcome.agent_session_id`.**~~ ✅ **DONE** (re-verified 2026-07-11 — `entities/email_message/intelligence/tools.py:347-348` forwards `ctx.session_id` as `agent_session_id`, so `EmailMessage.AgentSessionId` is populated). Historical: endpoint accepted it but the tool didn't pass it, so `EmailMessage.AgentSessionId` stayed NULL. The cleanest fix is to expose the agent's own `session_public_id` through `ToolContext` and have the tool look up the BIGINT id (single SELECT). Linkage is recoverable via `AgentSession.UserMessage` which always contains the email public_id, but a real FK would be cleaner for joins.
- [x] **Stale-`processing` recovery.** Shipped 2026-05-06. `dbo.RecoverStuckProcessingEmailMessages` sproc + 5-min `recover_stuck_email_processing` Function App timer. Each row carries `ProcessingResetCount`; over-budget rows dead-letter to `failed`. See "Function App scheduler — reliability gaps" section below.
- [ ] **Anthropic API rate-limit back-pressure for email_specialist.** Today's recovery cron (`RecoverStuckProcessingEmailMessages`) cycles stuck EmailMessage rows correctly, but when the agent's transport-level retries (3× exponential backoff in `intelligence/transport/anthropic.py`) exhaust on sustained 429s, the recovery cron just throws the same rate-limited work back at Anthropic. We saw this on 2026-05-06: 4 emails (Walker Lumber 202865, Green Acres 20669, Gant's Lawn forward, Alley-Cassetty) burned through ProcessingResetCount=2/3 against a 429 storm. Fixes worth considering, in order:
  - **(a) Verify prompt caching is on for email_specialist.** Cached input tokens don't count against ITPM (except on older models). The system prompt + tool defs are stable per run; caching them likely halves effective ITPM. Inspect `intelligence/transport/anthropic.py` for `cache_control` on the system block.
  - **(b) Concurrency cap on `process_one` dispatch.** `process_email_inbox` inner-drains back-to-back; on a fresh queue that fan-outs N parallel agent runs against Anthropic in seconds. An `asyncio.Semaphore(1)` (or 2) around `start_run` would smooth the burst.
  - **(c) `RetryAfter DATETIME2` column on `EmailMessage`.** When the transport layer surfaces a 429 with `Retry-After`, propagate that up through the agent runner and stamp it on the email. The recovery cron skips rows whose `RetryAfter > NOW()`. Cleaner than today's "wait 10 min then retry blindly" — distinguishes "stuck because crashed" from "stuck because rate-limited."
  - **(d) Check the org's tier first.** https://platform.claude.com/settings/limits — if we're on Tier 1 or 2 and the email pipeline is the dominant workload, depositing a credit threshold to advance one tier may eliminate the symptom entirely without code changes. Tier progression is deposit-driven, not time-on-platform.
- [ ] **Expense / BillCredit routing in email_specialist.** v1 routes to bill_specialist only. If real-data classification shows credit memos or refund receipts often enough to be worth automating, add `delegate_to_expense_specialist` / `delegate_to_bill_credit_specialist` (one `make_delegation_tool` call each in `email_specialist/__init__.py`, plus prompt instructions for when to route which way). Today the agent flags those for human review.
- [ ] **DI cost / latency monitoring.** Each invoice ≈ $0.05 + ~5-10s. Add Application Insights logging around `extract_email_attachment` calls so we can watch volume + latency drift over time. Decide whether to bump the per-attachment cap if a packet email comes in with 10+ PDFs (current limit is implicit — agent's max_turns budget is 20).

## Bill SQL drift

- [x] **`dbo.BillCompletionResult` schema drift.** **RESOLVED 2026-07-12 — aligned base file to prod.** Investigation (read-only prod queries) confirmed prod is the correct evolved design: table `CompletedAt DATETIME2(3) NOT NULL DEFAULT SYSUTCDATETIME()`, permanent (no TTL); `UpsertBillCompletionResult(@BillPublicId, @ResultJson)` and `GetBillCompletionResult(@BillPublicId)` carry no `ExpiresAt`. The canonical, prod-faithful definition **already existed** in the sibling file `entities/bill/sql/dbo.bill_completion_result.sql` (incl. an idempotent ExpiresAt→CompletedAt migration); the drift was purely a **stale duplicate block** left behind in `dbo.bill.sql` (old `ExpiresAt`/1h-TTL design). Fix (Option B): **deleted the duplicate block from `dbo.bill.sql`**, leaving the sibling file as the single source of truth — fits the dir's one-object-per-file pattern and removes the duplicate-definition drift hazard for good. Repo code (`repo.py`) is column-name-agnostic (passes only `{BillPublicId, ResultJson}`, reads only `ResultJson`), so zero code change. **No prod migration** — prod is already correct; applying the sibling file is a CREATE-OR-ALTER no-op. Codex (gpt-5.5 xhigh, read-only) review PASS; `run_sql.py` GO-split of `dbo.bill.sql` now parses clean with zero `ExpiresAt` refs. **FOLLOW-UP (separate unit, out of scope here):** a full `run_sql.py entities/bill/sql/dbo.bill.sql` re-run audit — other batches in that file may carry their own independent drift; only the `BillCompletionResult` batch was reconciled here.

## Data hygiene

- [ ] **Audit which entities should use soft delete vs hard delete (cross-cutting).** The model is inconsistent today — `Vendor` soft-deletes (`IsDeleted` column + `SoftDeleteVendorByPublicId` sproc), but `CostCode` / `SubCostCode` / `Customer` / `Project` hard-delete. Some hard-delete entities probably want soft: anything FK-referenced from historical records (customers with old projects, sub-cost-codes referenced from old line items, projects on completed bills, etc.). For each entity decide: what FKs point at it, what's the audit/history value of keeping the row, what's the UX cost of orphaned references on hard-delete. Apply uniformly. The agent delete tools' approval gate stays the same either way — only the underlying behavior changes.

## API cleanup (do in a week, after Function architecture is proven stable)

- [ ] Delete `shared/scheduler.py` + remove `start_scheduler()` / `shutdown_scheduler()` calls from `app.py`
- [ ] Remove `apscheduler` from `requirements.txt` and `requirements-prod.txt`
- [ ] Memory savings: probably 50–100 MB per worker after the scheduler imports are gone
- [ ] Keep `ENABLE_SCHEDULER` env var for at least one more deploy cycle so rollback is still available; remove the code+flag together
- [x] Delete duplicate `read_paginated`/`count` in `entities/bill/persistence/repo.py` (lines 123 & 328, 161 & 369). The first definition of each is dead code — the second wins due to Python attribute order — **DONE 2026-07-12:** deleted the dead first defs (`get_connection()` versions); live `_conn_ctx` supersets kept (retain `ActorUserId`/`ActorIsSystemAdmin` UserProject scoping). AST regression test `tests/test_bill_repo_no_shadowed_methods.py` locks against re-introduction. Codex xhigh review PASS (behavior-preserving confirmed).

## Box Excel follow-ups (2026-07-06)

- [ ] **`InvoiceInvoiceConnector`: reconcile adds AND removals; survive partial inserts (HA-04).** (a) A mid-loop DB disconnect left 3/89 ILIs uninserted, and the connector's don't-re-sync-a-populated-invoice guard then refused all later adds — make the guard diff-aware (sync missing mapped lines) instead of all-or-nothing. (b) QBO line removals delete the mapping but strand the dbo ILI — the connector should delete the orphaned ILI (+ un-bill its source) and update the header. Playbook delta re-run section documents the manual recovery meanwhile.
- [ ] **Packet generator: dedupe identical attachments** — one PDF supporting multiple lines (NES statement, HA-04) prints once with a note listing the lines it supports, instead of once per line. Cosmetic.
- [ ] **SP↔Box DETAILS auto-reconcile (KI-39).** The two workbooks are one logical ledger synced only by outbox writes; human edits don't cross-propagate. Periodic col-Z-keyed comparison flagging divergent rows (values, H-tags, presence) into ReconciliationIssue.
- [ ] **Box packet re-version leaves stale `box.File` rows on repeated regens (HA-04).** Symptom: `box.http.request.failed` / non_retryable on the packet's 409 path + dangling registry rows, though the physical file stays singular. Registry upsert keys on BoxFileId (sound); suspicion: per-regen variance in the dbo `Attachment.filename` fed to the enqueue (each regen mints a new Attachment) producing distinct Box names/identities. Needs a live repro: capture the failing request log + the box.File rows for one invoice, then key the packet's Box identity on the invoice, not the per-regen attachment.


- [ ] **Self-healing `apply_rows_to_details` for blank-cost-code rows (KI-36).** When an incoming row's col-Z key already exists but the EXISTING row's col B/C is blank and the incoming row now carries a code, update B/C in place instead of skipping — the current skip freezes uncoded rows forever (WVA-18 root cause; SHT-22 $4,030 live AIA under-report). Companion: a periodic sweep (reconcile job or drain-time pass) that re-codes any blank-B DETAILS row whose col-Z public_id resolves to a now-coded dbo line, and flags stale-Z rows whose public_id no longer resolves (QBO re-pull leftovers) for manual clearing. Fill-in-place only — never insert_rows; never touch G702/G703 (fullCalcOnLoad recalcs them from DETAILS).
- [ ] **Gate Box Excel enqueue on SubCostCodeId.** `_enqueue_box_excel` (Bill/Expense/BillCredit) enqueues rows for uncoded lines that then land blank-B and freeze (KI-36). Either skip uncoded lines with a distinct log (they enqueue on a later re-sync once coded — requires the self-heal above so the re-sync isn't skipped), or hold the row until the line is coded. Decide jointly with the self-heal.

## Invoice pull-sync follow-ups (2026-04-26)

- [ ] **Connector auto-linking of Manual ILIs.** `InvoiceInvoiceConnector.sync_from_qbo_invoice` creates `dbo.InvoiceLineItem` rows with `SourceType='Manual'` and no source FK. The InvoiceAgent playbook does fingerprint-matching against `qbo.BillLine` + `qbo.PurchaseLine` after the fact. Move this into the connector itself so a fresh QBO pull lands with `BillLineItemId` / `ExpenseLineItemId` already set. Match keys: Description + Amount + ServiceDate=TxnDate + CustomerRefValue + RealmId. LineNum-align for ambiguous descriptions. Leave `Manual` only when both Bill and Purchase staging miss. **Doing this also closes the keyspace footgun from the OHR2-GUEST-09 incident** — the `qbo.Bill.Id` value would no longer surface to playbook callers, and Step 4 wouldn't need to query `qbo.*` in the first place.
- [ ] **Add a `delete_excel_rows(drive_id, item_id, worksheet, row_indices)` helper to `integrations/ms/sharepoint/external/client.py`.** Today the only undo for a wrongly-inserted Excel row is `clear_excel_range`, which leaves a blank row inside a Cost Code section instead of restoring the original layout. Microsoft Graph supports `POST /workbook/worksheets/{name}/range(address='A{r}:Z{r}')/delete` with body `{"shift": "Up"}`. Wire it through `MsGraphClient`, accept a list of row indices, sort descending, and delete bottom-up so indices stay stable. Useful any time an outbox-driven enqueue races a human's "wait, that's wrong" reaction.
- [ ] **Decide policy for human-driven outbox cancellation.** The `build.one.scheduler` Function App POSTs `/api/v1/admin/outbox/drain` every 30s, so any row enqueued by a human-driven script (InvoiceAgent, ad-hoc reconciliation) is likely drained before the operator can review and cancel. Options: (a) add a `ReadyAfter` delay knob to the enqueue helpers (`enqueue(ready_after_seconds=N)`) so an operator can opt into a 5-min holding pattern for human runs; (b) add a `Status='paused'` value the drain skips, plus an admin endpoint to cancel paused rows; (c) document operator workflow as "disable the scheduler timer in Azure for the duration of risky runs, then re-enable." Pick one and ship before the next manual playbook run.
- [ ] **Dedupe `dbo.Invoice` rows with numeric suffixes.** The 2026-04-26 invoice pull added 54 new rows; many have suffixes (`-2`, `-3`, `-4`) because BR-MAIN-20 / BR-MAIN-21 / SHT-19 / etc. were created locally first and the unique constraint on `InvoiceNumber` forced a suffix on the QBO copy. Decide policy: keep the QBO copy (most recently modified) and drop the local-only? Merge them? Leave both with documented semantics? Either way the connector should not create suffixed duplicates silently — surface as a `qbo.ReconciliationIssue` when it detects a name collision.
- [ ] **Cross-source-state drift detector.** Locally `IsBilled=True` (from a prior local-finalized invoice) while `qbo.BillLine.BillableStatus='Billable'` for the same mapped line is silent drift. BR-MAIN-21 left 22 such lines on customer 356 ($540K total). Today the user mitigates by manually deselecting them in QBO's Suggested Transactions tray. Add a reconciliation detector (`integrations/intuit/qbo/reconciliation/business/service.py`) that flags this state per realm — flag-only, no auto-fix, since the canonical fix is to push BillableStatus back to QBO and we deliberately don't push.
- [x] ~~**`qbo.InvoiceLine` should capture LinkedTxn.**~~ Shipped 2026-07-03 (`LinkedTxnType`/`LinkedTxnId` staging columns + sproc/repo/model threading). CAVEAT discovered on WVA-18 (2026-07-04): QBO drops the ReimburseCharge's reverse LinkedTxn once `HasBeenInvoiced=true`, so the RC→source hop is unavailable on completed invoices — the captured line-level pointer only groups amount+markup siblings post-invoicing. See the follow-up item below for the durable deterministic path. Playbook KI-32.
- [ ] **Move Box per-invoice subfolder resolution to drain time.** `_enqueue_box_line_pdfs` and `_enqueue_box_packet_upload` currently make a synchronous Box `POST /folders` (create-first, ~30s retry budget) inside the user's Complete Invoice request — twice per completion — violating the "completion pipelines enqueue, never call external APIs inline" convention. Under a Box outage this adds up to ~60s of blocking wall-clock before the gates skip. Fix: payload carries `{parent_box_folder_id, subfolder_name}`; the drain worker resolves/creates the child folder before upload (idempotent via the same 409 recovery). Removes both inline Box calls from the request path. (WVA-18 review follow-up, 2026-07-06.)
- [ ] **Capture ReimburseCharges into staging while un-invoiced (durable deterministic linkage).** The RC→source mapping exists only until the RC is consumed by an invoice. A `qbo.ReimburseCharge` staging table synced on the scheduler cadence (like bills/purchases) would preserve `{rc_id → source_txn_type, source_txn_id, source_line}` BEFORE QBO drops it; Step 4 could then resolve completed-invoice lines deterministically via the stored line-level `LinkedTxnId` → staged RC → source, demoting fingerprint to true fallback. Needs: external client query (exists: `query_reimburse_charges`), staging table + sprocs, sync script + scheduler timer, watermark. Sized ~1 day. (WVA-18 follow-up, 2026-07-06.)
- [ ] **Audit for split-staging duplicates in `qbo.Purchase` / `qbo.Bill`.** Surfaced on BR-MAIN-23 (2026-05-08): a single QBO Purchase (QboApi 69340 / Artistic Tile / $45,484.04) appeared as two `qbo.Purchase` rows in our staging, each holding only one of its lines (Line 1 NotBillable on the kept row, Line 2 Billable on the orphan row). Past-sync corruption — recipe is in playbook Step 3c. Audit query to find others before they bite a future invoice: `SELECT QboId, COUNT(*) FROM qbo.Purchase WHERE RealmId='9130353016965726' GROUP BY QboId HAVING COUNT(*) > 1;` — same shape on `qbo.Bill`. Surface findings, then heal each in turn via Step 3c.
- [ ] **Fix `InvoiceInvoiceConnector` re-run UNIQUE-constraint failure** — surfaced HP2-09 / Q44862, 2026-05-15. Per-line `InvoiceLineItemConnector.sync_from_qbo_invoice_line` fails on the second connector invocation with `UQ_InvoiceLineItemInvoiceLine_QboInvoiceLineId` violations, leaving N orphan ILI rows (created but unmapped). The connector should either UPDATE the existing mapping in place or catch the unique-constraint collision and reuse the existing ILI. Workaround documented in playbook Known Issue #20 (delete orphan ILIs before re-fingerprinting).
- [x] ~~**Fix `BillService.sync_to_excel_workbook` to fall back to `Amount` when `Price` is NULL**~~ — shipped 2026-07-03 in the single-bill MS sync AND the Box row builder (parity with `sync_bills_batch_to_excel`); the DB side of `/reconcile` uses the same rule. Playbook KI-16.
- [x] ~~**Audit `dbo.Project` for duplicate same-Name rows.**~~ Closed 2026-06-03. Root cause was NOT project_specialist — it was `CustomerProjectConnector.sync_from_qbo_customer` creating a fresh `dbo.Project` whenever the QboCustomer had no `qbo.CustomerProject` mapping, with no name-match guard. Fix: a70dea8 added name-match-before-create + `duplicate_qbo_customer` reconciliation issue for QBO-side dups (e.g. SJC). Belt-and-suspenders: `UQ_Project_Name_CustomerId_Active` filtered unique index (see [add_uq_project_name_customerid_active.sql](scripts/migrations/add_uq_project_name_customerid_active.sql)). All 22 historical dups merged via [cleanup.project_duplicates.sql](intelligence/persistence/sql/cleanup.project_duplicates.sql). Audit doc: [docs/dedupe-project-rows.md](docs/dedupe-project-rows.md). Full incident write-up in SESSION_NOTES.md (2026-06-03).
- [x] ~~**Add `BillCreditService.sync_to_excel_workbook`.**~~ Exists as `BillCreditCompleteService.sync_to_excel_workbook(bill_credit, line_items, project_id)` (+ Box mirror `._enqueue_box_excel`). Playbook Step 7b + KI-26 updated 2026-07-03.
- [ ] **Fix `InvoiceInvoiceConnector` storing `Price = abs(Amount)` on BillCreditLineItem-sourced ILIs.** Connector stores Price as positive magnitude even when Amount is negative (credit). Display-layer workaround deployed 2026-05-28 (`_toc_signed_amount` in `entities/invoice/api/router.py` negates for credit source type). Root fix: connector should preserve the sign on Price. One-time backfill after fix: `UPDATE dbo.InvoiceLineItem SET Price = -ABS(Price) WHERE SourceType = 'BillCreditLineItem' AND Amount < 0 AND Price > 0` — verify that's the only drift shape before running. Surfaced on BR-MAIN-24 where the $21K credit rendered positive, inflating the TOC grand total by $42K.
- [ ] **Investigate `InvoiceLineItemConnector` "Mapping exists but InvoiceLineItem N not found" log line** — fires during re-run even when the referenced ILI is clearly present in `dbo.InvoiceLineItem`. The connector's lookup may be running through a service that applies the data-isolation UDF and filtering out rows that ContextVar should have bypassed. Worth a closer read of `InvoiceLineItemConnector.sync_from_qbo_invoice_line` to confirm it's using the system-admin path.
- [ ] **Fix `PurchaseExpenseConnector` (and `BillBillConnector`) orphan-ELI/BLI side effect on mid-cycle upsert** — surfaced MR2-MAIN-07 / Home Depot Purchase 67915, 2026-05-19. When the user assigns an Item to a previously-uncategorized line in QBO and the playbook force-pulls + `upsert_from_external` that purchase, `qbo.PurchaseLine` gets a fresh PK (staging row is replaced, not updated). The connector treats the new line as new, CREATES a new `dbo.ExpenseLineItem` (with the new `SubCostCodeId` populated), and tries to DELETE the old ELI — which fails on `FK_InvoiceLineItem_ExpenseLineItem` because the active invoice still references the old ELI. Worse, the cascade-style delete inside `ExpenseLineItemService.delete_by_public_id` runs in order `blob → Attachment row → ELA → ELI` with each step in its own try/except; the blob delete *succeeds* before the FK error kills the rest, leaving the dbo.Attachment row pointing at a 404 BlobUrl. Net state: orphan new ELI (has SCC, on no invoice), old ELI patched onto the invoice but with no SCC and no ELA link, and an Attachment row whose Azure blob is gone. Today's workaround is documented in InvoiceAgent playbook CRITICAL #6 (patch in-place + re-link attachment + re-point mapping + delete orphan) and Known Issue #21; the blob loss is self-healed on the next `QboAttachableService.sync_attachables_for_*` call because the AttachableAttachmentConnector detects the missing blob and re-downloads from QBO. **Proper fix**: connector should UPDATE the existing `dbo.ExpenseLineItem` in place when the new `qbo.PurchaseLine` semantically corresponds to it (e.g., same parent QboPurchase + same LineNum, or matched by amount+description). Avoid the create+delete dance entirely. Same shape on the Bill side via `FK_InvoiceLineItem_BillLineItem`. Without this fix, every uncategorized-purchase-fix flow will leak a stale Azure blob and require a manual re-sync.

## QBO hardening follow-ups (2026-04-23)

- [ ] **Clean up stale `ReconciliationIssue` rows.** Before the `upsert_from_external` fix landed, `_reconcile_bill_qbo_missing_locally` always crashed in the connector and recorded the bill as a flagged issue with `LastError LIKE '%vendor_ref_value%'`. Those rows pollute the Ch6 drift count and alert #6. Query and delete (or mark resolved) once the fix has been deployed for one reconcile cycle. Approx query: `DELETE FROM [qbo].[ReconciliationIssue] WHERE DriftType='qbo_missing_locally' AND LastError LIKE '%vendor_ref_value%'` — verify count before running.
- [ ] **Un-defer task #8 (QBO test scaffold).** The `vendor_ref_value` bug in reconciliation + all three `_refresh_*` paths would have been caught by a single sandbox integration test. Revisit when App Insights lands (current blocker for #8 per Phase 2 notes).

## Bill Folder follow-ups (2026-04-24)

- [x] **Scheduled folder pickup** (API `5c2f6d9` + scheduler `54702bc`, 2026-04-26). Files dropped in `/source` are auto-enqueued by the scheduler's `enumerate_bill_folder` timer (every 5 min) via `POST /api/v1/admin/bill-folder/enumerate`. Hour-window dedup on item_ids prevents stuck files from churning a fresh queue row every tick. Drain timer (`process_bill_folder`) handles the actual processing.
- [x] **`_move_file_to_processed` silent-failure bug** (API `5c2f6d9`, 2026-04-26). Helper now raises on unrecoverable failure (post-conflict-retry path included); silent `try/except` wrappers in `process_single_item` removed. Move failures land in `BillFolderRunItem.LastError` and surface in the run's errors view instead of leaving PDFs stranded in `/source`.
- [ ] **Diagnose what's wrong with the SP move call.** With raise-on-failure live, the next 5-min enumerate-tick should surface the real reason the 61 stuck files won't move (permissions? locked files? something with the post-conflict retry?). Watch the `BillFolderRunItem.LastError` column on the next failed batch and fix accordingly. Once root cause is known, manually clear the stuck rows.
- [ ] **`_run_single_file_processing` still uses the in-process dict** and has the same cross-worker `-w 2` bug the bulk Process Folder flow had before today's refactor. Route is `POST /api/v1/process/bill-folder-single` in `entities/bill/api/router.py`. Fix by routing it through the same `BillFolderRunItem` table + tick pattern (one ad-hoc `BillFolderRun` with one item in it) — or by replacing with a direct synchronous call since it's already single-file. Low urgency (users rarely hit it), but it will silently 404 the polling loop when workers disagree.
- [ ] **If we add a third feature that needs the one-file-per-tick pattern**, generalize into `[tasks].[Run]` / `[tasks].[RunItem]` + a `TaskQueueService` so we stop duplicating sprocs. Pattern captured in `project_one_file_per_tick.md` memory.

## Observability / Ops

- [ ] App Insights alert rules on Function App failures: `drain_outbox` > 3 failures in 15 min; any QBO sync > 2 consecutive failures
- [ ] Structured cross-service correlation: Function sends its invocation id in a header; API logs it alongside `admin.job.completed`. Lets App Insights trace "Function ran X" → "API processed Y"
- [ ] Outbox lag metric: query `SELECT COUNT(*) FROM [qbo].[Outbox] WHERE Status='pending' AND ReadyAfter <= GETUTCDATE()` periodically; alert on sustained backlog
- [ ] Write runbook `docs/runbooks/scheduler-function-failure.md` documenting Symptom / Recovery (flip `ENABLE_SCHEDULER=true` + restart) / Verification

## Lower priority / backlog

- [ ] Verify (or add) `Pooling=Yes` in the container's `/etc/odbcinst.ini`. Without driver-manager pooling, each request opens a fresh pyodbc connection (TLS + SQL auth handshake). `pyodbc.pooling = True` alone in Python doesn't help unless the driver config enables it.
- [ ] Rate-limit admin endpoints. Low risk given `sp_getapplock` serializes, but belt-and-suspenders
- [ ] Cross-region latency audit: confirm Azure SQL is in the same region as App Service + Function App. A different region adds 50–200 ms per query
- [ ] Consider going `-w 4` once we confirm `-w 2` is steady. Memory easily accommodates it (current usage ~334 MB on 3.5 GB tier)
- [ ] Shared cache (Redis) for `bill-folder-summary` — eliminates per-worker cache miss when `-w 2` round-robins. Not urgent; Always On mitigates

## Function App scheduler — reliability gaps (May 6, 2026)

Discovered while diagnosing why no notifications fired post-deploy. The forward-of-original notification system works end-to-end (Bills 18391 / 18392 / 18393 each got a notification once their EmailMessage rows were processed). But the upstream pipeline that **feeds** those rows is misbehaving — three discrete problems:

- [x] **`poll_email_inbox` / `process_email_inbox` timer triggers don't reliably fire on schedule.** **Resolved 2026-05-06.** Root cause: Flex Consumption with `alwaysReady=null` scales the host to zero, and timer triggers don't have a wake-up mechanism on Flex. Same root cause as App Insights silence (below). Fix: `az functionapp scale config always-ready set --settings function:drain_outbox=1` keeps one host instance continuously running; all 18 timers in the app share that worker. Validated 60-min observation showed all timers firing at expected cadence with zero exceptions. Cost: ~$5–15/mo. See `reference_scheduler.md` auto-memory for the full Flex `alwaysReady` discipline.
- [x] **App Insights telemetry from the Function App is silent.** **Resolved 2026-05-06.** Two compounding issues: (a) the connection string pointed at an orphan resource `build-one-scheduler202604222226` (auto-created by an earlier `func publish`), not the intended `build-one-scheduler` (~zero ingest on either, masking the misroute); (b) Flex scale-to-zero meant nothing was running to emit telemetry anyway. Same root cause + fix as the timer-misfire item above. Repointed `APPLICATIONINSIGHTS_CONNECTION_STRING` to the correct resource (key `cf3ef0f3-…`), removed the legacy `APPINSIGHTS_INSTRUMENTATIONKEY`, dialed `host.json` sampling to exclude `Request;Exception;Trace`. Validated all 18 timers' requests are now ingesting.
- [x] **EmailMessage rows can get stuck in `processing` status with NULL `AgentSessionId`.** **Resolved 2026-05-06.** Shipped recovery cron (option a from the original mitigation list). New `dbo.RecoverStuckProcessingEmailMessages` sproc + `dbo.TimeoutLongRunningAgentSessions` sproc + new `POST /api/v1/admin/email/recover_stuck` admin endpoint + new `recover_stuck_email_processing` Function App timer (every 5 min at :01,:06,:11,…). Each EmailMessage carries a `ProcessingResetCount` (NEW column, DEFAULT 0); under-budget rows reset to `pending`, over-budget rows dead-letter to `failed`. AgentSession 30-min idle timeout (= 'failed' + reset linked EmailMessage). Validated end-to-end: existing 7 stuck rows recovered + AgentSession 124 timed out cleanly via the manual smoke; first scheduled fire at 23:11:00 UTC succeeded. **Future hardening (option b — atomic claim+session-create transaction)** still recommended as defense-in-depth: today the claim sproc commits before `start_run` inserts the AgentSession row, so the recovery cron is the only thing keeping the queue moving when failures hit between those steps. Atomic approach would require sproc cross-talk between `ClaimNextPendingEmailMessage` and the AgentSession insert path — not trivial; defer.
- [ ] **Delete orphan App Insights resource `build-one-scheduler202604222226`** after a few days of soak (zero ingest, retains 90d empty data): `az resource delete --ids "/subscriptions/c1767ed0-339f-4d5c-a0da-0d578bdb9972/resourceGroups/buildone_group/providers/microsoft.insights/components/build-one-scheduler202604222226"`. Root cause unknown but likely an earlier `func publish` auto-provisioned a duplicate when no connection string was set — be wary of running publish on a fresh deployment without `APPLICATIONINSIGHTS_CONNECTION_STRING` already configured.
- [ ] **Runbook for the recovery cron** (`docs/runbooks/email-message-stuck-processing.md`). Symptom (orphan row count rising) / Diagnosis (`SELECT COUNT(*) ... WHERE ProcessingStatus='processing' AND AgentSessionId IS NULL`) / Manual recovery (`POST /api/v1/admin/email/recover_stuck`) / Verification (counts back to 0). Low-priority since the recovery cron self-heals; useful when a row keeps re-stalling and dead-lettering.

## Rotate / security hygiene

- [ ] `DRAIN_SECRET` was leaked briefly during this session's debugging, was rotated mid-session. If you see unfamiliar activity in App Insights for `/api/v1/admin/*` endpoints, rotate again.

## Review-submit notifications follow-ups (Waves 3-5, May 2026)

- [ ] **Synth-email fallback for manual web-UI bill creation (no SourceEmailMessageId).** Today's notification trigger is forward-only: `ReviewNotificationService.enqueue_for_bill` early-returns with a log (`reason=no_source_email_message_id`) when `Bill.SourceEmailMessageId IS NULL`. That covers bill_folder intake (already-reviewed historical batch + path being phased out) but skips **admin-created bills via the React UI** — those have no source email to forward from, so no notification fires. Fix: when `SourceEmailMessageId IS NULL`, fall back to synthesizing a new email (the design from the original Wave 4 implementation) with the bill's source-summary PDF attached, the same HTML body template (Project / Vendor / Number / Amount + Submitted By / Date + reply-CTA), subject `{ProjectAbbrev} - {Vendor} - {Number} - {Amount}` (since there's no original to derive `FW: ` from). The outbox + worker handler already dispatch on `forward_message_id` presence — passing `None` falls through to the existing `create_draft` path. Plumbing partially deleted in this session's pivot to forward-only; recoverable from commit `fe0d1f1` (the original `_build_body` HTML builder + attachment-fetch logic in `_do_enqueue`). Pick up when the React UI's "create bill" path becomes a real intake source.
- [ ] **Smoke-test `REVIEW_NOTIFICATION_MODE=send` branch.** Wave 3 validated `mode=draft` end-to-end (graph_message_id stamped on outbox row Payload). The `send` path uses `me/sendMail` which returns 202 with no body; handler treats absent `draft.message_id` as a normal success. Verify by enqueueing a `mode=send` outbox row once we're ready to flip out of draft mode. **Edge case to fix at the same time:** `integrations/ms/mail/external/client.py::send_message` unconditionally sets `toRecipients` (passes empty list when no TO recipients). When BCC-only is the configuration, that may fail Graph validation. Patch `send_message` to only set `toRecipients` when non-empty, mirroring the existing `create_draft` behavior. Mode=draft works today because `create_draft` already handles this conditionally.
- [ ] **Reply-handling for approval workflow.** PMs reply to the notification email with "Sub Cost Code: 5040, Description: ..." or "Not approved." Replies route back to `invoice@rogersbuild.com` (the From: address) where the email-agent classifies them as `internal_reply` per its controlled vocabulary — currently NOT acted on. Build a parser + Review-status advancer (or decline). Out of scope for Wave 4 per user direction; pick up in a dedicated session.
- [ ] **APNs push sender.** `entities/device_token/` registers iOS device tokens but no sender exists. Build the HTTP/2 + JWT-auth APNs sender stack. Decide on outbox shape: new `[notify].[Outbox]` table OR piggyback `[ms].[Outbox]` (poor fit; APNs isn't MS Graph). Cert/key handling — use Key Vault or App Service env. Token-revocation handling on send failures.
- [ ] **React in-app "Bills awaiting your review" surface.** Cheap (~1 wave) once React is deployed. Page lists open Review rows where current_user is a PM/Owner per `UserProject.RoleId`. Sidebar badge with unread count.
- [ ] **Deep-link CTA button in email body.** Currently the body has plain text "Log in to the build.one app to approve or decline" — no clickable button. When React deploys to a public URL, swap in `[ Review this bill ]` linking to `<web-host>/bill/:id/edit`.
- [ ] **React UI to manage `UserProject.RoleId`.** Admins populate role assignments via SQL today. Wire a UI on the Project edit page or User Profile page (dropdown: none / Project Manager / Owner). Until this lands, every new project needs manual UserProject seeding.
- [ ] **Resubmit-after-decline notifications.** Currently the trigger fires only on first-time submits (auto-Submit hook on `BillService.create`). The manual `/submit/review/bill/{id}` endpoint creates a fresh Submitted Review row but does NOT enqueue a notification. Decide whether resubmit should re-notify, and wire the trigger if yes.
- [ ] **Coalescing/digest mode.** When 6 Walker Lumber emails land in 60s, the PM gets 6 emails. v1 ships per-bill (1 bill = 1 email). Add a debounce window + multi-attachment digest if vendor cadence becomes noisy in practice.
- [ ] **`ReviewRecipient` join table for richer audit.** v1 captures the resolved recipient list inside the outbox row's Payload JSON (Audit option A from planning). If we ever want a queryable surface (e.g., "show me everyone notified about Bill X" in the React UI), add a typed join table.
- [ ] **Multi-email-per-user resolver.** `dbo.ResolveReviewRecipientsByBillId` picks the first Contact email per user (lowest Contact.Id). If a user has multiple Contact rows (e.g., chris@ + invoice@), only one is notified. To send to all of a user's emails, drop the `rn=1` filter in the sproc — emit one row per (User, Email) pair.
- [ ] **HTML-escape vendor/project/submitter names in email body.** `Walker Lumber & Hardware` round-trips OK because Outlook auto-escapes `&` to `&amp;`, but `<script>` or `<img>` would be injected as raw HTML. Internal use only for now; harden when external surfaces grow.
- [ ] **Daily check: projects with bills but no PM/Owner UserProject rows.** Easy to mis-configure a new project — bills get created, but resolver returns empty TO/CC. Cron query that surfaces these would catch silent gaps.
- [ ] **`Review.NotificationOutboxPublicId UNIQUEIDENTIFIER NULL` (option b from the audit-linkage discussion)** — non-breaking column add. v1 keeps audit linkage in `[ms].[Outbox].Payload` JSON (`review_id` + `bill_id`); querying "which email got sent for Review X" requires grep. If a React surface ever needs to show notification status inline on a Review row, add the column + stamp it at enqueue time in `notification_service.enqueue_for_bill`. Until then, runbook + log queries suffice.

## TimeLog `CreatedByUserId` actor threading (audit-trail integrity, 2026-05-12)

- [ ] **Thread `current_user_id` through TimeLog Create sproc invocations.** Production `dbo.TimeLog` rows currently all show `CreatedByUserId = 17` (Christopher), regardless of which user actually created the log — meaning the Gap 2-T threading (`commit a80cff3`) landed on TimeEntry creates but missed TimeLog creates. The Create sproc accepts `@CreatedByUserId BIGINT = NULL` with `COALESCE(@CreatedByUserId, 17)` fallback; every TimeLog insert is silently using the fallback. Effect is audit-trail integrity only — TimeLog scoping uses parent `TimeEntry.UserId` (which is correct), so this isn't a data-leak issue. Find the TimeLog Create call site(s) in `entities/time_entry/business/time_log_service.py` (or wherever), thread `current_user_id.get()` through. Verify on next non-Christopher user create: row's `CreatedByUserId` matches the actor.

## iOS TimeEntry cache user-scoping (deferred to next MARKETING_VERSION, 2026-05-12)

- [ ] **Fix `TimeEntryService.loadFromCache()` to filter by `tokenStore.userId`.** The v0.1.1 planned fix shipped half-done: `CDTimeEntry` has the `userId` column + `upsert` tags rows correctly + `fetchByUser(_:)` filtered query exists, but `TimeEntryService.loadFromCache()` (`BuildOne/Services/TimeEntry/TimeEntryService.swift:91`) calls the unfiltered `CDTimeEntry.fetchAll(in:)`. Result: on a multi-user device where user A's cache is present and user B signs in, user B briefly sees user A's entries (~1-2 seconds) until the server response replaces in-memory state. **Server-side data is fully scoped** (RBAC Phase 3 sproc filter); this is a local-cache visual leak only. **Bundled with next MARKETING_VERSION bump** since fixing it under 0.1.0 doesn't justify a fresh Beta App Review cycle on its own. Fix is ~5 lines: swap `fetchAll(in:)` → `fetchByUser(tokenStore.userId, in:)` with a nil-guard. Same pattern we used for `CDProject` in the redesign cutover.

## iOS Time Tracking enum-driven sheet refactor (deferred — speculative, 2026-05-09)

- [ ] **Replace stacked `.sheet(isPresented:)` modifiers with single enum-driven `.sheet(item:)` per screen.** ActiveScreen has 5 sheet modifiers chained, TodayScreen has 3. SwiftUI historically had a "first sheet wins" bug with multiple isPresented modifiers; iOS 17+ seems to handle it but the pattern is fragile. Field testing exercised every flow successfully so this is **deferred as speculative**. If a specific repro emerges (e.g., a sheet silently fails to present after a certain sequence), refactor to enum: `enum Sheet: Identifiable { case clockOut, startBreak, switchNote, switchPicker(note:String), editLog(TimeLog), staleRecovery }` + single `.sheet(item: $activeSheet)`. ~50 lines per screen × 4 screens. Files: `ActiveScreen.swift`, `TodayScreen.swift`, `BreakScreen.swift`, `PastDayScreen.swift`.

## Time Tracking auto-submit (deferred — field-test first, 2026-05-09)

Context: iOS v0.2.0 ships the slate Time Tracking redesign with a deliberate two-step Clock-out → Submit flow. Risk: a super clocks out at end of day, drives home, never opens the app again — the day rots in draft, the PM never sees it. Discussed Phase 1g (in-app banner on next-day open + EOD local notification) as a recovery UX, then decided on the simpler path: server-side auto-submit at midnight cutoff, with no iOS-side change. **Holding off on implementation** until field testing tells us how often the gap actually causes problems. If field reports show supers routinely forget to submit, build this. If they don't, drop the plan.

When picking this up, the agreed plan:

- [ ] **`AutoSubmitStaleDrafts` stored procedure** in `entities/time_entry/sql/dbo.time_entry.sql` (idempotent `CREATE OR ALTER`). For each TimeEntry with `current_status='draft'` (resolved via most-recent `TimeEntryStatus` row, same pattern as existing read sprocs) AND `work_date < CAST(GETDATE() AS DATE)` AND has at least one TimeLog AND every TimeLog has `clock_out IS NOT NULL`: insert a new `TimeEntryStatus` row with `Status='submitted'`, `UserId=17` (Christopher; matches the established `COALESCE(@CreatedByUserId, 17)` system-context pattern), `Note='Auto-submitted by midnight cutoff'`. Single transaction. Returns count of submitted entries. Skip already-non-draft entries (idempotent).
- [ ] **Apply the new sproc** via `python scripts/run_sql.py entities/time_entry/sql/dbo.time_entry.sql` (or as a fresh migration file once base-rebake from the Gap 2 follow-up TODO is done).
- [ ] **Admin endpoint** `POST /api/v1/admin/time-tracking/auto-submit-stale` — `X-Drain-Secret` gated (returns 503 when unset, 401 on mismatch — match existing admin pattern in `shared/api/admin.py`). Calls the sproc; returns `{"status": "accepted", "count": N}`. File: `entities/time_entry/api/router.py` (or sibling `admin.py` if convention prefers).
- [ ] **Scheduler timer** in `build.one.scheduler/function_app.py`. Cadence: daily at **06:00 UTC** (= 02:00 ET) — past midnight ET, before any super reopens the app the next morning. POSTs to the admin endpoint with `DRAIN_SECRET` header. Document alongside the existing 18 timers.
- [ ] **Verification**: (a) run sproc once manually, confirm draft test data transitions and second call is no-op; (b) `curl -X POST -H "X-Drain-Secret: $DRAIN_SECRET" $BASE_URL/api/v1/admin/time-tracking/auto-submit-stale` returns expected envelope; (c) check `az functionapp function list` shows the new timer; (d) Application Insights confirms the next fire.

**Decisions deferred until pickup:**
- Whether to use `UserId=17` (Christopher) for the audit row or build a dedicated `system-auto-submit` agent user (cleaner audit, slight extra setup). The `Note` field already disambiguates; user 17 is consistent with the established system-context pattern. Likely fine to defer the dedicated user until needed.
- Whether to ship a "preview / dry-run" admin endpoint that returns the count of would-be-submitted entries without actually submitting. Useful for first-week validation; minor extra code.
- Multi-timezone handling: today single-customer in Brattleboro VT (Eastern). 06:00 UTC works. If we expand westward (CT/MT/PT), bump the timer to **09:00 UTC** so it fires safely past midnight in all US timezones.

**Fallback path if auto-submit proves wrong:** Phase 1g (banner + notification) was scoped at ~150-200 lines of iOS code — see session transcript or memory. Larger scope, preserves the user's intentional review beat, costs more to maintain. Auto-submit is the simpler bet; revisit if PMs complain about being unable to edit auto-submitted days from the back office, or if supers want the review-before-submit beat back.

## Side discoveries from bill-review session (2026-05-18)

Surfaced during the per-bill review + Complete of 42 Draft+(no-review) bills via `POST /api/v1/complete/bill/{public_id}`. None blocked the session; all worth fixing.

- [x] ~~**`POST /api/v1/create/bill-credit-line-item` returns 400.**~~ Fixed 2026-07-11 (option (b)). Root cause: the instant-workflow dispatcher (`core/workflow/business/instant.py:359`) injects `tenant_id` into every service call as a keyword, but `BillCreditLineItemService.create` (`entities/bill_credit_line_item/business/service.py`) lacked the param and had no `**kwargs` → `TypeError` → `success=False` → router 400. Fix: added `tenant_id: Optional[int] = None` as the first keyword-only param on `create()` (accepted-but-ignored, matching sibling `update_by_public_id`; `CompanyId DEFAULT` handles tenancy — no behavior change). **A DB-free audit (AST scan of all 47 instant-workflow entities) found the sibling `delete_by_public_id` on the SAME entity broken by the identical omission** (`delete`→`delete_by_public_id`, dispatcher injects `tenant_id`) → `DELETE /api/v1/delete/bill-credit-line-item/{id}` also 400'd; folded its one-line fix into the same unit (`public_id` positional, then `*, tenant_id`). Regression test `tests/test_bill_credit_line_item_signature.py` locks the convention for both methods (present, keyword-only, default `None`). It was the only other at-risk method in the audit. Codex PASS; `pytest` 54 green.
- [x] ~~**Make the instant-workflow dispatcher signature-aware (shared-infra follow-up, from the 2026-07-11 bill-credit-line-item fix).**~~ Done 2026-07-11. `core/workflow/business/instant.py` now uses `_build_service_kwargs()` (`inspect.signature`) to inject `tenant_id` only when the target method accepts it (explicit `tenant_id` param or `**kwargs`); otherwise kwargs pass through unchanged — a future service omission degrades gracefully instead of `TypeError` → 400. Zero behavior change for current code: the 2026-07-11 AST audit found all 47 registry entities × 135 dispatched methods already accept `tenant_id` or `**kwargs`. Regression tests: `tests/test_instant_dispatch_tenant_injection.py`. Per-service `tenant_id` params are now belt-and-suspenders rather than load-bearing.
- [ ] **HP2 project naming inconsistency** — DB has `Project.Name = "HP2 - 4406 Harding Pike"` but every invoice (Walker Lumber DOC#s 802791/191612/193416/192967/195415/195997/etc., JTA `26-8778`, Liaison Home Automation `9329`) ships to "4406 Harding **Place**" / "4406 HARDING PL". Likely a DB typo at project creation. Verify with the customer / Austin which street name is correct (Pike vs Place), then update `Project.Name` and `Project.AddressLine1` (if separately stored). The wrong suffix mostly cosmetic today, but it makes the SharePoint folder prefix "HP2" inconsistent with the actual street name on archived invoice filenames.
- [ ] **OHR2 "Bluebird" / "Bluebirds Landing" alias documentation** — multiple invoices reference the project as "BLUEBIRD - ADDL RECESSED" (Visual Comfort), "BLUEBIRDS LANDING" (Ferguson), or `BLUEBIRD - ROGERS BUILD` (shipping label) even though DB `Project.Name` is `OHR2 - 1539 Old Hillsboro Road`. Per `Vendor.Notes` / `Project.Notes` pattern in CLAUDE.md, address aliases should be captured in `Project.Notes` so `FindProjectForInvoice` can resolve "Bluebird/Bluebirds Landing" → OHR2. Action: populate `Project.Notes` for OHR2 with "also referred to as Bluebird / Bluebirds Landing" (similar pattern for SHT if 2012 Sunset Hills = "Levitre Residence" per Greenrise invoice `INV-GRT90115`).
- [ ] **MS outbox worker's SharePoint-upload → local DriveItem link is broken (silent).** `ms.DriveItemAttachment` table is empty across the entire DB (0 rows ever), and `ms.DriveItem` hasn't received any new rows since 2026-04-18 — yet bill completions have been uploading to SharePoint successfully throughout that window. Root cause: `MsOutboxWorker._link_attachment_if_requested` (`integrations/ms/outbox/business/worker.py:750-789`) passes `uploaded_item.get("item_id")` (a MS Graph item_id string like `017ZKYN5ZCTDWCW7UPQNFJHBZC6XZYUWJC`) to `DriveItemAttachmentConnector().link_driveitem_to_attachment(attachment_id, ms_driveitem_id)` — but that connector's `ms_driveitem_id` parameter is typed `int` (local DB PK to `ms.DriveItem.Id`), not a string. The INSERT either fails the FK check or coerces garbage, then the `except Exception` swallows the error per the "non-fatal" docstring. **Fix**: the worker needs to (1) INSERT a `ms.DriveItem` row from the `uploaded_item` payload first, capture its local int PK, then (2) pass *that* PK to `link_driveitem_to_attachment`. Or, simpler: change the linking to be keyed off `MsDriveItemId` (the string) rather than `ms.DriveItem.Id` (the int). Not blocking anything today because nothing reads `ms.DriveItemAttachment`. Becomes blocking if/when SharePoint-link surfaces are wired (e.g. "show me the SharePoint location for this attachment" UI button).
- [x] ~~**QBO customer/project sync is creating DUPLICATE `dbo.Project` rows.**~~ Closed 2026-06-03. `CustomerProjectConnector.sync_from_qbo_customer` now matches existing local Project by exact Name before creating (a70dea8); raises a `duplicate_qbo_customer` reconciliation issue when an existing Project is already bound to a different QboCustomer (e.g. SJC's 1367 vs 1444). 22 historical dups merged via [cleanup.project_duplicates.sql](intelligence/persistence/sql/cleanup.project_duplicates.sql) (handles repoint of `qbo.CustomerProject` from dup → keeper + delete of dup `Project`). Belt-and-suspenders: `UQ_Project_Name_CustomerId_Active` filtered unique index ([add_uq_project_name_customerid_active.sql](scripts/migrations/add_uq_project_name_customerid_active.sql)). Lesson: the fix shipped 2026-05-29 (a70dea8) but didn't run in prod for 5 days because App Service `DOCKER_CUSTOM_IMAGE_NAME` was pinned to `:0d63b19` instead of `:latest` — `az webapp restart` silently relaunched the old image while `:latest` pushes were ignored. New runbook: [docs/runbooks/deploy-tag-pinned.md](docs/runbooks/deploy-tag-pinned.md). CLAUDE.md "Deploy path" section updated with the `az webapp config container show` verification step.
- [x] ~~**`BillService.delete_by_public_id` doesn't cascade `Review` rows → delete fails with FK violation.**~~ Fixed 2026-05-22. Found while deleting Microsoft + VaVia draft bills: the delete handled BillLineItem → Attachment (+blob) cascade + `InvoiceLineItem.BillLineItemId` nullify, but left `dbo.Review` rows, so `DELETE FROM dbo.Bill` tripped `FK_Review_Bill` (547) → 409. Fix: new `DeleteReviewsByBillId` sproc (deletes `dbo.Review` + guarded legacy `dbo.ReviewEntry`) in `entities/review/sql/dbo.review.sql` + `ReviewRepository.delete_by_bill_id()`; `delete_by_public_id` now clears Review rows AND `MsMessageBill` links (via its existing repo) before the final delete, each raising a clear `ValueError` on failure instead of the cryptic 409. Sproc applied to DB; **service code change needs a prod deploy to take effect** (the manual workaround already handled the 6 bills this session).
- [ ] **FP - 4005 Franklin Pike (project #35) has no SharePoint folders / Excel link.** Found 2026-05-22 — `ms.DriveItemProjectModule` and `ms.DriveItemProjectExcel` are both empty for FP. Bill #18545 (Puente's Drywall, QBO-linked + IsDraft=False) couldn't store its document because of this. Set up the FP project's module folders + Excel workbook link (same as other active projects), then re-run the SharePoint upload + Excel sync for #18545. Until then #18545 is "complete" in QBO + locally but its doc isn't archived in SharePoint.
- [ ] **`sync_attachments_to_sharepoint` legacy direct-upload path coexists with the outbox path.** `BillService.sync_attachments_to_sharepoint(bill_public_id)` at line 567 of `entities/bill/business/service.py` uploads to SharePoint synchronously via MS Graph (no outbox), no `ms.DriveItem` write, and is not idempotent vs the outbox path (re-running both produces a `… (1).pdf` duplicate on SharePoint). The modern path is `_upload_attachments_to_module_folder` (line 1979) which enqueues via `MsOutboxService.enqueue_sharepoint_upload`. The legacy method is callable but mostly unused — `complete_bill` doesn't call it. Pick one: either delete the legacy method outright (audit callers first), or document it as "bypass-only escape hatch for recovery scenarios" and add a comment warning about the duplicate-filename behavior if both paths fire.

## Mobile auth — refresh token UX (2026-06-02)

Surfaced during the iOS BuildOneApp.swift failure-mode review pass. Two related but distinct issues with the mobile auth flow; neither was blocking the iOS work but both are worth fixing before more field workers come online.

- [ ] **Bump `refresh_token_expire_seconds` from 86400 (24h) to ~30-90 days for the mobile flow.** Current value forces field workers to re-log in roughly every shift — 24h is a "kiosk / high-security web app" TTL, not a "field iPhone" TTL. Industry norm for mobile refresh tokens is 30-90 days because mobile devices spend long stretches in airplane mode / dead zones / closed. Biometric (Face ID) softens the re-login UX into a single tap, but only for users who opted in during onboarding AND whose biometric Keychain item hasn't been invalidated (Face ID re-enrollment invalidates it). One-line change in App Service Application Settings: `refresh_token_expire_seconds=2592000` (30d) or `7776000` (90d). The iOS code already handles whatever expiry the server returns and stores the rotated refresh_token on each `/mobile/auth/refresh` response — no client-side change needed.
  - **Web auth path is unaffected**: web uses the cookie-based refresh flow on a different endpoint (`/auth/refresh`), and the cookie's `max_age` is set from the same `refresh_token_expire_seconds`. If you want a different TTL for web vs mobile, the cleanest split is a separate `mobile_refresh_token_expire_seconds` setting + `MobileAuthService.refresh_access_token()` using that one; today they share the value.
  - **Verification after bump**: log in on the iOS app, then check the JWT's `exp` claim on the refresh token returned by `/mobile/auth/login` — should equal `now + 2592000` (30d). Optionally, verify a 25-hour-old refresh token still works by manually setting the `RefreshToken.CreatedAt` back in the DB and hitting `/mobile/auth/refresh`.

- [ ] **Mobile refresh-token rotation race on app SIGKILL (lower priority).** `POST /mobile/auth/refresh` rotates: server creates new refresh token `R2`, invalidates `R1`, returns `R2`. iOS persists `R2` to Keychain after the response lands ([AuthenticatedSession.swift:89-91](https://github.com/chriszubrod/build.one.ios/blob/master/BuildOne/Services/BuildOneAPI/AuthenticatedSession.swift#L89-L91)). If the app is killed between (a) the server's rotation and (b) the Keychain write committing, the user is locked out — next launch sends `R1`, server says "rotated out", iOS surfaces `tokenRefreshFailed` → critical alert → login screen. **In production** this requires a crash / OOM kill / user force-quit during the ~10-50ms refresh window — rare but real. **In dev** it's far more common: Xcode's Stop button is SIGKILL, and we hit this during the iOS work pass (session was observed routed to login on 2026-06-02 after heavy rebuild iteration). Two possible fixes, both server-side:
  1. **Grace window (preferred)**: keep `R1` valid for ~5 minutes after `R2` is issued so a client that didn't persist `R2` can still authenticate with `R1` once. Requires `RefreshToken.RotatedFromId` + `RotatedAt` columns + a `RevokeOldRotation` background sweep that purges grace-window tokens after the window. Standard OAuth pattern.
  2. **No-op no-rotate response**: detect "already rotated" specifically (vs "expired") and return the previously-issued `R2` instead of 401, with idempotency keyed off the request body. Simpler but breaks the rotation invariant (caller could replay forever within the grace).
  - Option 1 is the correct shape long-term; defer until refresh failures appear in prod logs OR the iOS team builds a dev-loop tool that needs it.

- [ ] **Device-token rebind cleanup on `/mobile/device-token/register` (push leak prevention).** Today the iOS app calls `POST /mobile/device-token/deactivate` during `userInitiatedLogout` to clean up the `(deviceToken → userId)` mapping server-side. That call requires the just-logged-out user's auth token to authenticate, and silently fails if the network is down at the moment Sign Out is tapped (logged via `os.Logger` subsystem `one.build.app` category `push` from 2026-06-02). The premise that this is "non-fatal because the token expires naturally" is **wrong** — APNs device tokens don't expire on a schedule (they're stable per device + app), and the server-side mapping persists indefinitely. Without cleanup, when User B logs in on the same device, the dormant `(token, UserA)` mapping can leak pushes addressed to UserA into UserB's notification center once the push sender ships. **Fix shape**: `/mobile/device-token/register` should atomically clear any prior `(token, *)` mappings before/while inserting the new `(token, newUser)` row. One device-token has at most one user at a time; the server is the only place that has authoritative knowledge to enforce that invariant (the client can't retry post-logout because its auth context is already cleared). Touches `dbo.DeviceToken` (likely a new `UpsertDeviceTokenAndClearPriorMappings` sproc or an `INSTEAD OF INSERT` trigger) and possibly the `entities/device_token/` service to thread the cleanup through. Higher priority **before** the push sender ships — once notifications start going out, this becomes a privacy bug. Today (no sender) the failure surface is purely latent.



- [ ] **BillCredit completion never writes the `DriveItemAttachment` mapping** (found during QBO Step 5 review, 2026-06-22). `BillCreditCompleteService` (completion path) enqueues the SharePoint upload with `attachment_id`, but the credit-side link write extracts the DriveItem id with a different key than the outbox worker's `uploaded_item.get("item_id") or uploaded_item.get("id")`, so the `attachment → DriveItem` link is never persisted for completed credits. Effect: `DriveItemAttachmentConnector.is_attachment_synced()` always returns False for credit attachments; any feature relying on that link (unlink, sync-state checks) is blind to credit uploads. Bills/Expenses are unaffected (worker links them correctly). Low urgency — SharePoint upload itself still works (conflictBehavior=replace); only the back-link is missing. Fix: align the credit completion's DriveItem-id extraction with the worker's, or let the worker's `_link_attachment_if_requested` be the sole linker. NOT a QBO-pull bug (Step 5 removed its dependency on this link).

- [ ] **QBO-pulled entities have no Workflow/WorkflowEvent audit row + default to CreatedByUserId=17** (found during QBO Step 7, 2026-06-22). `ProcessEngine.execute_synchronous` (the only writer of Workflow/WorkflowEvent rows) is invoked ONLY in the API routers (`entities/{bill,expense,bill_credit}/api/router.py`); the QBO pull goes scripts → connectors → `service.create/update` directly, bypassing it. So pulled bills/expenses/credits get NO audit-trail rows, and because `assert_cli_system_admin()` sets `current_user_id=None`, their entity-row `CreatedByUserId` falls to the column DEFAULT (17 = Christopher) — mildly misleading (a person who didn't create them). **Currently latent**: Workflow/WorkflowEvent tables are internal-only (no React surface / admin endpoint / report consumes them as of 2026-06), and the pull batch IS tracked via `Sync` records + `intake_source`. **Decision (Step 7): ACCEPT the audit gap, DEFER the attribution fix.** The attribution fix is NOT trivial: `current_user_id` is the SAME ContextVar that drives RBAC user-scoping, so setting it to a real user to fix attribution also changes the pull's access-scoping identity. Pull reads are almost all by-id/by-mapping (not user-scoped per "list-only first"), so it's *probably* safe, but it should be done deliberately: (1) create a dedicated `qbo_sync` (or reuse `claude_agent`) system user, (2) audit every read in the post-Steps-4-6 pull path to confirm none is user-scoped, (3) set `set_authz_context(user_id=<sync_user>, is_system_admin=True)` in the pull. Full Workflow-audit integration (writing Workflow rows per pulled entity) is over-engineering while nothing reads those rows — revisit if a Workflow-audit surface or compliance requirement appears.

- [ ] **Push-to-QBO (app→QBO) is Bill-only despite field_ownership declaring Expense + BillCredit bidirectional** (found during QBO Step 8, 2026-06-22; OUT OF SCOPE for the pull audit — logged for a future OUTBOUND / round-trip review). Current outbound state:
  - **Bill — fully wired (LIVE, load-bearing — do NOT remove):** `complete_bill` → `_enqueue_qbo_sync` (`entities/bill/business/service.py:1557,1606`) → `QboOutbox` kind `sync_bill_to_qbo` → worker `_handle_sync_bill` (`integrations/intuit/qbo/outbox/business/worker.py:64`) → `BillBillConnector.sync_to_qbo_bill`.
  - **Expense (Purchase) — machinery COMPLETE but DORMANT (trigger never wired):** connector `PurchaseExpenseConnector.sync_to_qbo_purchase` exists (`integrations/intuit/qbo/purchase/connector/expense/business/service.py:260`) + worker handler `_handle_sync_expense` dispatches kind `sync_expense_to_qbo` (`outbox/worker.py:65,460`), but NOTHING enqueues it (`complete_expense` has no `_enqueue_qbo_sync`; no push endpoint). One wiring line away from live.
  - **BillCredit (VendorCredit) — NOT implemented:** no `sync_to_qbo` connector in `integrations/intuit/qbo/vendorcredit/`, no worker handler (dispatch table is bill/expense/invoice only). Only the declaration exists in the field-ownership registry.
  - **field_ownership.py declares all four bidirectional** (Bill:88, Invoice:123, Purchase=Expense:143, VendorCredit=BillCredit:170) with APP_OWNED/QBO_OWNED rules — but only Bill is actually wired. The declarations look aspirational, not a wired contract. (Invoice push connector+handler also exist but completion deliberately doesn't push per CLAUDE.md.)
  - **Circular-push safety (why the asymmetry hasn't bitten):** push only fires on COMPLETION, and QBO-pulled entities are projected is_draft=False and never go through complete_*(), so a pulled entity never pushes back. Enabling expense push would therefore only affect APP-completed expenses, not pulled ones — but the connector must be verified mapping-aware (upsert, not blind-create) first.
  - **Decision:** treat enabling expense push (small — machinery exists) and building BillCredit push (larger lift) as DELIBERATE outbound features gated on a real "we create these in-app and need them in QBO" need + mapping-aware idempotency — NOT a mechanical "make them match the pull" change. Do NOT remove the working Bill push.
